---
phase: 03-programmatic-location-service-engine
plan: 20
type: execute
wave: 7
depends_on: ["03-19"]
files_modified:
  - web/app/page.jsx
  - web/app/services/[service]/page.jsx
  - web/scripts/check-html-locks.mjs
autonomous: true
requirements: [REQ-hub-and-spoke-architecture, REQ-programmatic-page-scale]

must_haves:
  truths:
    - "Every page that Lock 6 names carries an InterlinkBlock computed from geography"
    - "No two identical band tones sit adjacent on any page, counting the interlink as paper"
    - "The staged pending set is empty and the positive half of the lock holds everywhere"
  artifacts:
    - path: "web/app/page.jsx"
      provides: "the locations teaser InterlinkBlock and the corrected FAQ band tone"
      contains: "InterlinkBlock"
    - path: "web/app/services/[service]/page.jsx"
      provides: "the towns-we-cover InterlinkBlock on all seven service pages"
      contains: "InterlinkBlock"
  key_links:
    - from: "web/app/services/[service]/page.jsx"
      to: "web/content/interlinks.js"
      via: "serviceTowns, bounded at 8 links"
      pattern: "serviceTowns"
---

<objective>
Close the last eight gaps in Lock 6: the home page and all seven service pages gain the
InterlinkBlock the design system has been waiting for since Phase 1, and the FAQ band on those eight
pages moves `paper` → `tint` so the new block does not sit paper-on-paper.

Purpose: the self-restoring gate has done its job. `INTERLINK_LOCK_ACTIVE` forced a decision by
asserting the inverse; plan 03-01 turned that into a per-template rule with a staged pending set;
this plan empties the set. After it, every page `design-system.md` §4's Lock 6 names carries a
geography-computed interlink block, and the blanket rule is a blanket rule with no exemptions.

Output: eight pages corrected, one staged set emptied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Binding sections:
- `03-UI-SPEC.md` §4 (the band-adjacency rule and the boxed note: *"This breaks eight pages that ship
  today, and Phase 3 is what breaks them"*), §5 (the `<h2>` deck), §12 delta 16
- `03-PATTERNS.md` — *Band tone, and the two colour rules that are easy to break*

> **§4, quoted because it is the whole plan:** `InterlinkBlock` renders
> `<section class="bhc-interlink">` with its own container and **no ground of its own**, so it paints
> as `paper` and carries no `bhc-section--*` class. It counts as a `paper` band for adjacency, and it
> may not sit immediately before or after a `tone="paper"` `SectionBand`. Home and every service page
> currently end `tint → warm → paper (FAQ) → navy`, and the only slot for the interlink — after the
> rail, before the FAQ, where `design-system.md` §3.1 and §3.2 both put it — lands a paper interlink
> immediately above a paper FAQ band. **Fix: the FAQ band moves `paper` → `tint`**, giving
> `warm → paper (interlink) → tint → navy`. One prop per file.

**All three tasks land in ONE commit. Do not commit between them.** Tasks 1 and 2 leave the suite
deliberately red — the staged pending set asserts the inverse until task 3 empties it — and CI runs
on `branches: ['**']`, so a commit between them publishes a red head on a branch whose required
checks gate the merge.
</context>

<interfaces>
```jsx
// Home — web/app/page.jsx. One block, 8 links to published town hubs, heading `Towns we cover`.
import { homeLocationsTeaser } from '@/content/interlinks.js';
<InterlinkBlock {...homeLocationsTeaser()} />

// Service pages — web/app/services/[service]/page.jsx. One block, 8 links, heading
// `Towns we cover for {noun}` — except /services/move-in-cleaning, which has no combos at all and
// links to 8 published town HUBS under `Areas we cover`. That scoping lives in interlinks.js;
// this file calls one function and does not branch.
import { serviceTowns } from '@/content/interlinks.js';
<InterlinkBlock {...serviceTowns(service)} />
```

`move-in-cleaning` is **not** exempted from Lock 6. §5 scopes its destination set rather than the
requirement, which keeps Lock 6 a blanket rule — an exemption would be the first hole in it at
exactly the moment it starts mattering.

Band positions after this plan:

```
home     … tint ProcessSteps · warm ReviewRail · interlink(paper) · tint FAQ · navy CTABand
service  … tint ProcessSteps · warm ReviewRail · interlink(paper) · tint FAQ · navy CTABand
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The home page gains its locations teaser and its FAQ band tone</name>
  <files>web/app/page.jsx</files>
  <read_first>
    - web/app/page.jsx (whole file — the band-order header comment, the import block, and the
      numbered list of things the file deliberately does not do, one of which is the InterlinkBlock
      this task adds)
    - web/content/interlinks.js (`homeLocationsTeaser` — its heading, its bound and its meta)
  </read_first>
  <action>
Add `InterlinkBlock` to the package import set and `homeLocationsTeaser` to the content imports.
Render one block between the review-rail band and the FAQ band, spread from
`homeLocationsTeaser()`. It takes no `tone` and no wrapper `SectionBand` — it is not a `SectionBand`
and giving it one would nest a section inside a section and add a second ground.

Change the FAQ band's tone from the default `paper` to `tint`. That is one prop.

Update the file's header: the band-order table now reads
`… tint → warm → paper (interlink) → tint → navy`, and the numbered item that says this page
deliberately renders no InterlinkBlock is no longer true — replace it with the reason it now does,
and with §4's adjacency argument for why the FAQ band moved with it.
  </action>
  <verify>
    <automated>npm run build --workspace @bhc/web && node --test web/scripts/check-html-locks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The build exits 0. The lock suite is expected to be RED at this point on the `/` entry of the
      staged pending set — that is the self-restoring gate firing exactly as designed. Record the
      failure message; task 3 clears it.
    - On the built home page: exactly one `class="[^"]*bhc-interlink__list` match, exactly 8
      `class="[^"]*bhc-interlink__link` matches (the `class="` form throughout — a bare class-name
      count reads double because the RSC flight payload is inlined), and every one of their hrefs present in the prerender manifest.
    - The FAQ band carries `bhc-section--tint` and the interlink section immediately precedes it in
      document order. Extract the ordered list of `bhc-section--*` modifiers plus the interlink's
      position and quote it in the SUMMARY.
    - `grep -c 'SectionBand' web/app/page.jsx` is unchanged from before this task — the interlink was
      added, not wrapped.
    - No new `tel:` link: the page still carries at most four.
  </acceptance_criteria>
  <done>Home carries a geography-computed locations teaser and its band sequence has no adjacent identical tones.</done>
</task>

<task type="auto">
  <name>Task 2: All seven service pages gain their towns-we-cover block</name>
  <files>web/app/services/[service]/page.jsx</files>
  <read_first>
    - web/app/services/[service]/page.jsx (whole file — the band-order header and the six things it
      deliberately does not do, item 3 of which is the InterlinkBlock this task adds)
    - web/content/interlinks.js (`serviceTowns` — including the `move-in-cleaning` branch)
  </read_first>
  <action>
One file, seven pages. Add `InterlinkBlock` to the package imports and `serviceTowns` to the content
imports, render one block between the review-rail band and the FAQ band spread from
`serviceTowns(service)`, and change the FAQ band's tone to `tint`.

The route does not branch on the service. `serviceTowns` owns the `move-in-cleaning` case — it has no
combo pages, so it links to eight published town hubs under `Areas we cover`. Branching in the
template would put the exemption in the wrong layer and make it invisible to the module-load
assertions in `interlinks.js`.

Update the header's band-order table and replace item 3 of the deliberately-not-doing list with the
reason the block is now here, citing §4's adjacency fix for the FAQ tone change.
  </action>
  <verify>
    <automated>npm run build --workspace @bhc/web && node --test web/scripts/check-html-locks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The build exits 0. The lock suite is still expected to be red on the staged pending set until
      task 3; record which routes it names.
    - Each of the seven built service pages has exactly one `class="[^"]*bhc-interlink__list` match and 8
      `class="[^"]*bhc-interlink__link` matches, and every href is in the prerender manifest.
    - `/services/move-in-cleaning`'s block heading is `Areas we cover` and its 8 hrefs are all
      `/locations/…`. Quote the heading and one href.
    - `/services/short-term-rental-cleaning`'s block links only to towns that carry that service —
      three at batch 1 if `serviceTowns` is scoped to carriers, eight if it falls back to hubs.
      Record which behaviour shipped and confirm it matches plan 03-18's SUMMARY.
    - All seven pages' FAQ bands carry `bhc-section--tint`, and no page has two adjacent identical
      band tones counting the interlink as paper. Quote one page's full ordered tone sequence.
  </acceptance_criteria>
  <done>All seven service pages carry a bounded, geography-computed interlink block above a tint FAQ band.</done>
</task>

<task type="auto">
  <name>Task 3: Empty the staged set and prove the positive lock holds on all 167 routes</name>
  <files>web/scripts/check-html-locks.mjs</files>
  <read_first>
    - web/scripts/check-html-locks.mjs — the delta 16 rule and `INTERLINK_PENDING` as plans 03-01,
      03-14 and 03-15 left them
    - web/scripts/check-html-locks.mjs lines 258-300 (the record of how `NO_RATING_YET` and
      `NO_PRIMARY_CTA_YET` emptied, and why they were kept rather than deleted)
  </read_first>
  <action>
Empty `INTERLINK_PENDING`. Keep the set rather than deleting it, exactly as the two Phase 2 exemption
sets were kept: an empty set is the honest record that every page Lock 6 names is now held to the
positive half, and a route may be added back only with a spec clause naming it.

Write the sequence down beside it, in the style the neighbouring comment block uses: the gate was
seeded in plan 03-01 with eight routes, plan 03-15 added the seventh service page to it, and this
plan emptied it when the blocks landed. Nobody had to remember — the inverse assertion failed and
forced it.

Then run the full suite over all 167 routes. This is the first run in which delta 16's positive half
is asserted everywhere it applies: `/`, all seven `/services/*`, all 21 hubs and all 126 combos
required to have at least one; the ten utility routes, `/locations` and `/_not-found` required to
have exactly zero.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0 with zero failures.
    - `grep -c 'INTERLINK_PENDING' web/scripts/check-html-locks.mjs` returns 3 or more and the set is
      declared empty.
    - The positive half is non-vacuous: 155 routes are required to carry a block (1 + 7 + 21 + 126)
      and 12 are required to carry none. Print both counts from the harness run and record them.
    - The inverse still bites: temporarily remove the block from `web/app/page.jsx`, rebuild, confirm
      delta 16 goes red naming `/`, restore, confirm green. Record both outcomes.
    - `node web/scripts/check-budget.mjs` exits 0; record the worst page. Eight more interlink blocks
      of eight links each is a few hundred bytes, so a move of more than 5 KB is a finding.
    - Total internal links on one built combo page, one hub, one service page and the home page are
      all at or under 60. Print all four figures — plan 03-22's delta 19 is calibrated against them.
  </acceptance_criteria>
  <done>The staged set is empty, the positive lock holds on 155 routes and asserts zero on 12, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a dormant lock → a live lock | the moment a gate flips from asserting the inverse to asserting the rule is the moment it can be quietly weakened instead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-29 | Repudiation | `INTERLINK_PENDING` | mitigate | the set is emptied, not deleted, and the non-vacuity assertion proves 155 routes are held to the positive half; a route may re-enter only with a spec clause naming it |
| T-03-30 | Tampering | link volume | mitigate | every block is bounded at 8 by `interlinks.js`; task 3 measures the per-page total on four templates and hands the figures to delta 19 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- `npm run verify` exits 0.
- The four measured per-page link totals are recorded.
- `git diff --stat` touches only the three declared files.
</verification>

<success_criteria>
- Home and all seven service pages carry one geography-computed InterlinkBlock each.
- Those eight pages' FAQ bands are `tint`, and no page has two adjacent identical tones.
- `INTERLINK_PENDING` is empty and kept.
- Delta 16's positive half holds on 155 routes and its zero half on 12.
- `npm run verify` is green.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-20-SUMMARY.md` when done.
</output>
