---
phase: 03-programmatic-location-service-engine
plan: 16
type: execute
wave: 4
depends_on: ["03-15", "03-09", "03-10", "03-11", "03-12", "03-13"]
files_modified:
  - web/content/towns.js
autonomous: true
requirements: [REQ-programmatic-page-scale, REQ-fix-duplicate-coventry, REQ-canonical-service-taxonomy, REQ-content-depth-bar, REQ-hub-and-spoke-architecture]

must_haves:
  truths:
    - "One data file drives every generated page in the phase"
    - "A published town is published with all six of its combo pages or not at all"
    - "Incrementing one constant publishes the next batch and nothing else"
    - "No two towns share both composition-index values, and no batch clusters them"
    - "A missing prose fragment, a missing county note or a drifted service noun fails before a page is built"
  artifacts:
    - path: "web/content/towns.js"
      provides: "TOWNS[58], COMBO_SERVICES[7], PUBLISHED_BATCHES, PUBLISHED_TOWNS and every module-load guard"
      min_lines: 400
      exports: ["TOWNS", "COMBO_SERVICES", "PUBLISHED_BATCHES", "PUBLISHED_TOWNS", "comboServiceBySlug", "townBySlug"]
  key_links:
    - from: "web/content/towns.js"
      to: "web/content/services.js"
      via: "a module-load guard comparing COMBO_SERVICES display nouns to the service records' crumbs"
      pattern: "crumb"
    - from: "web/content/towns.js"
      to: "@bhc/design-system/geo"
      via: "distanceMiles from the leamington-spa centroid, for the batch and county ordering"
      pattern: "distanceMiles"
---

<objective>
`web/content/towns.js` — the phase's structural core, and Success Criterion 1: a single towns-data
file that drives generation of every combo page and every town hub.

Purpose: this module is where batching stops being a process and becomes a property. `PUBLISHED_TOWNS`
is the single filtered array and **nothing reads the raw list**, which converts "a hub links to a
page that is not built yet" from a class of bug delta 6 catches *after* the fact into something
structurally impossible. Its module-load assertions are the phase's cheapest gate: they run before a
single page is built and they fail in `next build` as well as in CI.

Output: 58 town rows, seven combo-service records, one batch lever, and eleven guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Binding sections:
- `03-UI-SPEC.md` §8.2 (the field contract and the two-region rule), §5 (the concept table, the two
  six-service sets, the seven combo eyebrows), §9.2 (the URL arithmetic and the seven county groups),
  §10.2 (the Latin square), §11 (batching and partial states), §12 delta 29, §13-B/C/D/E/O/W/Y/Z
- `03-RESEARCH.md` — *Pattern 2*, *Pitfall 5*, *Anti-Patterns to Avoid*
- `03-PATTERNS.md` — `web/content/towns.js (data module + module-load guards)`

**The registered-office coordinates never enter this repo.** Every pin-relative ordering in this
phase uses the `leamington-spa` **town centroid**, a public town-centre coordinate that sits under a
mile from the pin and reproduces the audited distances. State that rule in the header without
instancing the coordinate pair it excludes — the acceptance criteria grep for exactly that pair, and
a comment naming it is an occurrence of it.

**Scale, stated once so nothing downstream re-derives it wrongly.** 58 towns · 348 combos at full
rollout · 258 new combos · 57 new hubs · 317 new pages · 426 built routes. **Phase 3 completes with
batch 1 published**, which is 21 towns → 126 combos + 21 hubs + `/locations` + the seven service
pages + the ten utility routes + `/` + `/_not-found` = **167 app routes**. The roadmap's
"~56 / ~336 / ~241" predates the cross with the frozen slug set and is stale.

**Why the two content greps in this plan's acceptance criteria are scoped, and not widened.**
Measured on the four shipped files this module copies its house style from, an unscoped
`[A-Z]{1,2}[0-9]{1,2}` matcher returns 2 on `services.js` (`D14`, `A1`), 1 on `nav.js` (`D14`), 1 on
`faqs.js` (`D13`) and 1 on `site.js` (`H1`) — every match is required header vocabulary, so the
unscoped form fails correct work. The postcode-area-scoped form returns 0 on all four. A bare `@`
fails the same way: the standard module header names the `@/*` alias. This is the argument plan
03-22 makes for delta 25, applied to the source side.
</context>

<interfaces>
```js
// web/content/towns.js — the field contract, §8.2
{
  slug: 'stourport-on-severn',   // GLOBALLY unique: /locations/<town> has no region segment.
                                 // Frozen for the 19 legacy slugs.
  name: 'Stourport-on-Severn',   // display name. A post town or a named suburb. NEVER a district.
  urlRegion: 'warwickshire',     // the FROZEN first URL segment. A legacy string, not a truth claim.
                                 // NEVER rendered.
  county: 'Worcestershire',      // the DISPLAY county. Everything visible reads this.
  lat: 52.3396, lon: -2.2779,    // town centroid. Public data. Feeds geo.js. Never rendered.
  batch: 1,                      // 1..5, ascending distance from the leamington-spa centroid
}

export const COMBO_SERVICES = [/* 7 records */];
// { slug, noun, titleNoun, eyebrow, descriptionSubject, servicePage? }
// `noun` is the display noun (h1/crumb/card/prose), `titleNoun` the <title> form, and
// `servicePage` the parent href where one exists — absent for `move-out-cleaning`.

export const PUBLISHED_BATCHES = 1;                       // the one-line batch lever
export const PUBLISHED_TOWNS = TOWNS.filter((t) => t.batch <= PUBLISHED_BATCHES);
```

The seven combo services, from §5 — `noun` / `titleNoun` / `eyebrow`:

| slug | noun | titleNoun | eyebrow | servicePage |
|---|---|---|---|---|
| `domestic-cleaning` | Domestic Cleaning | Domestic Cleaning | Weekly or fortnightly | yes |
| `deep-cleaning` | Deep Cleaning | Deep Cleaning | Top to bottom, once | yes |
| `end-of-tenancy-cleaning` | End of Tenancy Cleaning | End of Tenancy | Get the deposit back | yes |
| `apartment-cleaning` | Flat Cleaning | Flat Cleaning | Flats and maisonettes | yes |
| `post-construction-cleaning` | Builders Clean | Builders Clean | After the builders leave | yes |
| `short-term-rental-cleaning` | Holiday Let Cleaning | Holiday Lets | Guest-ready between stays | yes |
| `move-out-cleaning` | Move-Out Cleaning | Move-Out Clean | Before you hand over the keys | **none** |

The Latin square, over the distance-ordered index `i` (§10.2):

```js
a = i % 8
b = (Math.floor(i / 8) + a) % 8
```

The `geo.js` boundary — copy this mapping at every call site, in this and every later plan:

```js
// geo.js builds `/location/${town.region}/…`; this module calls that field `urlRegion`, and
// TownCard.region is the DISPLAY county. Opposite meanings, same name.
const forGeo = (t) => ({ ...t, region: t.urlRegion });
```

Import geography from the package's `./geo` subpath (added by plan 03-02), never from the barrel —
the barrel re-exports through JSX and a plain `node` import of it fails.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The 58 town rows, and the guards that prove the coordinates are real</name>
  <files>web/content/towns.js</files>
  <read_first>
    - web/content/services.js lines 1-90 (the module-header house style this file inherits and must
      exceed: 58 towns, seven services, a Latin square and eleven guards make the header load-bearing)
    - docs/research/service-area-coverage.md — the *Post towns* table (39 rows) and the county
      groupings implied by the district lists
    - `03-UI-SPEC.md` §9.2 (the 19 frozen town slugs, and the seven county groups with their full
      town lists) and §11.2 (the batch table)
    - design-system/src/components/InterlinkBlock/geo.js (`distanceMiles`, `nearestTowns`)
  </read_first>
  <action>
Create `web/content/towns.js` with a long header in the `services.js` house style: what the file is
for, what reads it, what breaks if it drifts, which gate catches each drift, and the two-region rule
in full.

Author `TOWNS`, 58 rows against the field contract above. The set is: the **18** legacy town slugs
that survive the Coventry merge, plus `coventry`, plus the **39** post towns from
`docs/research/service-area-coverage.md`. §9.2's seven county-group tables list every one by display
name.

Four things this task has to get right, each with a guard behind it:

  1. **`urlRegion` is `warwickshire` for all 19 legacy slugs**, whether or not that is true of the
     place. Those URLs are frozen by D2. `county` is what is actually true — Banbury is
     Oxfordshire, Daventry is Northamptonshire, Evesham is Worcestershire, Solihull South and South
     Coventry are West Midlands. The 39 new towns take a `urlRegion` derived from their real county
     slug, since their URLs are not frozen.
  2. **Coordinates are real town centroids.** Public data, never rendered. The pin's own coordinates
     do not enter this file.
  3. **`batch` is authored, not derived**, and follows §11.2: 21 / 10 / 10 / 9 / 8, ordered by
     ascending distance from the `leamington-spa` centroid. Batch 1 is the 18 legacy towns plus
     `coventry`, `henley-in-arden` and `solihull` — 21 rather than 19 deliberately, because 19
     produces 44 new pages, below SC-5's 50-100 floor.
  4. **`LEGACY_TOWN_SLUGS`** — a frozen list of the 18, declared once. The sixth service and the
     preserved-URL set both derive from it, and a hard-coded 18-entry list is the only honest source
     for "which URLs existed before this rebuild".

Then add the first four module-load guards, each throwing with the repo's
`<file>: <what was found> — <what it means>` message shape:

  - `TOWNS.length === 58`, and `LEGACY_TOWN_SLUGS.length === 18` with every entry present in `TOWNS`.
  - Every `slug` is globally unique (the hub URL has no region segment, so a collision is two towns
    fighting over one page).
  - Every row has a finite `lat` and `lon`, no two rows share a coordinate pair, and every pair sits
    inside a bounding box of lat 51.9-53.0 and lon -2.9 to -1.0. A typo that swaps a sign or drops a
    decimal lands outside it.
  - **The frame reproduces the audited distances**: from the `leamington-spa` row, `distanceMiles`
    to the `birmingham` row is 18.8 ± 1.5 and to the `telford` row is 46.2 ± 1.5. Those are the two
    figures the SEO audit publishes and the two `design-system/test/locks.test.js` already locks; if
    they reproduce, the whole coordinate set is in the right frame.
  - Batch sizes are exactly 21 / 10 / 10 / 9 / 8, and batch 1 is exactly the 21 named slugs.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/towns.js')).href);const g=await import('@bhc/design-system/geo');const T=m.TOWNS;console.log('towns',T.length);if(T.length!==58)throw new Error('TOWNS is '+T.length);const s=new Set(T.map(t=>t.slug));if(s.size!==58)throw new Error('duplicate slug');for(const t of T){if(!(t.lat>51.9&&t.lat<53.0&&t.lon>-2.9&&t.lon<-1.0))throw new Error(t.slug+' is outside the bounding box: '+t.lat+','+t.lon);if(!t.county||!t.urlRegion||!t.name)throw new Error(t.slug+' is missing a field');}const by=k=>T.find(t=>t.slug===k);const L=by('leamington-spa'),B=by('birmingham'),W=by('telford');const d1=g.distanceMiles(L,B),d2=g.distanceMiles(L,W);console.log('leam->bham',d1.toFixed(1),'leam->telford',d2.toFixed(1));if(Math.abs(d1-18.8)>1.5||Math.abs(d2-46.2)>1.5)throw new Error('coordinate frame is wrong');const sizes=[1,2,3,4,5].map(b=>T.filter(t=>t.batch===b).length);console.log('batch sizes',sizes);if(sizes.join(',')!=='21,10,10,9,8')throw new Error('batch sizes '+sizes.join(','));for(const t of T)if(['banbury','daventry','evesham','solihull-south','south-coventry'].includes(t.slug)&&t.urlRegion!=='warwickshire')throw new Error(t.slug+' lost its frozen urlRegion');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: 58 unique slugs, every coordinate inside the box, the two audited
      distances reproduced, batch sizes 21/10/10/9/8, and the five off-county legacy towns still
      under the frozen `warwickshire` URL segment.
    - `grep -c '52\.29358' web/content/towns.js` returns 0 and `grep -c '1\.55378' web/content/towns.js`
      returns 0 — the pin's coordinates are not in the file, and the header states the rule without
      instancing the pair.
    - `grep -Ec '\b(B|CV|DY|TF|WS|WV|WR|ST|SY|OX|NN)[0-9]{1,2}[A-Z]?\b' web/content/towns.js` returns 0 — no postcode
      district in a name, a comment or a data field.
    - Banbury's `county` is `Oxfordshire`, Daventry's is `Northamptonshire`, Evesham's is
      `Worcestershire`, and all three still have `urlRegion: 'warwickshire'`. Print all three rows
      into the SUMMARY.
    - `grep -c "from '@bhc/design-system'" web/content/towns.js` returns 0 — geography comes from the
      `./geo` subpath, so this module stays importable by a plain node process.
  </acceptance_criteria>
  <done>58 real towns with real coordinates, frozen URL segments, honest counties and authored batches.</done>
</task>

<task type="auto">
  <name>Task 2: COMBO_SERVICES, the two six-service sets, the Latin square and the batch lever</name>
  <files>web/content/towns.js</files>
  <read_first>
    - web/content/towns.js (task 1's rows and guards)
    - `03-UI-SPEC.md` §5 (the concept table, the two six-service sets, the seven eyebrows) and
      §10.2 (the Latin square, and why an injective-but-clustered assignment measures E[J] = 0.078)
    - web/content/services.js — the seven records' `slug` and `crumb` fields only
  </read_first>
  <action>
Add `COMBO_SERVICES`, the seven records in the interface table above. This is its **own** table and
not a read of `services.js` (§13-Z): `move-out` has 18 combo pages and no service record, and
`move-in` has a record and no combos, so one table cannot serve both without a hole and a dead entry.

Derive each town's `services` — its own six, in a stable order:
  - all towns: `apartment-cleaning`, `deep-cleaning`, `domestic-cleaning`, `end-of-tenancy-cleaning`,
    `post-construction-cleaning`;
  - plus `move-out-cleaning` if the slug is in `LEGACY_TOWN_SLUGS`, else `short-term-rental-cleaning`.

Derive the composition index. Order all 58 towns by ascending `distanceMiles` from the
`leamington-spa` row — deterministically, with a tie-break on slug so the ordering cannot depend on
sort stability — then assign `a = i % 8` and `b = (⌊i/8⌋ + a) % 8`.

Do not "simplify" that to `(i % 8, ⌊i/8⌋)`. Both are injective, but the clustered form puts the
18-town `move-out` cohort across only three `b` values and measures **E[J] = 0.078** against delta
23's 0.08 mean ceiling — injective, provable, and one rounding error from red. The Latin square
measures 0.048 on the same cohort.

Then `PUBLISHED_BATCHES = 1` and `PUBLISHED_TOWNS = TOWNS.filter((t) => t.batch <= PUBLISHED_BATCHES)`,
with `PUBLISHED_TOWNS.length === 0` a build failure — there is no rendered empty state for
`/locations` and no "coming soon" copy anywhere in this site.

Add `townBySlug` and `comboServiceBySlug`, both throwing on a miss (`reviews.js:346-356`'s idiom),
and export `forGeo` — the one-line `{ ...t, region: t.urlRegion }` mapping — so every later call site
uses the same one rather than re-deriving it. Pitfall 5: a raw row passed into `buildInterlinks`
produces `/location/undefined/...` on every link of every generated page, and delta 6 only catches it
after 406 pages have been generated wrong.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/towns.js')).href);const T=m.TOWNS;if(m.COMBO_SERVICES.length!==7)throw new Error('COMBO_SERVICES '+m.COMBO_SERVICES.length);for(const t of T){if(t.services.length!==6)throw new Error(t.slug+' has '+t.services.length+' services');const hasMO=t.services.includes('move-out-cleaning'),hasST=t.services.includes('short-term-rental-cleaning');if(hasMO===hasST)throw new Error(t.slug+' sixth slot is wrong');}const legacy=T.filter(t=>t.services.includes('move-out-cleaning'));console.log('legacy towns',legacy.length,'new towns',58-legacy.length);if(legacy.length!==18)throw new Error('legacy cohort is '+legacy.length);const pairs=new Set(T.map(t=>t.a+','+t.b));if(pairs.size!==58)throw new Error('(a,b) is not injective: '+pairs.size+' distinct pairs');const cohorts={all:T};for(const s of m.COMBO_SERVICES)cohorts[s.slug]=T.filter(t=>t.services.includes(s.slug));for(const [name,set] of Object.entries(cohorts)){const cap=Math.ceil(set.length/8)+1;for(const k of ['a','b']){const c={};for(const t of set)c[t[k]]=(c[t[k]]||0)+1;const max=Math.max(...Object.values(c));console.log(name,k,'n='+set.length,'max='+max,'cap='+cap);if(max>cap)throw new Error(name+' '+k+' clusters: '+max+' > '+cap);}}console.log('published',m.PUBLISHED_TOWNS.length,'batches',m.PUBLISHED_BATCHES);if(m.PUBLISHED_TOWNS.length!==21)throw new Error('published is '+m.PUBLISHED_TOWNS.length);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: seven combo services; every town carries exactly six with exactly
      one of the two scoped services; the legacy cohort is exactly 18; `(a,b)` is injective across
      all 58; and no `a` or `b` value exceeds `⌈n/8⌉ + 1` in **any** cohort — the full set and each
      of the seven service cohorts. Record the per-cohort max/cap table in the SUMMARY.
    - The same balance check passes over the **published** subsets: rerun the cohort loop against
      `PUBLISHED_TOWNS` and record the figures. If a published cohort clusters, permute the ordering
      — never widen the cap and never change the formula's shape.
    - `PUBLISHED_TOWNS.length === 21` and the 21 slugs are exactly the batch-1 set.
    - Setting `PUBLISHED_BATCHES = 0` makes the module throw: prove it, restore, prove it imports.
      Record both outcomes.
    - `grep -c 'forGeo' web/content/towns.js` returns 2 or more (definition and export), and the
      mapping is `region: t.urlRegion` — not the other way round.
  </acceptance_criteria>
  <done>Seven combo services, two six-service sets, a balanced injective composition index and a one-constant batch lever.</done>
</task>

<task type="auto">
  <name>Task 3: The coverage and drift guards — the phase's cheapest gate</name>
  <files>web/content/towns.js</files>
  <read_first>
    - web/content/towns.js (tasks 1 and 2)
    - web/content/reviews.js lines 330-356 (the key-set guard idiom and the message shape)
    - web/content/town-prose.js, web/content/hub-town-prose.js, web/content/hub-choose-prose.js,
      web/content/county-notes.js — their exported slug lists and key lists
    - web/content/services.js — the seven records' `slug` and `crumb` fields
  </read_first>
  <action>
Add the remaining module-load guards. All of them run before a single page is built and fail in
`next build` as well as in CI, which is what makes them cheaper than any lock.

  1. **Noun agreement with `services.js`.** For every `COMBO_SERVICES` entry that declares a
     `servicePage`, the service record at that slug must exist and its `crumb` must equal the combo
     record's `noun` exactly. This is what makes "one concept, one noun, every template" an
     assertion rather than an intention. `move-out-cleaning` declares no `servicePage` and is
     exempt by construction, not by exception.
  2. **Prose coverage.** Every published town has an entry in `TOWN_PROSE`, in `HUB_TOWN` and in
     `HUB_CHOOSE`. A batch increment that adds towns without adding their prose is then a red build
     rather than three pages hundreds of words short.
  3. **County-note coverage.** Every `published town × its six services` pair resolves a county note,
     checked against `COUNTY_NOTE_KEYS`. This is the pair most likely to be missed when a later batch
     brings a county into play for the first time.
  4. **Locality coverage.** Every published town's `TOWN_PROSE` entry declares at least three
     localities, since the hub's town-specific FAQ is built from them.

Each guard throws with the repo's `<file>: <what was found> — <what it means>` message shape. Do not
collapse them into one: a single "something drifted" message is what makes a red build take an hour.

Finish the header. It carries the full field contract, the two-region rule, the batch lever's
one-line semantics, the rule that nothing reads the raw `TOWNS` array outside this module, and the
rule about the pin's coordinates — stated without instancing them.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const r=p=>pathToFileURL(resolve(p)).href;const m=await import(r('web/content/towns.js'));const s=await import(r('web/content/services.js'));const tp=await import(r('web/content/town-prose.js'));const ht=await import(r('web/content/hub-town-prose.js'));const hc=await import(r('web/content/hub-choose-prose.js'));const cn=await import(r('web/content/county-notes.js'));for(const c of m.COMBO_SERVICES){if(!c.servicePage)continue;const rec=s.SERVICES.find(x=>'/services/'+x.slug===c.servicePage);if(!rec)throw new Error(c.slug+' points at '+c.servicePage+' which is not a service record');if(rec.crumb!==c.noun)throw new Error(c.slug+': combo noun \''+c.noun+'\' vs service crumb \''+rec.crumb+'\'');}const keys=new Set(cn.COUNTY_NOTE_KEYS);for(const t of m.PUBLISHED_TOWNS){if(!tp.TOWN_PROSE[t.slug])throw new Error('no town prose for '+t.slug);if(!ht.HUB_TOWN[t.slug])throw new Error('no hub prose for '+t.slug);if(!hc.HUB_CHOOSE[t.slug])throw new Error('no hub-choose prose for '+t.slug);if(tp.TOWN_PROSE[t.slug].localities.length<3)throw new Error(t.slug+' has fewer than 3 localities');for(const sv of t.services)if(!keys.has(sv+'|'+t.county))throw new Error('no county note for '+sv+' in '+t.county+' (town '+t.slug+')');}console.log('all guards satisfied for',m.PUBLISHED_TOWNS.length,'published towns');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: every `servicePage`-bearing combo service agrees with its record's
      crumb; every published town has all three prose entries and at least three localities; and
      every published `town × service` pair resolves a county note.
    - Each of the four guards is proved to fire, one at a time: break the input, confirm the module
      throws with a message naming the offending key, restore, confirm it imports. Record all four
      messages verbatim in the SUMMARY.
    - `node --input-type=module -e "await import(<towns.js>)"` completes in under two seconds — the
      guards are O(towns), not O(pages).
    - `npm run verify` still exits 0. Nothing imports `towns.js` yet, so the built site is unchanged
      and the route count is still 19.
    - `grep -c 'PUBLISHED_BATCHES' web/content/towns.js` returns 3 or more and the constant is a
      literal integer on one line — plan 03-18's route floor and every later batch depend on it
      being a one-line change.
  </acceptance_criteria>
  <done>Eleven module-load guards stand between a data mistake and 167 built pages, and every one of them has been shown to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data module → 147 generated pages | every row here is multiplied by seven; a wrong field is a systematic error, not a one-page one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information Disclosure | town coordinates | mitigate | public town centroids only; the `leamington-spa` centroid is the origin for all pin-relative ordering; the greps in task 1 prove the pin's own pair is absent |
| T-03-17 | Information Disclosure | postcode districts in town data | mitigate | `name` is a post town or named suburb, never a district; the district grep runs over the whole file, comments included; delta 25 asserts the same over built HTML |
| T-03-18 | Tampering | a batch increment without its content | mitigate | the prose-coverage and county-note-coverage guards fail at module load rather than shipping thin pages |
| T-03-19 | Repudiation | a false county on 29 pages | mitigate | `county` is authored per row and never derived from `urlRegion`; delta 28 asserts it over built HTML in plan 03-22 task 3 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- All three verify commands exit 0.
- `npm run verify` exits 0 and the built route count is unchanged at 19.
- `git diff --stat` touches exactly one file.
</verification>

<success_criteria>
- One data file describes 58 towns, seven combo services and five batches.
- `PUBLISHED_TOWNS` is the only filtered array and is 21 towns at `PUBLISHED_BATCHES = 1`.
- `(a, b)` is injective across all 58 and balanced in every cohort, published and full.
- Eleven module-load guards exist and every one has been shown to fire.
- The pin's coordinates and every postcode district are absent from the file.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-16-SUMMARY.md` when done.
</output>
