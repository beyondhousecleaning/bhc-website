---
phase: 03-programmatic-location-service-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/content/variants/deep-cleaning.js
autonomous: true
requirements: [REQ-content-depth-bar, REQ-programmatic-page-scale]

must_haves:
  truths:
    - "Eight deep-cleaning service-detail passages exist, each about a different thing"
    - "Eight deep-cleaning how-the-job-runs passages exist, each about a different stage"
    - "No passage names a town or a county"
  artifacts:
    - path: "web/content/variants/deep-cleaning.js"
      provides: "A_VARIANTS[8] and B_VARIANTS[8] for the deep-cleaning combo pages"
      min_lines: 200
      exports: ["A_VARIANTS", "B_VARIANTS"]
  key_links:
    - from: "web/content/variants/deep-cleaning.js"
      to: "web/content/blocks.jsx"
      via: "the authored block shape"
      pattern: "type: 'p'"
---

<objective>
The eight-by-eight service-variant grid for **deep cleaning** — the copy that makes 58 towns'
deep-cleaning pages read as 58 pages rather than 58 copies.

Purpose: town-independent, and on batch 1's critical path — a 21-town batch under the Latin-square
composition index exercises all eight `a` values and all eight `b` values, so all sixteen passages
are needed before a single combo page can be built.

Output: `web/content/variants/deep-cleaning.js` — roughly 4,500 authored words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Binding sections:
- `03-UI-SPEC.md` §10.2 (the block table and the three prohibitions), §5 *Voice*, §13-W
- `03-RESEARCH.md` — *Pitfall 9* (eight paraphrases that pass delta 23)
- `03-PATTERNS.md` — the copy-deck section

**`services.js` already ships a ~1,000-word `deep-cleaning` service-page record.** These sixteen
passages must not restate it. The service page is the region-wide parent; these are the eight
angles a combo page can take that the parent does not.

**Why the two content greps in this plan's acceptance criteria are scoped, and not widened.**
Measured on the four shipped files this module copies its house style from, an unscoped
`[A-Z]{1,2}[0-9]{1,2}` matcher returns 2 on `services.js` (`D14`, `A1`), 1 on `nav.js` (`D14`), 1 on
`faqs.js` (`D13`) and 1 on `site.js` (`H1`) — every match is required header vocabulary, so the
unscoped form fails correct work. The postcode-area-scoped form returns 0 on all four. A bare `@`
fails the same way: the standard module header names the `@/*` alias. This is the argument plan
03-22 makes for delta 25, applied to the source side.
</context>

<interfaces>
```js
// Block shape, fixed by web/content/blocks.jsx:53-64
{ type: 'p',  text: string }
{ type: 'h2', text: string }
{ type: 'h3', text: string }
{ type: 'ul', text: string[] }   // `text` carries the items
// No `h1` block type, ever. renderBlocks drops an unrecognised type SILENTLY.

export const A_VARIANTS = [ /* 8 arrays of blocks */ ];
export const B_VARIANTS = [ /* 8 arrays of blocks */ ];
```
Implicit string concatenation with a trailing space per line — `services.js:166-171`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: A_VARIANTS — eight deep-cleaning service-detail passages</name>
  <files>web/content/variants/deep-cleaning.js</files>
  <read_first>
    - web/content/services.js lines 1-90 (module header — the house style, the no-address rule, and
      the rule that a comment naming a banned shape is itself an occurrence of it)
    - web/content/services.js lines 151-300 (the `deep-cleaning` record — the parent page these
      passages must NOT restate, and the density they must match)
    - web/content/blocks.jsx lines 40-95
  </read_first>
  <action>
Create `web/content/variants/deep-cleaning.js` with a header in the `services.js` house style, then
`export const A_VARIANTS`, exactly eight block arrays. Each: one `h2`, one or two `p`, one `ul` of
five to seven items, **≥ 280 counted words** (target ~300).

Eight different SUBJECTS, not eight rewordings. Index 0-7 is the `a` value a town carries:

  0. The oven, hob and extractor — the single job most people book a deep clean for.
  1. Limescale — shower screens, taps, tiles, the toilet, and what actually shifts it.
  2. Kitchen cupboards and the tops of things — inside, on top of, and behind the units.
  3. Skirting boards, door frames and the paintwork nobody washes.
  4. Windows inside, sills, tracks, and the black in the corners of the seals.
  5. Bathrooms beyond the limescale — grout, sealant, extractor fans, and telling stained from dirty.
  6. Floors a weekly clean maintains but never resets — grout lines, edges, under the units.
  7. What a deep clean cannot fix — dirt versus wear, and when the honest answer is a decorator.

Hard rules, each with a gate behind it:
  - **No town name and no county name anywhere in the file, including comments** (§10.2 prohibition 1).
  - **No postcode, postcode district, street line, phone number, email or price** — D-04 / Lock 5,
    and the site quotes rather than price-lists. Do not name a banned shape in a comment either.
  - No `h2` may begin `Deep Cleaning in ` — delta 20 asserts that phrase appears in exactly one
    heading per page, and that heading is the `<h1>`.
  - Plain UK English, contractions, concrete nouns. A literal `&`, never the entity.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/deep-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const V=m.A_VARIANTS;if(V.length!==8)throw new Error('A_VARIANTS has '+V.length);const w=V.map(words);console.log('A words',w);if(Math.min(...w)<280)throw new Error('short A variant: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error('A '+i+','+j+' J='+d.toFixed(3));}console.log('A_VARIANTS OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: exactly 8 entries, each ≥ 280 counted words, all 28 pairwise
      5-gram Jaccard scores ≤ 0.10.
    - `grep -Ec 'Warwick|Leamington|Coventry|Birmingham|Telford|Solihull|Rugby|Stratford|Dudley|Walsall|Wolverhampton|Midlands|Shropshire|Staffordshire|Worcestershire|Oxfordshire|Northamptonshire' web/content/variants/deep-cleaning.js` returns 0.
    - `grep -Ec '\b(B|CV|DY|TF|WS|WV|WR|ST|SY|OX|NN)[0-9]{1,2}[A-Z]?\b' web/content/variants/deep-cleaning.js` returns 0.
    - `grep -Ec '£|[0-9]{5}|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' web/content/variants/deep-cleaning.js` returns 0.
    - `grep -c "type: 'h1'" web/content/variants/deep-cleaning.js` returns 0 and
      `grep -c '&amp;' …` returns 0.
    - Cross-check against the parent page: the worst 5-gram Jaccard between any `A_VARIANT` and the
      `deep-cleaning` record's `prose` in `services.js` is ≤ 0.10. Report the figure in the SUMMARY.
  </acceptance_criteria>
  <done>Eight distinct ~300-word passages exist, none restating the parent service page.</done>
</task>

<task type="auto">
  <name>Task 2: B_VARIANTS — eight how-the-job-runs passages</name>
  <files>web/content/variants/deep-cleaning.js</files>
  <read_first>
    - web/content/variants/deep-cleaning.js (the `A_VARIANTS` this task must not overlap with)
    - web/content/process.js (`PROCESS_STEPS` — stripped from the counted corpus, so do not restate it)
    - `03-UI-SPEC.md` §10.2 block 4
  </read_first>
  <action>
Append `export const B_VARIANTS`, exactly eight block arrays. Each: one `h3` then two or three `p`,
**≥ 240 counted words** (target ~260).

An `h3`, not an `h2` — this block renders inside `Prose` beneath the `A_VARIANT`'s `h2`, and §3's
outline discipline is one `<h1>`, a flat run of `<h2>`s, `<h3>` only inside `Prose`.

Eight stages of the job, orthogonal to task 1's eight subjects. Index 0-7 is the `b` value:

  0. The look round before we quote — what we need to see, and why a photo is not enough.
  1. Who comes, how many, and why a deep clean is not a one-person job.
  2. The order we work the house in, and why the kitchen is not first.
  3. Products and kit — what we bring, what needs dwell time, what we will not put on a surface.
  4. Access, and what has to be clear before we start.
  5. Time — how long a deep clean honestly takes, and what happens when a house needs more than
     the day allows.
  6. The end-of-job walk round, and what "done" means when there is no checklist on the door.
  7. Afterwards — keeping it, when the next one is due, and what to do if something was missed.

Do not restate the shared `ProcessSteps` copy: that band is stripped from the counted corpus
because it is identical on 400-plus pages, so a paraphrase of it is words that do not count.

The same hard rules as task 1, unchanged.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/deep-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};for(const k of ['A_VARIANTS','B_VARIANTS']){const V=m[k];if(V.length!==8)throw new Error(k+' has '+V.length);const w=V.map(words);console.log(k,w);const min=k==='A_VARIANTS'?280:240;if(Math.min(...w)<min)throw new Error(k+' short: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error(k+' '+i+','+j+' J='+d.toFixed(3));}}const A=m.A_VARIANTS.map(v=>sh(toks(v)));const B=m.B_VARIANTS.map(v=>sh(toks(v)));let worst=0;for(let i=0;i<8;i++)for(let j=0;j<8;j++)worst=Math.max(worst,J(A[i],B[j]));console.log('worst A-vs-B J',worst.toFixed(3));if(worst>0.10)throw new Error('an A and a B passage overlap: '+worst.toFixed(3));console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: both grids exactly 8, `A` ≥ 280 and `B` ≥ 240 counted words each,
      all within-grid pairwise Jaccard ≤ 0.10, worst cross-grid Jaccard ≤ 0.10.
    - `grep -c "type: 'h3'" web/content/variants/deep-cleaning.js` returns 8 and
      `grep -c "type: 'h2'" …` returns 8.
    - The town/county, postcode-district, price, `h1` and `&amp;` greps from task 1 all still return 0.
    - Total authored words 4,200-5,200; record both word arrays in the SUMMARY.
    - **Cross-deck sweep.** Every passage in this deck against every passage in every sibling deck
      already present in `web/content/variants/`, worst 5-gram Jaccard ≤ 0.10. The seven B-grids are
      structurally parallel by instruction — the same eight stages of the job for seven different
      services — so this is the region where two decks are most likely to converge. Record how many
      sibling decks were compared and the worst figure. **`compared against 0 sibling deck(s)` is a
      legitimate pass** — the seven variant plans are wave-mates with no ordering between them, so
      this sweep is an early warning whose coverage depends on what has landed, not a gate. Plan
      03-17 task 1 runs the complete deterministic seven-deck sweep over all 6,216 pairs once they
      all exist, and that is the one that has to hold.
      Command:
      `node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';import {readdirSync} from 'node:fs';const dir='web/content/variants';const mine='deep-cleaning.js';const load=async f=>await import(pathToFileURL(resolve(dir,f)).href);const toks=v=>v.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const m=await load(mine);const A=[...m.A_VARIANTS,...m.B_VARIANTS].map(v=>sh(toks(v)));const others=readdirSync(dir).filter(f=>f.endsWith('.js')&&f!==mine);let worst=0,wp='';for(const f of others){const o=await load(f);const B=[...o.A_VARIANTS,...o.B_VARIANTS].map(v=>sh(toks(v)));for(let i=0;i<A.length;i++)for(let j=0;j<B.length;j++){const d=J(A[i],B[j]);if(d>worst){worst=d;wp=f+' #'+j+' vs mine #'+i;}}}console.log('compared against',others.length,'sibling deck(s); worst cross-deck J',worst.toFixed(3),wp);if(worst>0.10)throw new Error('cross-deck collision: '+wp);"`
  </acceptance_criteria>
  <done>The deep-cleaning 8×8 grid is complete, distinct within and across both axes, and town-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → 58 rendered pages | one careless sentence is published 58 times |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | authored prose | mitigate | no address, postcode, district, phone, email or price in copy or comments; asserted by greps here and by Lock 5 / SC-2d over built HTML |
| T-03-06 | Repudiation | fabricated claims | mitigate | no number in copy that is not computed; no claim the business does not already make in `web/content/` |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- Both verify commands exit 0.
- `git diff --stat` touches exactly one file.
- `npm run verify` still exits 0 — nothing imports this module yet.
</verification>

<success_criteria>
- `A_VARIANTS[8]` and `B_VARIANTS[8]` exported, at or above their word floors.
- No pair of passages exceeds 0.10 5-gram Jaccard, within a grid or across the two.
- No overlap above 0.10 with the parent `deep-cleaning` service-page prose.
- No town, county, postcode district, price, phone or email in the file.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-04-SUMMARY.md` when done.
</output>
