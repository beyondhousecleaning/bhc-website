---
phase: 03-programmatic-location-service-engine
plan: 15
type: execute
wave: 3
depends_on: ["03-14"]
files_modified:
  - web/content/services.js
  - web/content/faqs.js
  - web/content/reviews.js
  - web/content/nav.js
  - web/scripts/check-html-locks.mjs
autonomous: true
requirements: [REQ-canonical-service-taxonomy, REQ-hub-and-spoke-architecture, REQ-content-depth-bar]

must_haves:
  truths:
    - "/services/apartment-cleaning resolves and renders a real page"
    - "Every combo service that ships on 58 towns has a topical parent page"
    - "The new page is reachable from the header and the footer on every route"
  artifacts:
    - path: "web/content/services.js"
      provides: "a seventh record, apartment-cleaning, rendering as Flat Cleaning"
      contains: "apartment-cleaning"
  key_links:
    - from: "web/content/nav.js"
      to: "/services/apartment-cleaning"
      via: "a seventh NAV child and a seventh FOOTER_COLUMNS Services link"
      pattern: "apartment-cleaning"
---

<objective>
Build `/services/apartment-cleaning` — the one static page this phase adds outside the three
generated templates.

Purpose: without it, 58 towns' `apartment-cleaning` combo pages have **no topical parent**. That is
audit §6's *"57 of 95 location pages have no parent service page"* defect, reproduced at a larger
scale by the very phase meant to fix it. One page, one seventh record in `services.js` rather than a
new route file, and `EXPECTED_APP_ROUTES` goes 18 → 19.

The slug is frozen American; the visible noun is **Flat Cleaning**, exactly the shipped
`post-construction-cleaning` → *builders clean* precedent.

Output: a seventh service page at ~1,100 words, linked from every route's header and footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Binding sections:
- `03-UI-SPEC.md` §5 (the service-concept table, the `apartment-cleaning` bullet), §9.1, §13-F,
  §13-U
- `03-PATTERNS.md` — `web/content/services.js · nav.js`

**Home keeps six service cards, not seven.** §9.2's grid is six and §12 delta 19's chrome arithmetic
counts this phase as adding exactly **three** chrome links sitewide — `apartment-cleaning` in `NAV`
and in `FOOTER_COLUMNS`, plus the `Areas We Cover` nav item that plan 03-19 adds. A seventh home
card would be a fourth, and it would change the home page's card grid, which no spec clause asks for.
</context>

<interfaces>
```js
// The seventh record's identity fields. Everything else follows the six shipped records' shape:
// slug, h1, crumb, eyebrow, title, description, lead, prose, faqs.
{
  slug: 'apartment-cleaning',
  h1: `Flat Cleaning in ${AREA_LONG}`,          // AREA_LONG carries a LITERAL &
  crumb: 'Flat Cleaning',                        // must equal the nav label exactly
  eyebrow: 'Flats and maisonettes',
  title: titleFor('Flat Cleaning in Warwickshire'),   // 52 chars, measured
  description: describe('Flat cleaning'),
  // lead: the Hero's supporting line
  // prose: the block array — ~1,100 words at the density of the six shipped records
  faqs: serviceFaqs('apartment-cleaning'),
}
```

Two module-load guards will fail until this plan finishes, and that is the designed behaviour:
`reviews.js`'s `SERVICE_REVIEWS` key set and the `faqs.js` guard plan 03-14 added both compare
against `services.js` slugs exactly.

**All eighteen reviews in `reviews.js` are already allocated** — six services × three. The seventh
trio therefore reuses three of them, and nothing forbids that. Pick the three whose verbatim text is
most about a smaller property or a one-off clean. **Do not invent, embellish or edit a review**: the
text is quoted verbatim including typos, no review carries a town, and fabricating review text is a
banned practice under the DMCC Act 2024 (`02-16-REVIEWS-SUMMARY.md`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The seventh services.js record</name>
  <files>web/content/services.js</files>
  <read_first>
    - web/content/services.js lines 1-160 (the header's contracts and the `SERVICES` preamble)
    - web/content/services.js — the `deep-cleaning` record in full (one complete ~1,000-word record,
      the shape and density to match)
    - web/content/variants/apartment-cleaning.js (plan 03-06's sixteen combo passages — this page
      is the region-wide parent and must not restate any of them)
    - web/content/site.js lines 116-175 (`titleFor`, `describe`, and the measured caps)
  </read_first>
  <action>
Add a seventh record to `SERVICES`, in the position the nav will carry it. Identity fields exactly as
the interface above; then author `lead` and `prose` at **≥ 1,000 counted words**, matching the six
shipped records' density (they measure 865-1,026).

What this page is for, and what keeps it distinct: it is the region-wide parent for flat cleaning.
The combo pages beneath it are about a flat *in a town*; this page is about flats as a property type
across the whole trading area — the mix of purpose-built blocks, conversions and new-builds, what
that means for access and equipment, and what a flat needs that a house does not.

Plan 03-06 authored sixteen passages on this service. This record must not restate any of them; the
similarity gate in plan 03-23 measures the whole corpus, and the parent-versus-child pair is the one
most likely to collide.

Follow every rule in the file's own header: no address, postcode, phone, email or price; a literal
`&`; no `h1` block type in `prose`; plain UK English; and the visible noun is *flat*, never the
American word, in prose, headings and bullets alike.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/services.js')).href);const r=m.bySlug('apartment-cleaning');if(!r)throw new Error('no apartment-cleaning record');const words=b=>b.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ').trim().split(/\s+/).length;const w=words(r.prose)+r.lead.split(/\s+/).length;console.log('slug',r.slug,'| h1',r.h1,'| crumb',r.crumb,'| title',r.title,'('+r.title.length+')','| desc',r.description.length,'| words',w);if(r.title.length>60)throw new Error('title '+r.title.length);if(r.description.length>155)throw new Error('description '+r.description.length);if(w<1000)throw new Error('prose is '+w+' words');if(m.SERVICES.length!==7)throw new Error('SERVICES has '+m.SERVICES.length);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: seven records, the new one at ≥ 1,000 counted words, title ≤ 60,
      description ≤ 155.
    - `h1` is exactly `Flat Cleaning in Warwickshire & the West Midlands` and `crumb` is exactly
      `Flat Cleaning`.
    - The American noun appears in no string in the record: inspect the record and confirm it occurs
      only in the `slug` and in the `serviceFaqs(...)` key.
    - Parent-versus-child check: the worst 5-gram Jaccard between this record's `prose` and any
      passage in `web/content/variants/apartment-cleaning.js` is ≤ 0.10. Report the figure.
    - `grep -c '&amp;' web/content/services.js` still returns 0.
  </acceptance_criteria>
  <done>A seventh, ~1,100-word service record exists, in British vocabulary, distinct from its own combo decks.</done>
</task>

<task type="auto">
  <name>Task 2: The FAQ set, the review trio, and the two chrome links</name>
  <files>web/content/faqs.js, web/content/reviews.js, web/content/nav.js</files>
  <read_first>
    - web/content/faqs.js lines 85-100 (the `SERVICE_FAQS` literal), one complete service's FAQ array,
      and the key-set guard plan 03-14 added
    - web/content/reviews.js lines 140-360 (`REVIEWS` — the eighteen verbatim reviews with their ids —
      `pick`, `SERVICE_REVIEWS`, the guard, and the two hard constraints in the header comments)
    - web/content/nav.js (`NAV` Services children and `FOOTER_COLUMNS` Services links)
  </read_first>
  <action>
**`faqs.js`** — add an `apartment-cleaning` key to `SERVICE_FAQS` with four or five question/answer
pairs in the register the shipped sets use: real questions, plain answers, no schema. Subjects worth
covering: communal areas and what is not ours to clean, access to a block, whether a small flat
needs a shorter visit, and what happens with balconies or drying spaces. The guard plan 03-14 added
will already be throwing until this key exists — that is what it is for.

**`reviews.js`** — add an `apartment-cleaning` trio to `SERVICE_REVIEWS` using `pick(...)` with three
existing review ids. All eighteen are already allocated, so this trio reuses three; that is
permitted and deliberate. Record in a comment beside it *why* these three, in the style the existing
trios' comment uses. Do not invent a review, do not edit one, do not attach a town to one.

**`nav.js`** — add `Flat Cleaning` → `/services/apartment-cleaning` to both `NAV`'s Services children
and `FOOTER_COLUMNS`'s Services links, in the same position in each. The label must equal the record's
`crumb` character for character; `home.js`'s `hrefFor` and the file's own header both require it.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const r=p=>pathToFileURL(resolve(p)).href;const s=await import(r('web/content/services.js'));const n=await import(r('web/content/nav.js'));const f=await import(r('web/content/faqs.js'));const v=await import(r('web/content/reviews.js'));const fq=f.serviceFaqs('apartment-cleaning');if(fq.length<4)throw new Error('apartment faqs: '+fq.length);const rv=v.reviewsForService('apartment-cleaning');if(rv.length!==3)throw new Error('apartment reviews: '+rv.length);const crumbs=s.SERVICES.map(x=>x.crumb).sort();const kids=n.NAV.find(i=>i.label==='Services').children;const foot=n.FOOTER_COLUMNS.find(c=>c.heading==='Services').links;if(kids.length!==7||foot.length!==7)throw new Error('nav '+kids.length+' footer '+foot.length);if(kids.map(k=>k.label).sort().join('|')!==crumbs.join('|'))throw new Error('nav labels drifted');if(foot.map(k=>k.label).sort().join('|')!==crumbs.join('|'))throw new Error('footer labels drifted');console.log('faqs',fq.length,'reviews',rv.map(x=>x.id),'nav/footer 7 each');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: four or more FAQs, exactly three reviews, seven nav children and
      seven footer Services links whose labels equal the seven crumbs.
    - The three chosen review ids exist in `REVIEWS` and their `quote` text is byte-identical to what
      it was before this plan: `git diff web/content/reviews.js` shows no change inside the `REVIEWS`
      array itself, only in `SERVICE_REVIEWS`.
    - `grep -c 'FAQPage' web/content/faqs.js` returns 0 — the accordion is markup, never schema (D13).
    - Every new FAQ answer is free of address, postcode, district, phone, email and price.
  </acceptance_criteria>
  <done>The new page has its FAQs, its rail and its two chrome links, and both module-load guards are satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Raise the route floor and the expectation table, then verify the whole site</name>
  <files>web/scripts/check-html-locks.mjs</files>
  <read_first>
    - web/scripts/check-html-locks.mjs — `EXPECTED_APP_ROUTES`, `MIN_APP_ROUTES` and
      `INTERLINK_PENDING` (all as plan 03-01 and plan 03-14 left them), and `PAGE_EXPECTATIONS`
    - .planning/phases/03-programmatic-location-service-engine/03-01-SUMMARY.md (what the route
      floor is for and the rule that it is never derived)
  </read_first>
  <action>
Four literal updates, all in one file:

  - `EXPECTED_APP_ROUTES` gains `/services/apartment-cleaning`.
  - `MIN_APP_ROUTES` goes from `18` to `19`. It stays a typed integer — never derived.
  - `PAGE_EXPECTATIONS` gains an entry for the new route: its exact `<h1>`, `hasBreadcrumbs: true`,
    `ldJsonBlocks: 2`, `reviewCards: 3`. Match the six shipped service entries' shape.
  - `INTERLINK_PENDING` gains the new route. It renders no InterlinkBlock until plan 03-19, and the
    set is asserted in the inverse, so omitting it would make the positive half of delta 16 fail on
    a page nobody has got to yet.

Then run the full suite. This is the point at which the taxonomy half of D14 is closed: every combo
service that ships on all 58 towns now has a topical parent page.
  </action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` exits 0 with zero failures.
    - `node -e "const m=require('./web/.next/prerender-manifest.json');const r=Object.keys(m.routes).filter(x=>x.startsWith('/services/'));console.log(r.length, r.sort());"` prints 7 and includes `/services/apartment-cleaning`.
    - `grep -n 'MIN_APP_ROUTES' web/scripts/check-html-locks.mjs` shows the integer `19` on the
      assignment line, with no `.length` and no content-module import anywhere in the file.
    - The built page proves one concept one noun: on `/services/apartment-cleaning` the `<h1>`, the
      last breadcrumb and the header nav label all read *Flat Cleaning*. Quote all three in the
      SUMMARY.
    - `node web/scripts/check-budget.mjs` prints the worst-page figure; record it. It should be
      within a kilobyte of the pre-plan baseline, since one more static page does not move the worst
      page.
  </acceptance_criteria>
  <done>Nineteen app routes, all asserted, and the orphan cohort has a parent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review data → rendered page | published review text is a legal artefact, not copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Repudiation | the review trio | mitigate | three existing ids selected by `pick(...)`, which throws on an unknown id; the `REVIEWS` array itself is not edited, proved by `git diff`; fabricated review text is banned under the DMCC Act 2024 |
| T-03-05 | Information Disclosure | authored prose and FAQ answers | mitigate | no address, postcode, district, phone, email or price; Lock 5 / SC-2d assert it over built HTML |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- `npm run verify` exits 0.
- The prerender manifest lists seven `/services/*` routes.
- `git diff --stat` touches only the five declared files.
</verification>

<success_criteria>
- `/services/apartment-cleaning` prerenders with ≥ 1,000 counted words, one `<h1>`, breadcrumbs, a
  three-card rail and its FAQ band.
- It is linked from the header and the footer on every route.
- Its noun is *Flat Cleaning* on every surface.
- `MIN_APP_ROUTES` is the typed integer 19.
- `npm run verify` is green.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-15-SUMMARY.md` when done.
</output>
