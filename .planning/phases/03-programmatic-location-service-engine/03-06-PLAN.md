---
phase: 03-programmatic-location-service-engine
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - web/content/variants/apartment-cleaning.js
autonomous: true
requirements: [REQ-content-depth-bar, REQ-canonical-service-taxonomy, REQ-programmatic-page-scale]

must_haves:
  truths:
    - "Eight flat-cleaning service-detail passages exist, each about a different thing"
    - "Eight flat-cleaning how-the-job-runs passages exist, each about a different stage"
    - "The rendered noun is Flat Cleaning throughout — the American slug never becomes the visible word"
  artifacts:
    - path: "web/content/variants/apartment-cleaning.js"
      provides: "A_VARIANTS[8] and B_VARIANTS[8] for the apartment-cleaning combo pages"
      min_lines: 200
      exports: ["A_VARIANTS", "B_VARIANTS"]
  key_links:
    - from: "web/content/variants/apartment-cleaning.js"
      to: "web/content/services.js"
      via: "one concept, one noun — the display noun is Flat Cleaning on every template"
      pattern: "flat"
---

<objective>
The eight-by-eight service-variant grid for **flat cleaning**, on all 58 towns.

Purpose: `apartment-cleaning` is the orphan cohort. 58 towns carry a combo page for it and, until
plan 03-15 builds one, it has no parent service page at all — audit §6's *"57 of 95 location pages
have no parent service page"* defect, reproduced at larger scale by the phase meant to fix it. The
slug is frozen by D2; the visible noun is **Flat Cleaning**, exactly the shipped
`post-construction-cleaning` → *builders clean* precedent (§13-F).

Output: `web/content/variants/apartment-cleaning.js` — roughly 4,500 authored words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Binding sections:
- `03-UI-SPEC.md` §10.2, §5 (the service-concept table and *the slug is not the noun*), §13-F
- `03-RESEARCH.md` — *Pitfall 9*
- `03-PATTERNS.md` — the copy-deck section

**Why the two content greps in this plan's acceptance criteria are scoped, and not widened.**
Measured on the four shipped files this module copies its house style from, an unscoped
`[A-Z]{1,2}[0-9]{1,2}` matcher returns 2 on `services.js` (`D14`, `A1`), 1 on `nav.js` (`D14`), 1 on
`faqs.js` (`D13`) and 1 on `site.js` (`H1`) — every match is required header vocabulary, so the
unscoped form fails correct work. The postcode-area-scoped form returns 0 on all four. A bare `@`
fails the same way: the standard module header names the `@/*` alias. This is the argument plan
03-22 makes for delta 25, applied to the source side.
</context>

<interfaces>
```js
{ type: 'p', text: string } | { type: 'h2', text: string } | { type: 'h3', text: string }
| { type: 'ul', text: string[] }
// No `h1` block type, ever.

export const A_VARIANTS = [ /* 8 arrays of blocks */ ];
export const B_VARIANTS = [ /* 8 arrays of blocks */ ];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: A_VARIANTS — eight flat-cleaning service-detail passages</name>
  <files>web/content/variants/apartment-cleaning.js</files>
  <read_first>
    - web/content/services.js lines 1-90 (module header — house style, no-address rule, and the rule
      that a comment naming a banned shape is itself an occurrence of it)
    - web/content/services.js — the `post-construction-cleaning` record (the shipped precedent for a
      frozen American slug whose visible noun is British throughout)
    - web/content/blocks.jsx lines 40-95
  </read_first>
  <action>
Create `web/content/variants/apartment-cleaning.js` with a header in the `services.js` house style —
including the note that the slug is frozen and the rendered noun is *flat cleaning* — then
`export const A_VARIANTS`, exactly eight block arrays. Each: one `h2`, one or two `p`, one `ul` of
five to seven items, **≥ 280 counted words**.

Eight different SUBJECTS. Index 0-7 is the `a` value a town carries:

  0. Communal doors, lifts and stairs — getting kit in, and what is and is not ours to clean.
  1. Small kitchens and galley layouts — cleaning where there is nowhere to stand.
  2. Wet rooms and internal bathrooms with no window — condensation, mould and extraction.
  3. Balconies, drying racks and the damp that comes with drying indoors.
  4. Storage-poor flats — working round things that have nowhere else to go.
  5. New-build flats — the dust that keeps coming back, sealed windows, ventilation filters.
  6. Converted period flats — old floors, sash windows, and what cannot be scrubbed.
  7. Cleaning a flat for a person versus cleaning one for a handover to an agent.

Vocabulary rule with a gate behind it: the visible noun is **flat**, never the American word. Use
*flat*, *maisonette*, *block*, *communal*. The frozen slug appears in the URL and nowhere in the
prose.

Hard rules:
  - **No town name and no county name anywhere in the file, including comments.**
  - **No postcode, postcode district, street line, phone number, email or price.** Do not name a
    banned shape in a comment.
  - No `h2` may begin `Flat Cleaning in ` (delta 20).
  - Plain UK English. A literal `&`, never the entity.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/apartment-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const V=m.A_VARIANTS;if(V.length!==8)throw new Error('A_VARIANTS has '+V.length);const w=V.map(words);console.log('A words',w);if(Math.min(...w)<280)throw new Error('short A variant: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error('A '+i+','+j+' J='+d.toFixed(3));}console.log('A_VARIANTS OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: exactly 8 entries, each ≥ 280 counted words, all 28 pairwise
      Jaccard ≤ 0.10.
    - `grep -Ec 'Warwick|Leamington|Coventry|Birmingham|Telford|Solihull|Rugby|Stratford|Dudley|Walsall|Wolverhampton|Midlands|Shropshire|Staffordshire|Worcestershire|Oxfordshire|Northamptonshire' web/content/variants/apartment-cleaning.js` returns 0.
    - `grep -Ec '\b(B|CV|DY|TF|WS|WV|WR|ST|SY|OX|NN)[0-9]{1,2}[A-Z]?\b' …` returns 0; `grep -Ec '£|[0-9]{5}|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' …` returns 0.
    - `grep -c "type: 'h1'" …` returns 0; `grep -c '&amp;' …` returns 0.
    - One concept, one noun: `grep -oic 'apartment' web/content/variants/apartment-cleaning.js`
      returns 0 outside the filename — inspect the file and confirm the American noun appears in no
      string and no comment.
  </acceptance_criteria>
  <done>Eight distinct passages exist, all in British vocabulary, none naming a town.</done>
</task>

<task type="auto">
  <name>Task 2: B_VARIANTS — eight how-the-job-runs passages</name>
  <files>web/content/variants/apartment-cleaning.js</files>
  <read_first>
    - web/content/variants/apartment-cleaning.js (the `A_VARIANTS` this must not overlap with)
    - web/content/process.js (`PROCESS_STEPS` — stripped from the counted corpus; do not restate it)
  </read_first>
  <action>
Append `export const B_VARIANTS`, exactly eight block arrays. Each: one `h3` then two or three `p`,
**≥ 240 counted words**.

Eight stages, orthogonal to task 1's subjects. Index 0-7 is the `b` value:

  0. Booking a flat — what we ask that we would not ask about a house.
  1. Who comes, and why one cleaner is usually right for a flat when two are right for a house.
  2. The order we work a flat in, and why it runs away from the front door rather than towards it.
  3. Products and kit in a space with no ventilation and no cupboard to keep anything in.
  4. Access — buzzers, fobs, concierges, parking, and the ten minutes those can cost.
  5. Time — what fits into a visit in a one-bed versus a three-bed, and what we drop first.
  6. The check at the end, and leaving a flat that a neighbour on the landing would not notice.
  7. Afterwards — standing arrangements, keys held or not held, and changing a booking.

Do not restate the shared `ProcessSteps` copy. Same hard rules as task 1, unchanged.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/apartment-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};for(const k of ['A_VARIANTS','B_VARIANTS']){const V=m[k];if(V.length!==8)throw new Error(k+' has '+V.length);const w=V.map(words);console.log(k,w);const min=k==='A_VARIANTS'?280:240;if(Math.min(...w)<min)throw new Error(k+' short: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error(k+' '+i+','+j+' J='+d.toFixed(3));}}const A=m.A_VARIANTS.map(v=>sh(toks(v)));const B=m.B_VARIANTS.map(v=>sh(toks(v)));let worst=0;for(let i=0;i<8;i++)for(let j=0;j<8;j++)worst=Math.max(worst,J(A[i],B[j]));console.log('worst A-vs-B J',worst.toFixed(3));if(worst>0.10)throw new Error('an A and a B passage overlap: '+worst.toFixed(3));console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 on every clause.
    - `grep -c "type: 'h3'" …` returns 8; `grep -c "type: 'h2'" …` returns 8.
    - The town/county, district, price, `h1`, `&amp;` and American-noun checks all still hold over
      the whole file.
    - Total authored words 4,200-5,200; record both word arrays in the SUMMARY.
    - **Cross-deck sweep.** Every passage in this deck against every passage in every sibling deck
      already present in `web/content/variants/`, worst 5-gram Jaccard ≤ 0.10. The seven B-grids are
      structurally parallel by instruction — the same eight stages of the job for seven different
      services — so this is the region where two decks are most likely to converge. Record how many
      sibling decks were compared and the worst figure. Plan 03-17 task 1 runs the complete
      deterministic seven-deck sweep once they all exist; this is the early warning.
      Command:
      `node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';import {readdirSync} from 'node:fs';const dir='web/content/variants';const mine='apartment-cleaning.js';const load=async f=>await import(pathToFileURL(resolve(dir,f)).href);const toks=v=>v.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const m=await load(mine);const A=[...m.A_VARIANTS,...m.B_VARIANTS].map(v=>sh(toks(v)));const others=readdirSync(dir).filter(f=>f.endsWith('.js')&&f!==mine);let worst=0,wp='';for(const f of others){const o=await load(f);const B=[...o.A_VARIANTS,...o.B_VARIANTS].map(v=>sh(toks(v)));for(let i=0;i<A.length;i++)for(let j=0;j<B.length;j++){const d=J(A[i],B[j]);if(d>worst){worst=d;wp=f+' #'+j+' vs mine #'+i;}}}console.log('compared against',others.length,'sibling deck(s); worst cross-deck J',worst.toFixed(3),wp);if(worst>0.10)throw new Error('cross-deck collision: '+wp);"`
  </acceptance_criteria>
  <done>The flat-cleaning 8×8 grid is complete, distinct within and across both axes, and town-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → 58 rendered pages | one careless sentence is published 58 times |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | authored prose | mitigate | no address, postcode, district, phone, email or price in copy or comments |
| T-03-06 | Repudiation | claims about communal areas | mitigate | state plainly what is and is not ours to clean; make no claim about a block's management |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- Both verify commands exit 0.
- `git diff --stat` touches exactly one file.
- `npm run verify` still exits 0.
</verification>

<success_criteria>
- `A_VARIANTS[8]` and `B_VARIANTS[8]` exported at or above their word floors.
- No pair of passages exceeds 0.10 Jaccard, within a grid or across the two.
- The visible noun is *flat* everywhere; the frozen slug lives in the URL only.
- No town, county, postcode district, price, phone or email in the file.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-06-SUMMARY.md` when done.
</output>
