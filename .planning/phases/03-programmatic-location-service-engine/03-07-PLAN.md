---
phase: 03-programmatic-location-service-engine
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - web/content/variants/post-construction-cleaning.js
autonomous: true
requirements: [REQ-content-depth-bar, REQ-canonical-service-taxonomy, REQ-programmatic-page-scale]

must_haves:
  truths:
    - "Eight builders-clean service-detail passages exist, each about a different thing"
    - "Eight builders-clean how-the-job-runs passages exist, each about a different stage"
    - "The rendered noun is builders clean throughout — the frozen slug never becomes the visible word"
  artifacts:
    - path: "web/content/variants/post-construction-cleaning.js"
      provides: "A_VARIANTS[8] and B_VARIANTS[8] for the post-construction-cleaning combo pages"
      min_lines: 200
      exports: ["A_VARIANTS", "B_VARIANTS"]
  key_links:
    - from: "web/content/variants/post-construction-cleaning.js"
      to: "web/content/services.js"
      via: "one concept, one noun — the parent record already renders as builders clean"
      pattern: "builders clean"
---

<objective>
The eight-by-eight service-variant grid for the **builders clean**, on all 58 towns.

Purpose: this is the service every batch-1 legacy town gains for the first time — 18 of batch 1's
36 new combo pages are exactly this service on the 18 frozen towns. It is also the service whose
parent page already ships the slug-is-not-the-noun pattern correctly, so this deck inherits a
settled vocabulary rather than establishing one.

Output: `web/content/variants/post-construction-cleaning.js` — roughly 4,500 authored words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Binding sections:
- `03-UI-SPEC.md` §10.2, §5 (the service-concept table), §13-D
- `03-RESEARCH.md` — *Pitfall 9*
- `03-PATTERNS.md` — the copy-deck section

`services.js` ships a ~1,000-word `post-construction-cleaning` record whose prose already says
*builders clean* throughout and whose angle is the two dust passes. This deck must not restate it.

**Why the two content greps in this plan's acceptance criteria are scoped, and not widened.**
Measured on the four shipped files this module copies its house style from, an unscoped
`[A-Z]{1,2}[0-9]{1,2}` matcher returns 2 on `services.js` (`D14`, `A1`), 1 on `nav.js` (`D14`), 1 on
`faqs.js` (`D13`) and 1 on `site.js` (`H1`) — every match is required header vocabulary, so the
unscoped form fails correct work. The postcode-area-scoped form returns 0 on all four. A bare `@`
fails the same way: the standard module header names the `@/*` alias. This is the argument plan
03-22 makes for delta 25, applied to the source side.
</context>

<interfaces>
```js
{ type: 'p', text: string } | { type: 'h2', text: string } | { type: 'h3', text: string }
| { type: 'ul', text: string[] }
// No `h1` block type, ever.

export const A_VARIANTS = [ /* 8 arrays of blocks */ ];
export const B_VARIANTS = [ /* 8 arrays of blocks */ ];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: A_VARIANTS — eight builders-clean service-detail passages</name>
  <files>web/content/variants/post-construction-cleaning.js</files>
  <read_first>
    - web/content/services.js lines 1-90 (module header — house style, no-address rule, and the rule
      that a comment naming a banned shape is itself an occurrence of it)
    - web/content/services.js — the `post-construction-cleaning` record in full (the parent page this
      deck must not restate; its angle is the two dust passes)
    - web/content/blocks.jsx lines 40-95
  </read_first>
  <action>
Create `web/content/variants/post-construction-cleaning.js` with a header in the `services.js` house
style, then `export const A_VARIANTS`, exactly eight block arrays. Each: one `h2`, one or two `p`,
one `ul` of five to seven items, **≥ 280 counted words**.

Eight different SUBJECTS. Index 0-7 is the `a` value a town carries:

  0. Paint, plaster and adhesive on glass, tiles and handles — getting it off without damage.
  1. Stickers, film and protective wrap on units, appliances and glazing.
  2. Kitchens straight off a fit — units, worktops, the appliances that have never been used.
  3. Bathrooms straight off a fit — grout haze, sealant, protective coatings, tile film.
  4. Floors — screed dust, adhesive, and why a new floor needs different treatment from an old one.
  5. Windows, frames, tracks and trickle vents after a glazing or a rendering job.
  6. Sequencing — coming in after the last trade, and what happens when you do not.
  7. Where a clean stops and snagging starts — our job, the builder's job, and how to tell them apart.

The parent record owns the two-dust-passes argument. Reference the fact that dust returns if you
must, but do not re-explain it — that is the one passage guaranteed to collide with the page every
combo page links up to.

Hard rules:
  - **No town name and no county name anywhere in the file, including comments.**
  - **No postcode, postcode district, street line, phone number, email or price.** Do not name a
    banned shape in a comment.
  - No `h2` may begin `Builders Clean in ` (delta 20).
  - Plain UK English — *builders clean*, *trades*, *snagging*, *skirting boards*. A literal `&`,
    never the entity.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/post-construction-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const V=m.A_VARIANTS;if(V.length!==8)throw new Error('A_VARIANTS has '+V.length);const w=V.map(words);console.log('A words',w);if(Math.min(...w)<280)throw new Error('short A variant: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error('A '+i+','+j+' J='+d.toFixed(3));}console.log('A_VARIANTS OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: exactly 8 entries, each ≥ 280 counted words, all 28 pairwise
      Jaccard ≤ 0.10.
    - `grep -Ec 'Warwick|Leamington|Coventry|Birmingham|Telford|Solihull|Rugby|Stratford|Dudley|Walsall|Wolverhampton|Midlands|Shropshire|Staffordshire|Worcestershire|Oxfordshire|Northamptonshire' web/content/variants/post-construction-cleaning.js` returns 0.
    - `grep -Ec '\b(B|CV|DY|TF|WS|WV|WR|ST|SY|OX|NN)[0-9]{1,2}[A-Z]?\b' …` returns 0; `grep -Ec '£|[0-9]{5}|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' …` returns 0.
    - `grep -c "type: 'h1'" …` returns 0; `grep -c '&amp;' …` returns 0.
    - Cross-check against the parent page: the worst 5-gram Jaccard between any `A_VARIANT` and the
      `post-construction-cleaning` record's `prose` in `services.js` is ≤ 0.10. Report it in the
      SUMMARY.
  </acceptance_criteria>
  <done>Eight distinct passages exist, none re-arguing the parent page's dust-passes angle.</done>
</task>

<task type="auto">
  <name>Task 2: B_VARIANTS — eight how-the-job-runs passages</name>
  <files>web/content/variants/post-construction-cleaning.js</files>
  <read_first>
    - web/content/variants/post-construction-cleaning.js (the `A_VARIANTS` this must not overlap with)
    - web/content/process.js (`PROCESS_STEPS` — stripped from the counted corpus; do not restate it)
  </read_first>
  <action>
Append `export const B_VARIANTS`, exactly eight block arrays. Each: one `h3` then two or three `p`,
**≥ 240 counted words**.

Eight stages, orthogonal to task 1's subjects. Index 0-7 is the `b` value:

  0. Quoting a job that is not finished yet — what we need to see, and what changes the price.
  1. Who comes, how many, and why a builders clean is the one job where team size is the whole plan.
  2. The order we work a site in, and why it runs top-down and away from the exit.
  3. Kit — what we bring that a domestic clean never needs, and what we will not put on a new finish.
  4. Access, power, water and skips — what has to be working before we can start.
  5. Time — a single room versus a whole-house refurbishment, and what we do when a trade is
     still on site.
  6. The walk round at the end, with whoever is signing it off.
  7. Afterwards — the second visit, when it is worth having, and what we come back for.

Do not restate the shared `ProcessSteps` copy. Same hard rules as task 1, unchanged.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/variants/post-construction-cleaning.js')).href);const flat=v=>v.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');const words=v=>flat(v).trim().split(/\s+/).length;const toks=v=>flat(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};for(const k of ['A_VARIANTS','B_VARIANTS']){const V=m[k];if(V.length!==8)throw new Error(k+' has '+V.length);const w=V.map(words);console.log(k,w);const min=k==='A_VARIANTS'?280:240;if(Math.min(...w)<min)throw new Error(k+' short: '+Math.min(...w));const S=V.map(v=>sh(toks(v)));for(let i=0;i<8;i++)for(let j=i+1;j<8;j++){const d=J(S[i],S[j]);if(d>0.10)throw new Error(k+' '+i+','+j+' J='+d.toFixed(3));}}const A=m.A_VARIANTS.map(v=>sh(toks(v)));const B=m.B_VARIANTS.map(v=>sh(toks(v)));let worst=0;for(let i=0;i<8;i++)for(let j=0;j<8;j++)worst=Math.max(worst,J(A[i],B[j]));console.log('worst A-vs-B J',worst.toFixed(3));if(worst>0.10)throw new Error('an A and a B passage overlap: '+worst.toFixed(3));console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 on every clause.
    - `grep -c "type: 'h3'" …` returns 8; `grep -c "type: 'h2'" …` returns 8.
    - The town/county, district, price, `h1` and `&amp;` greps still return 0 over the whole file.
    - Total authored words 4,200-5,200; record both word arrays in the SUMMARY.
    - **Cross-deck sweep.** Every passage in this deck against every passage in every sibling deck
      already present in `web/content/variants/`, worst 5-gram Jaccard ≤ 0.10. The seven B-grids are
      structurally parallel by instruction — the same eight stages of the job for seven different
      services — so this is the region where two decks are most likely to converge. Record how many
      sibling decks were compared and the worst figure. **`compared against 0 sibling deck(s)` is a
      legitimate pass** — the seven variant plans are wave-mates with no ordering between them, so
      this sweep is an early warning whose coverage depends on what has landed, not a gate. Plan
      03-17 task 1 runs the complete deterministic seven-deck sweep over all 6,216 pairs once they
      all exist, and that is the one that has to hold.
      Command:
      `node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';import {readdirSync} from 'node:fs';const dir='web/content/variants';const mine='post-construction-cleaning.js';const load=async f=>await import(pathToFileURL(resolve(dir,f)).href);const toks=v=>v.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};const m=await load(mine);const A=[...m.A_VARIANTS,...m.B_VARIANTS].map(v=>sh(toks(v)));const others=readdirSync(dir).filter(f=>f.endsWith('.js')&&f!==mine);let worst=0,wp='';for(const f of others){const o=await load(f);const B=[...o.A_VARIANTS,...o.B_VARIANTS].map(v=>sh(toks(v)));for(let i=0;i<A.length;i++)for(let j=0;j<B.length;j++){const d=J(A[i],B[j]);if(d>worst){worst=d;wp=f+' #'+j+' vs mine #'+i;}}}console.log('compared against',others.length,'sibling deck(s); worst cross-deck J',worst.toFixed(3),wp);if(worst>0.10)throw new Error('cross-deck collision: '+wp);"`
  </acceptance_criteria>
  <done>The builders-clean 8×8 grid is complete, distinct within and across both axes, and town-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → 58 rendered pages | one careless sentence is published 58 times |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | authored prose | mitigate | no address, postcode, district, phone, email or price in copy or comments |
| T-03-06 | Repudiation | claims about site safety and trades | mitigate | describe what we do; make no claim about certification, insurance limits or site responsibilities that `web/content/legal.js` does not already make |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- Both verify commands exit 0.
- `git diff --stat` touches exactly one file.
- `npm run verify` still exits 0.
</verification>

<success_criteria>
- `A_VARIANTS[8]` and `B_VARIANTS[8]` exported at or above their word floors.
- No pair of passages exceeds 0.10 Jaccard, within a grid or across the two.
- No overlap above 0.10 with the parent service-page prose.
- No town, county, postcode district, price, phone or email in the file.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-07-SUMMARY.md` when done.
</output>
