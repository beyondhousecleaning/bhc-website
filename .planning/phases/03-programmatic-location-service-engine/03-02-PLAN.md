---
phase: 03-programmatic-location-service-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/package.json
  - design-system/src/components/InterlinkBlock/geo.js
  - design-system/src/components/InterlinkBlock/InterlinkBlock.d.ts
  - design-system/src/components/ReviewRail/ReviewRail.jsx
  - design-system/src/components/ReviewRail/ReviewRail.d.ts
  - design-system/src/components/ReviewRail/ReviewRail.html
  - design-system/src/components/ReviewRail/ReviewRail.prompt.md
  - design-system/styles.css
  - design-system/test/locks.test.js
  - design-system/test/run-locks.mjs
autonomous: true
requirements: [REQ-hub-and-spoke-architecture, REQ-programmatic-page-scale]

must_haves:
  truths:
    - "An interlink to a town 46 miles away does not advertise the distance"
    - "The review rail's heading is true on a Telford page as well as a Warwickshire one"
    - "The locations index and the stacked interlink pair have real classes, not inline styles"
  artifacts:
    - path: "design-system/src/components/InterlinkBlock/geo.js"
      provides: "buildInterlinks with an optional metaFor callback, defaulting to today's string"
      contains: "metaFor"
    - path: "design-system/styles.css"
      provides: ".bhc-jump-links, .bhc-town-group (+ __back), .bhc-interlink + .bhc-interlink"
      contains: "bhc-jump-links"
  key_links:
    - from: "design-system/src/components/InterlinkBlock/geo.js"
      to: "design-system/test/locks.test.js"
      via: "the audited pin-distance test, which must stay green"
      pattern: "nearestTowns"
---

<objective>
Three package-level changes Phase 3's templates need, none of which adds a component, a dependency
or a token.

Purpose: `geo.js` already computes the geography Success Criterion 4 requires — the phase satisfies
"computed from geography, never hand-picked" by *using* it. The one thing it does wrong at this
scale is publishing the distance: `46.2 miles away` on a Telford page advertises the opposite of
local. And `ReviewRail`'s default heading claims a county the review data cannot support.

Output: an optional `metaFor` callback, an honest default rail heading, and three CSS classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Sections that bind this plan:
- `03-UI-SPEC.md` §7.2 (call-site rules 1-3), §7.5 (the three classes), §5 (`What our customers say`
  sitewide), §13-G, §13-J, §13-P
- `03-PATTERNS.md` — `design-system/src/components/InterlinkBlock/geo.js (modified — metaFor)`

**This is a package change under D-02 / D-05 / D-06.** Zero dependencies, no build step, the
four-file shape holds, and `delta 9` in `design-system/test/locks.test.js` polices it. No new
component directory — a 23rd component would cost four files, two `.design-sync` registrations and
a raised delta-9 floor for one gap and one border.
</context>

<interfaces>
```js
// design-system/src/components/InterlinkBlock/geo.js — today
export function buildInterlinks({ town, service, allTowns, allServices, nearbyCount = 5 }) {
  // nearby.links[].meta === `${miles.toFixed(1)} miles away`
  // BOTH blocks build `/location/${t.region}/${t.slug}/${service.slug}` — `region` here is the
  // URL SEGMENT, which web/content/towns.js calls `urlRegion`. Opposite meaning to TownCard.region.
}

// design-system/src/components/ReviewRail/ReviewRail.jsx:61
const DEFAULT_HEADING = 'What Warwickshire customers say';
```

Tokens available (locked, `tokens.css`): `--bhc-space-2/3/4/12`, `--bhc-text-sm`,
`--bhc-weight-semibold`, `--bhc-line`, `--bhc-action-hover`. Touch-target floor is
`min-height: 44px` declared directly — padding alone computes to 42.9px at the mobile end of the
`--bhc-text-sm` clamp and looks like it clears the target when it does not.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: geo.js gains an optional metaFor callback</name>
  <files>design-system/package.json, design-system/src/components/InterlinkBlock/geo.js, design-system/src/components/InterlinkBlock/InterlinkBlock.d.ts, design-system/test/locks.test.js, design-system/test/run-locks.mjs</files>
  <read_first>
    - design-system/src/components/InterlinkBlock/geo.js (whole file — 64 lines)
    - design-system/src/components/InterlinkBlock/InterlinkBlock.d.ts (the `buildInterlinks` declaration)
    - design-system/test/locks.test.js lines 645-685 (the two existing geo tests — both must stay green)
    - design-system/test/run-locks.mjs lines 45-60 (`MIN_TESTS`)
  </read_first>
  <action>
Add one optional parameter, `metaFor`, to `buildInterlinks`'s destructured input, defaulting to a
function that returns today's string. Use it to build the `meta` on each `nearby` link. The
`services` block's `meta` (the service tagline) is unchanged.

The idiom to copy is `TownCard.jsx`'s `HEADING_TAGS[headingLevel] || HEADING_TAGS[3]` — a default
rather than a throw, and `Button.jsx:12-17` is what that itself cites. There are no existing callers
to break; the point of the default is that Phase 4 and Phase 5 inherit a component whose behaviour
did not change under them.

Record in the JSDoc *why* it exists: geography stays in the ORDERING, which is where Success
Criterion 4 requires it; the distance is not a thing to publish on a page whose whole job is to read
as local.

Update `InterlinkBlock.d.ts`'s `buildInterlinks` input type with the optional callback. Its
signature takes the destination town and the miles.

**Also add a `./geo` subpath to the package exports map**, pointing at
`./src/components/InterlinkBlock/geo.js`. The barrel (`.`) re-exports the geo functions through
`InterlinkBlock.jsx`, so a plain `node` import of the barrel fails on JSX — and Phase 3's data and
composition modules need `distanceMiles`, `nearestTowns` and `buildInterlinks` in modules that plans
03-16 and 03-17 verify with bare `node`. One line, no dependency, no build step, and the file it
points at is already plain `.js` with no React import.

Add one test to `design-system/test/locks.test.js`, beside the existing geo tests: with no
`metaFor`, `nearby.links[0].meta` still matches the miles-away form; with a `metaFor` returning the
destination's county, it is that county. Raise `MIN_TESTS` in `run-locks.mjs` from 20 to 21 in the
same change — the runner fails a suite that ran fewer tests than expected, which is what makes a
silently-dropped test a red required check.
  </action>
  <verify>
    <automated>node design-system/test/run-locks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node design-system/test/run-locks.mjs` exits 0 and prints `lock suite: 21 passing` (or more).
    - `grep -c 'metaFor' design-system/src/components/InterlinkBlock/geo.js` returns 3 or more
      (parameter with default, call site, JSDoc).
    - `grep -c 'metaFor' design-system/src/components/InterlinkBlock/InterlinkBlock.d.ts` returns 1
      or more.
    - The two pre-existing geo tests (`nearestTowns is ordered by real distance and excludes self`,
      `the audited pin distances reproduce`) still pass, unmodified — confirm by name in the runner
      output.
    - Backwards compatibility proved, not assumed: a `buildInterlinks` call with no `metaFor` still
      produces a `miles away` meta string. This is the new test's first assertion.
    - The subpath resolves from a plain node process:
      `node --input-type=module -e "const g=await import('@bhc/design-system/geo');console.log(typeof g.distanceMiles, typeof g.nearestTowns, typeof g.buildInterlinks);"`
      prints `function function function` and exits 0.
    - `grep -c '"dependencies"' design-system/package.json` unchanged and the package still declares
      none.
  </acceptance_criteria>
  <done>`buildInterlinks` accepts an optional per-link meta formatter, defaults to today's behaviour, and the lock floor tracks the new test.</done>
</task>

<task type="auto">
  <name>Task 2: The default review-rail heading becomes true outside Warwickshire</name>
  <files>design-system/src/components/ReviewRail/ReviewRail.jsx, design-system/src/components/ReviewRail/ReviewRail.d.ts, design-system/src/components/ReviewRail/ReviewRail.html, design-system/src/components/ReviewRail/ReviewRail.prompt.md</files>
  <read_first>
    - design-system/src/components/ReviewRail/ReviewRail.jsx (whole file — the default heading and
      the `town`-narrowed form)
    - design-system/src/components/ReviewRail/ReviewRail.d.ts
    - design-system/src/components/ReviewRail/ReviewRail.html (the preview — delta 9 walks it)
    - .planning/phases/02-component-library-completion-core-templates/02-16-REVIEWS-SUMMARY.md
      (the constraint: no review carries a town and none may be invented one)
  </read_first>
  <action>
Change the default heading to `What our customers say`, in all four files, and update the derived
heading id in the preview so it still matches what the component would render.

The reason belongs in the `.prompt.md` and in the `.jsx` header: plan 02-16 established that no
review in the data carries a town and that none may be invented one, so the county in the old
default is a claim the data cannot support — and on a Telford or Market Drayton page it is simply
false. This phase puts a rail on 406 more pages.

Leave the `town` prop and its narrowing behaviour in place. It is not used by any template in this
phase (§13-J) and Phase 4 owns the decision about town-filtered reviews; deleting the prop would
make that a breaking change instead of a data change.

No lock asserts `<h2>` text, so this is safe. Verify that claim rather than trusting it — the
acceptance criteria below check the built HTML both ways.
  </action>
  <verify>
    <automated>node design-system/test/run-locks.mjs && npm run build --workspace @bhc/web && node --test web/scripts/check-html-locks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'Warwickshire customers say' design-system/src/components/ReviewRail/` returns 0 for
      every file in that directory.
    - `grep -rl 'What our customers say' design-system/src/components/ReviewRail/` lists all four
      files (`.jsx`, `.d.ts`, `.html`, `.prompt.md`).
    - `node --test web/scripts/check-html-locks.mjs` exits 0 with zero failures — proving no lock
      asserted the old string.
    - `grep -c 'What our customers say' web/.next/server/app/index.html` returns 1 or more after the
      build, and the same for one service page.
    - The rail's section id is unchanged: `grep -c 'id="reviews"' web/.next/server/app/index.html`
      returns 1, so delta 15's `#reviews` anchor still resolves.
  </acceptance_criteria>
  <done>The default heading is honest on all 426 routes it will eventually reach, the preview matches the component, and no lock moved.</done>
</task>

<task type="auto">
  <name>Task 3: Three plain CSS classes for the locations index and the stacked interlink pair</name>
  <files>design-system/styles.css</files>
  <read_first>
    - design-system/styles.css — the last 60 lines (the focus-override banner and its rule: every
      later block is appended ABOVE it, never below)
    - design-system/styles.css — the `.bhc-section__action` block (plan 02-16's precedent for a
      plain class a template applies rather than a component prop)
    - `03-UI-SPEC.md` §7.5 (the three classes and their exact declarations), §2 (the 44px rule)
  </read_first>
  <action>
Append three declaration blocks immediately ABOVE the focus-override banner, which must remain the
last section of the file.

  1. `.bhc-jump-links` — the wrapped `<ul>` of county anchors on `/locations`. Gap
     `--bhc-space-2`, `--bhc-text-sm` at semibold, and each link `display: inline-flex;
     align-items: center; min-height: 44px`. Declare the 44px directly; padding alone computes to
     42.9px at the mobile end of the clamp.
  2. `.bhc-town-group` — `+ .bhc-town-group { margin-top: var(--bhc-space-12) }`, plus
     `.bhc-town-group__back` in the same block: `--bhc-text-sm`, `--bhc-action-hover`,
     `margin-top: var(--bhc-space-4)`, `display: inline-flex; align-items: center;
     min-height: 44px`. `__back` is part of this block, not a fourth class.
  3. `.bhc-interlink + .bhc-interlink` — `border-top: 1px solid var(--bhc-line);
     padding-top: var(--bhc-space-12)`. This is the hairline that separates the combo page's
     stacked pair.

Every value is a `var(--bhc-*)` token or the literal `44px` / `1px`. No new token, no undocumented
breakpoint (`480 / 768 / 1024 / 1280`, `min-width` only), no `!important`, and nothing that
suppresses a focus ring.

Do not add a route-level CSS import anywhere in `web/app` to reach these — a second hashed
stylesheet chunk breaks SC-1b, which asserts the app ships exactly one stylesheet linked once per
page.
  </action>
  <verify>
    <automated>node design-system/test/run-locks.mjs && npm run build --workspace @bhc/web && node --test web/scripts/check-html-locks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bhc-jump-links' design-system/styles.css` returns 1 or more, same for
      `bhc-town-group` and for the adjacent-sibling interlink selector.
    - The focus overrides are still last: `grep -n 'focus-visible' design-system/styles.css | tail -1`
      reports a line number greater than every line reported by
      `grep -n 'bhc-jump-links\|bhc-town-group' design-system/styles.css`.
    - `grep -c '!important' design-system/styles.css` returns 0.
    - Every new declaration's colour, spacing and size value is a `var(--bhc-` reference: inspect the
      three blocks and confirm the only bare literals are `44px` and `1px`.
    - `node --test web/scripts/check-html-locks.mjs` exits 0 — in particular SC-1b (`--bhc-ink`
      declared exactly once across built CSS; one stylesheet linked once per page).
  </acceptance_criteria>
  <done>Three token-only classes exist, the focus overrides remain the file's last section, and the single-stylesheet invariant holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package source → 426 rendered pages | anything wrong here is wrong on every page at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `geo.js` link `meta` | mitigate | `metaFor` replaces a published distance matrix with the destination county; the geography stays in the ordering |
| T-03-04 | Tampering | `design-system/styles.css` | accept | no runtime, no user input; the risk is visual regression, covered by the phase's human-verify checkpoint |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; `design-system/package.json` declares zero dependencies and zero devDependencies and must continue to (D-06) |
</threat_model>

<verification>
- `node design-system/test/run-locks.mjs` reports 21+ passing.
- `npm run verify` exits 0.
- `git diff --stat` touches only the nine declared files.
</verification>

<success_criteria>
- `buildInterlinks` takes an optional per-link meta formatter and behaves identically without one.
- No file under `design-system/src/components/ReviewRail/` names the old county-bearing heading.
- Three token-only CSS classes exist above the focus-override banner.
- The design-system lock floor tracks the new test.
- `npm run verify` is green.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-02-SUMMARY.md` when done.
</output>
