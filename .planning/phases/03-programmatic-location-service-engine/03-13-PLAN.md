---
phase: 03-programmatic-location-service-engine
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - web/content/hub-choose-prose.js
autonomous: true
requirements: [REQ-content-depth-bar, REQ-hub-and-spoke-architecture]

must_haves:
  truths:
    - "Every batch-1 town hub has a ~220-word passage helping a reader pick which clean they need"
    - "That passage points at the town's own six services and never at a service the town does not carry"
  artifacts:
    - path: "web/content/hub-choose-prose.js"
      provides: "HUB_CHOOSE[slug] — block B of the town hub, for the 21 batch-1 towns"
      min_lines: 260
      exports: ["HUB_CHOOSE", "hubChooseProse", "HUB_CHOOSE_SLUGS"]
  key_links:
    - from: "web/content/hub-choose-prose.js"
      to: "web/content/hub-town-prose.js"
      via: "the two hub blocks together must clear the 600-word hub floor with the locality paragraph"
      pattern: "hubChooseProse"
---

<objective>
Block B of the town hub — *which clean do you need* — about 220 words per town, for the 21 towns
batch 1 publishes.

Purpose: the hub is the "real parent" REQ-hub-and-spoke requires, and its job is to send a reader
to the right one of the six combo pages beneath it. This block is the prose that does that; the
`ServiceCard` grid above it is the link layer. Together with plan 03-12's block A (260 words) and
plan 03-11's locality paragraph (130 words) plus the hero lead (35), it is what puts the hub at 645
counted words against a 600 floor.

Output: `web/content/hub-choose-prose.js` — roughly 4,700 authored words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Binding sections:
- `03-UI-SPEC.md` §9.4 (the hub template and its six `ServiceCard`s), §10.3 (the hub's block table),
  §5 (the service-concept table and the two six-service sets)
- `03-PATTERNS.md` — the copy-deck section

**The town's six services are not the same six everywhere.** All 21 batch-1 towns carry
`apartment` · `deep` · `domestic` · `end-of-tenancy` · `post-construction`. The sixth is
`move-out` on the 18 legacy towns and `short-term-rental` on the three new ones — `coventry`,
`henley-in-arden` and `solihull`. A passage that offers a town a service it does not carry points at
a page that is not built, which delta 6 turns red after the fact; getting it right here is cheaper.
</context>

<interfaces>
```js
// web/content/hub-choose-prose.js
export const HUB_CHOOSE = {
  'leamington-spa': [
    { type: 'h2', text: '…' },
    { type: 'p',  text: '…' },
    { type: 'p',  text: '…' },
  ],
  // …
};
export function hubChooseProse(slug) { /* throws on a miss */ }
export const HUB_CHOOSE_SLUGS = /* sorted slugs, for plan 03-16's guard */;
```

The visible service nouns, from §5's concept table — use these words and no others:
**Domestic Cleaning · Deep Cleaning · End of Tenancy Cleaning · Flat Cleaning · Builders Clean ·
Move-Out Cleaning · Holiday Let Cleaning**. Never *apartment*, never *post-construction*, never
*short-term rental* as visible prose.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seven hubs — barford, bishops-itchington, hatton, kenilworth, leamington-spa, long-itchington, warwick</name>
  <files>web/content/hub-choose-prose.js</files>
  <read_first>
    - web/content/services.js lines 1-90 (module header house style, the no-address rule, and the
      rule that a comment naming a banned shape is itself an occurrence of it)
    - web/content/home.js lines 150-230 (`HOME_SERVICE_CARDS` — the shipped one-or-two-sentence
      summaries of each service, the register this prose sits beside)
    - web/content/reviews.js lines 346-356 (the throw-on-miss accessor idiom)
  </read_first>
  <action>
Create `web/content/hub-choose-prose.js` with a header in the `services.js` house style, then
`export const HUB_CHOOSE` carrying the seven towns above. Each entry: one `h2` and two `p`,
**≥ 210 counted words**.

This block answers *which of these six do I need?* for somebody standing on the hub. It is
decision-shaped: the situation that points at each service, the two that people most often confuse,
and the honest boundary between them. All seven of these towns are legacy towns, so the sixth
service is **Move-Out Cleaning** — never Holiday Let Cleaning.

Make each town's version genuinely local rather than a shared paragraph with a name dropped in.
Different towns have different mixes — a lot of rented flats, a lot of family houses that turn over
slowly, a lot of second homes, a lot of new build — and which two services get confused most often
follows from that. That is the variation §10.2's first prohibition demands and it is real, not
cosmetic.

Hard rules, each with a gate behind it:
  - **No postcode, postcode district, street line, phone number, email or price**, in copy or
    comment. Do not name a banned shape in a comment.
  - Use only §5's display nouns for the services.
  - The town's own name and its real county may appear. No other town's name may.
  - No `h2` may be a template with the town name substituted in.
  - Plain UK English. A literal `&`, never the entity.

Also export `hubChooseProse(slug)` (throws on a miss) and `HUB_CHOOSE_SLUGS`.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/hub-choose-prose.js')).href);const want=['barford','bishops-itchington','hatton','kenilworth','leamington-spa','long-itchington','warwick'];const flat=b=>b.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ');const words=b=>flat(b).trim().split(/\s+/).length;const toks=b=>flat(b).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};for(const s of want){const b=m.hubChooseProse(s);const w=words(b);console.log(s,w);if(w<210)throw new Error(s+' is '+w+' words');const t=flat(b);if(/Holiday Let/i.test(t))throw new Error(s+' is a legacy town and must not offer Holiday Let Cleaning');if(/apartment|post-construction|short-term rental/i.test(t))throw new Error(s+' uses a slug word as a visible noun');}const S=want.map(s=>sh(toks(m.hubChooseProse(s))));let worst=0,pair='';for(let i=0;i<S.length;i++)for(let j=i+1;j<S.length;j++){const d=J(S[i],S[j]);if(d>worst){worst=d;pair=want[i]+' vs '+want[j];}}console.log('worst pair',pair,worst.toFixed(3));if(worst>0.10)throw new Error('passages collide: '+pair);let threw=false;try{m.hubChooseProse('nowhere-town');}catch{threw=true;}if(!threw)throw new Error('hubChooseProse must throw on an unknown slug');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: seven towns, each ≥ 210 counted words, no legacy town offering
      Holiday Let Cleaning, no slug word used as a visible noun, worst pairwise Jaccard ≤ 0.10,
      unknown slug throws.
    - `grep -Ec '\b[A-Z]{1,2}[0-9]{1,2}[A-Z]?\b' web/content/hub-choose-prose.js` returns 0.
    - `grep -Ec '£|[0-9]{5}|@' web/content/hub-choose-prose.js` returns 0.
    - `grep -c "type: 'h1'" …` returns 0; `grep -c '&amp;' …` returns 0; `grep -c '|| \[\]' …` returns 0.
  </acceptance_criteria>
  <done>Seven decision-shaped hub passages exist, each offering only the six services its town carries.</done>
</task>

<task type="auto">
  <name>Task 2: Seven hubs — henley-in-arden, rugby, ryton-on-dunsmore, southam, stratford-upon-avon, wellesbourne, whitnash</name>
  <files>web/content/hub-choose-prose.js</files>
  <read_first>
    - web/content/hub-choose-prose.js (the seven entries from task 1)
    - `03-UI-SPEC.md` §5 (the two six-service sets — `henley-in-arden` is a NEW town)
  </read_first>
  <action>
Add these seven, same shape, same floor, same rules — with one difference that matters:

**`henley-in-arden` is a new town, not a legacy one.** Its sixth service is **Holiday Let Cleaning**
and it does NOT carry Move-Out Cleaning. The other six in this task are legacy towns and carry
Move-Out Cleaning and not Holiday Let Cleaning. Getting this backwards points a paragraph at a page
that is not built.

Stratford-upon-Avon is a genuine holiday-let market and does not carry that service, because D2
freezes its five live URLs and §13-D refuses to extend the duplicated tenancy intent. Do not offer
it there and do not apologise for its absence — the region-wide service page covers that job, and
the interlink layer reaches it.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/hub-choose-prose.js')).href);const legacy=['barford','bishops-itchington','hatton','kenilworth','leamington-spa','long-itchington','warwick','rugby','ryton-on-dunsmore','southam','stratford-upon-avon','wellesbourne','whitnash'];const fresh=['henley-in-arden'];const flat=b=>b.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ');const words=b=>flat(b).trim().split(/\s+/).length;const toks=b=>flat(b).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};for(const s of legacy){const t=flat(m.hubChooseProse(s));if(words(m.hubChooseProse(s))<210)throw new Error(s+' short');if(/Holiday Let/i.test(t))throw new Error(s+' offers Holiday Let and is a legacy town');}for(const s of fresh){const t=flat(m.hubChooseProse(s));if(/Move-Out/i.test(t))throw new Error(s+' offers Move-Out and is a new town');}const all=[...legacy,...fresh];const S=all.map(s=>sh(toks(m.hubChooseProse(s))));let worst=0,pair='';for(let i=0;i<S.length;i++)for(let j=i+1;j<S.length;j++){const d=J(S[i],S[j]);if(d>worst){worst=d;pair=all[i]+' vs '+all[j];}}console.log('14 hubs, worst pair',pair,worst.toFixed(3));if(worst>0.10)throw new Error('passages collide: '+pair);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: 14 towns at their floor, the legacy/new service split correct in
      both directions, worst pairwise Jaccard ≤ 0.10.
    - The postcode-district, price, `h1`, `&amp;` and `|| []` greps still return 0 over the whole file.
  </acceptance_criteria>
  <done>Fourteen hub passages exist and each offers exactly its own town's six services.</done>
</task>

<task type="auto">
  <name>Task 3: Seven hubs — coventry, solihull, solihull-south, south-coventry, banbury, daventry, evesham</name>
  <files>web/content/hub-choose-prose.js</files>
  <read_first>
    - web/content/hub-choose-prose.js (the 14 entries from tasks 1 and 2)
    - `03-UI-SPEC.md` §5 (the two six-service sets), §8.2 (the two-region rule), §13-E
  </read_first>
  <action>
Add the last seven, same shape, same floor, same rules, with three specifics:

  1. **`coventry` and `solihull` are NEW towns** — sixth service is Holiday Let Cleaning, and they
     carry no Move-Out Cleaning. `south-coventry`, `solihull-south`, `banbury`, `daventry` and
     `evesham` are legacy towns — sixth service is Move-Out Cleaning, no Holiday Let Cleaning. Four
     of these seven are one of a merged pair with different service sets, which is the single
     easiest thing to get wrong in this file.
  2. **Counties.** Banbury is Oxfordshire, Daventry is Northamptonshire, Evesham is Worcestershire.
     Nothing in those three passages may say Warwickshire.
  3. **The merged pairs must read as separate places** — city versus southern suburbs, town centre
     versus the villages south of it — for the same reason as in plans 03-11 and 03-12.

Finalise `HUB_CHOOSE_SLUGS` at all 21.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {pathToFileURL} from 'node:url';import {resolve} from 'node:path';const m=await import(pathToFileURL(resolve('web/content/hub-choose-prose.js')).href);const all=['banbury','barford','bishops-itchington','coventry','daventry','evesham','hatton','henley-in-arden','kenilworth','leamington-spa','long-itchington','rugby','ryton-on-dunsmore','solihull','solihull-south','south-coventry','southam','stratford-upon-avon','warwick','wellesbourne','whitnash'];if(m.HUB_CHOOSE_SLUGS.join(',')!==all.join(','))throw new Error('HUB_CHOOSE_SLUGS is '+m.HUB_CHOOSE_SLUGS.join(','));const fresh=new Set(['coventry','henley-in-arden','solihull']);const flat=b=>b.flatMap(x=>Array.isArray(x.text)?x.text:[x.text]).join(' ');const words=b=>flat(b).trim().split(/\s+/).length;const toks=b=>flat(b).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim().split(' ');const sh=w=>{const s=new Set();for(let i=0;i+5<=w.length;i++)s.add(w.slice(i,i+5).join(' '));return s;};const J=(A,B)=>{let h=0;for(const x of A)if(B.has(x))h++;return h/(A.size+B.size-h);};let total=0;for(const s of all){const b=m.hubChooseProse(s);const w=words(b);total+=w;if(w<210)throw new Error(s+' is '+w);const t=flat(b);if(fresh.has(s)&&/Move-Out/i.test(t))throw new Error(s+' is new and offers Move-Out');if(!fresh.has(s)&&/Holiday Let/i.test(t))throw new Error(s+' is legacy and offers Holiday Let');if(/apartment|post-construction|short-term rental/i.test(t))throw new Error(s+' uses a slug word as a visible noun');}const S=all.map(s=>sh(toks(m.hubChooseProse(s))));const idx=n=>all.indexOf(n);console.log('cov vs south-cov',J(S[idx('coventry')],S[idx('south-coventry')]).toFixed(3),'sol vs sol-south',J(S[idx('solihull')],S[idx('solihull-south')]).toFixed(3));let worst=0,pair='';for(let i=0;i<S.length;i++)for(let j=i+1;j<S.length;j++){const d=J(S[i],S[j]);if(d>worst){worst=d;pair=all[i]+' vs '+all[j];}}console.log('21 hubs, total words',total,'worst pair',pair,worst.toFixed(3));if(worst>0.10)throw new Error('passages collide: '+pair);console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: `HUB_CHOOSE_SLUGS` is exactly the 21 batch-1 slugs sorted, every
      passage ≥ 210 counted words, the legacy/new service split correct for all 21 in both
      directions, worst pairwise Jaccard ≤ 0.10.
    - The two merged-pair figures are printed and both ≤ 0.10; record them in the SUMMARY.
    - `grep -c 'Oxfordshire' …` ≥ 1, same for `Northamptonshire` and `Worcestershire`, and none of
      those three towns' passages says Warwickshire.
    - The postcode-district, price, `h1`, `&amp;` and `|| []` greps still return 0 over the whole file.
    - Total authored words 4,400-5,600.
  </acceptance_criteria>
  <done>All 21 hub decision passages exist, each offering exactly its own town's six services, county-correct, and distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → one hub page per town | a passage offering a service the town does not carry points a reader at a page that is not built |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | authored prose | mitigate | no address, postcode, district, phone, email or price in copy or comments |
| T-03-12 | Repudiation | offering a service the town does not carry | mitigate | the legacy/new split asserted in both directions by the verify command, before delta 6 has to catch it over built HTML |
| T-03-10 | Repudiation | a hub published under a false county | mitigate | banbury / daventry / evesham say their real counties |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan; this phase installs zero packages |
</threat_model>

<verification>
- All three verify commands exit 0.
- `git diff --stat` touches exactly one file.
- `npm run verify` still exits 0 — nothing imports this module yet.
</verification>

<success_criteria>
- 21 hub decision passages, each ≥ 210 counted words.
- Every passage offers exactly the six services its town carries, using §5's display nouns only.
- No two passages exceed 0.10 5-gram Jaccard, including the merged pairs.
- Banbury, Daventry and Evesham read their real counties.
- No postcode district, price, phone or email in the file.
</success_criteria>

<output>
Create `.planning/phases/03-programmatic-location-service-engine/03-13-SUMMARY.md` when done.
</output>
