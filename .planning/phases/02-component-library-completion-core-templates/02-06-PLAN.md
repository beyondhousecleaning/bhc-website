---
phase: 02-component-library-completion-core-templates
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-04"]
files_modified:
  - web/jsconfig.json
  - web/content/site.js
  - web/content/nav.js
  - web/content/process.js
  - web/content/faqs.js
  - web/content/blocks.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Route files import site copy through a stable alias that does not change with directory depth"
    - "The header nav tree and the footer columns are declared once and reach every one of the 17 routes"
    - "The rating figure, the description form and the served-town list are declared once, not seventeen times"
    - "Every route that renders long-form copy maps its blocks through one shared renderer"
  artifacts:
    - path: "web/jsconfig.json"
      provides: "the @/* path alias"
      contains: "paths"
    - path: "web/content/nav.js"
      provides: "the Header NavItem tree, the three Footer link columns and the legal row"
      contains: "FOOTER_COLUMNS"
    - path: "web/content/site.js"
      provides: "RATING, AREA_SERVED, HOURS, the title form and the description form"
      contains: "AREA_SERVED"
    - path: "web/content/blocks.jsx"
      provides: "renderBlocks — the one prose-block to element mapping every content route uses"
      contains: "renderBlocks"
  key_links:
    - from: "web/content/nav.js"
      to: "the 17 Phase 2 routes"
      via: "every href in the nav tree and footer columns is a route that plan 02-02's delta 6 will assert"
      pattern: "/services/"
    - from: "web/content/site.js AREA_SERVED"
      to: "NAPFooter's HomeAndConstructionBusiness JSON-LD"
      via: "plan 02-13 passes it through Footer so the areaServed City nodes survive the layout rewrite"
      pattern: "AREA_SERVED"
    - from: "web/content/blocks.jsx"
      to: "plans 02-08, 02-09 and 02-12"
      via: "renderBlocks consumed by every route rendering a prose block array"
      pattern: "renderBlocks"
---

<objective>
Create the app-side data layer the templates and the lock harness both depend on: a `@/*` path
alias, the site-copy modules that are not tied to a single route (`site.js`, `nav.js`, `process.js`,
`faqs.js`), and the one shared prose-block renderer every content route maps through.

Purpose: framework-agnostic UI components must not carry site copy, and the copy must not live in
`.jsx` files — the `claude-seo` PostToolUse hook rejects a `.jsx` write containing the
case-insensitive substring `REPLACE`, which ordinary prose like "we'll replace" contains. A sibling
`web/content/` directory plus the alias is also stable at any route depth, which matters when Phase 3
authors `app/location/[region]/[town]/[service]/page.jsx`.

`renderBlocks` lands here, in wave 3, rather than in one of its three consumer plans, because plans
02-08 and 02-09 are wave-mates: if either owned the file the other would import something that did
not yet exist.

Output: `web/jsconfig.json`, four data modules and one renderer. No route file changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-02-SUMMARY.md
@.planning/phases/02-component-library-completion-core-templates/02-04-SUMMARY.md

<consumer_schemas>
<!-- These shapes are already fixed by shipped .d.ts files and by UI-SPEC §7. Match them exactly
     or the components will not consume the data. -->

From design-system/src/components/NAPFooter/NAPFooter.d.ts:
  interface FooterLink   { label: string; href: string; }
  interface FooterColumn { heading: string; links: FooterLink[]; }
  // NAPFooter also takes `areaServed?: string[]` (default []) and `hours?: string`.

From 02-UI-SPEC.md §7.1 (Header):
  interface NavItem { label: string; href?: string; children?: NavItem[]; }
  // `href` is OPTIONAL. A NavItem WITH children renders as a <details>/<summary> disclosure and
  // its own href is never emitted — see task 2.

From 02-UI-SPEC.md §7.8 (ProcessSteps):
  interface Step { title: string; body: string; }

From 02-UI-SPEC.md §7.9 (FAQAccordion):
  interface FaqItem { question: string; answer: ReactNode; }
  // answer is authored as a plain string here; the component wraps it.

THE PROSE BLOCK SHAPE, defined by this plan and consumed by 02-08, 02-09 and 02-12:
  type Block =
    | { type: 'p';  text: string }
    | { type: 'h2'; text: string }
    | { type: 'h3'; text: string }
    | { type: 'ul'; text: string[] }     // note: `text` carries the ITEMS for a list

The 17 Phase 2 routes, from 02-RESEARCH.md § "The Route Inventory, Resolved":
  /
  /services/{deep-cleaning, standard-home-cleaning, move-in-cleaning, move-out-cleaning,
             short-term-rental-cleaning, post-construction-cleaning}
  /about-us /checklist /contact-us /customer-login /customer-service-agreement
  /get-a-quote /gift-cards /privacy-policy /terms-of-service /work-with-us
</consumer_schemas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The @/* alias and web/content/site.js</name>
  <files>web/jsconfig.json, web/content/site.js</files>
  <read_first>
    - web/package.json and web/next.config.mjs — confirm no existing alias configuration and no `paths` entry anywhere.
    - web/app/layout.jsx lines 60-70 — the shipped `<NAPFooter areaServed={['Leamington Spa', 'Warwick', 'Kenilworth', 'Coventry']} hours="Mon–Sat, 8am–7pm" />` call. Those four town names are currently a literal in the layout and this task moves them into data.
    - design-system/src/components/NAPFooter/NAPFooter.jsx lines 43 and 57-58 — `areaServed = []` by default, and the `HomeAndConstructionBusiness` JSON-LD emits `areaServed` City nodes ONLY when the array is non-empty. Dropping the prop silently removes `areaServed` from the structured data on every page, and no lock catches it.
    - design-system/src/components/InterlinkBlock/geo.js — the module-shape analog: a plain `.js` export of pure data and functions with a header comment stating why it lives apart from the `.jsx`.
    - design-system/src/components/RatingBadge/RatingBadge.d.ts — the `rating` / `count` / `source` prop shape `site.js` must supply.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "Recommended structure" and § "Why `web/content/` and not `web/app/_content/` or the package".
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 — the measured titles and the description form.
  </read_first>
  <action>
Create `web/jsconfig.json` containing exactly `{ "compilerOptions": { "paths": { "@/*": ["./*"] } } }`.
Verified working on Next 16.3.0. Do not add `baseUrl` unless the build demands it, and do not create
a `tsconfig.json` — this is a JavaScript app.

Create `web/content/site.js` as a plain `.js` module with a `geo.js`-style header comment stating
that site copy lives here rather than in a `.jsx` for two independent reasons: framework-agnostic UI
components must not carry site copy, and the `claude-seo` hook rejects a `.jsx` write containing the
substring `REPLACE`, which ordinary prose contains.

Export:
- `RATING` — `{ rating: 4.9, count: 175, source: 'Google' }`. Every template passes this to `Hero`.
  Add a comment recording that Phase 4 makes this a live figure and that `emitSchema` stays OFF on
  every Phase 2 template (plan 02-02's SC-4f replacement encodes that answer, so flipping it here
  turns CI red).
- `BRAND` — `'Beyond House Cleaning'`.
- **`AREA_SERVED`** — `['Leamington Spa', 'Warwick', 'Kenilworth', 'Coventry']`, lifted verbatim from
  the literal currently inline at `web/app/layout.jsx`. Add a comment recording WHY this must exist
  as a named export: `NAPFooter` emits `areaServed` City nodes into the sitewide
  `HomeAndConstructionBusiness` JSON-LD only when the array is non-empty, its default is `[]`, and
  plan 02-13 rewrites the layout to render `<Footer/>` instead of `<NAPFooter/>` directly. If the
  value is not threaded through `Footer`, all 18 pages silently lose `areaServed` from their
  structured data, no lock catches it, and ROADMAP Phase 5 SC-1 explicitly requires
  `LocalBusiness` + `areaServed`.
- **`HOURS`** — `'Mon–Sat, 8am–7pm'`, for the same reason. Note in the comment that this one is
  harmless in isolation because it equals `NAPFooter`'s own default, but it travels with
  `AREA_SERVED` so the pair cannot drift.
- `titleFor(pageTitle)` — returns `` `${pageTitle} | ${BRAND}` ``. A pure function in a `.js` file, so
  `.replace(` is safe here if needed.
- `describe(serviceOrSubject)` — returns the UI-SPEC §5 description form:
  `Rated 4.9 by 175 Google reviews. {subject} across Warwickshire by DBS-checked, insured local cleaners. Free quote in two minutes.`
  Add an assertion-friendly comment recording that §5 measured this at 133 to 146 characters against
  the 155 cap, and that Lock 8's enforcement is Phase 5's job.
- `AREA_SHORT` — `'Warwickshire'` and `AREA_LONG` — `'Warwickshire & the West Midlands'`, so the two
  forms the `<h1>` deck and the title deck use are declared once. Note in a comment that `&` renders
  as `&amp;` in built HTML and that plan 02-02's `<h1>` extractor decodes it before comparing.

Do NOT put per-route copy in this file. `utility.js`, `legal.js`, `home.js` and `services.js` belong
to plans 02-08, 02-09, 02-11 and 02-12. Do NOT edit `web/app/layout.jsx` in this plan — plan 02-13
owns that file and imports these exports.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `web/jsconfig.json` parses as JSON and its `compilerOptions.paths` maps `@/*` to `./*`.
    - `npm run build` exits 0 with the alias present.
    - `node --input-type=module -e "import {RATING, titleFor, describe, AREA_LONG, AREA_SERVED, HOURS} from './web/content/site.js'; if(RATING.rating!==4.9||RATING.count!==175||RATING.source!=='Google'){console.error(RATING);process.exit(1)} const t=titleFor('House Cleaning in Warwickshire'); if(t!=='House Cleaning in Warwickshire | Beyond House Cleaning'){console.error(t);process.exit(1)} const d=describe('Deep cleaning'); if(d.length>155){console.error('description is',d.length,'chars');process.exit(1)} if(!AREA_LONG.includes('&')){console.error('AREA_LONG missing ampersand');process.exit(1)} if(!Array.isArray(AREA_SERVED)||AREA_SERVED.length!==4){console.error('AREA_SERVED',AREA_SERVED);process.exit(1)} if(typeof HOURS!=='string'||!HOURS){console.error('HOURS',HOURS);process.exit(1)} console.log('ok')"` prints `ok`.
    - `AREA_SERVED` matches the layout's current literal exactly: `node --input-type=module -e "import {AREA_SERVED} from './web/content/site.js';const want=['Leamington Spa','Warwick','Kenilworth','Coventry'];if(JSON.stringify(AREA_SERVED)!==JSON.stringify(want)){console.error('got',AREA_SERVED,'want',want);process.exit(1)}console.log('ok')"` prints `ok`.
    - Every title produced by `titleFor` for the seven UI-SPEC §5 titles is 60 characters or fewer — check each one and record the measured lengths in the SUMMARY.
    - `grep -c "emitSchema" web/content/site.js` returns 0 in any executable position; any mention is a comment recording the decision.
    - `git diff web/app/layout.jsx` is empty — this plan does not touch the layout.
  </acceptance_criteria>
  <done>Route files can import site-wide constants through `@/content/site.js`, and the rating, brand, served-town list, opening hours, area forms, title form and description form each exist exactly once.</done>
</task>

<task type="auto">
  <name>Task 2: nav.js, process.js, faqs.js and the shared block renderer</name>
  <files>web/content/nav.js, web/content/process.js, web/content/faqs.js, web/content/blocks.jsx</files>
  <read_first>
    - design-system/src/components/NAPFooter/NAPFooter.d.ts lines 1-2 — the exact `FooterLink` and `FooterColumn` shapes.
    - design-system/src/components/NAPFooter/NAPFooter.jsx lines 89-107 — the guard idiom (`columns.filter(Boolean)` and `(col.links || [])`) and its comment explaining that this footer is in the root layout, so a single malformed entry would 500 EVERY route rather than break one list. The data authored here is what that guard protects against.
    - design-system/src/components/Prose/Prose.jsx and its `.d.ts` (from plan 02-04) — `Prose` styles its descendants through `:where()` and never emits an `<h1>`; `renderBlocks` therefore needs no classes of its own and must never emit an `<h1>`.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §7.1 (the Phase 2 Header nav: Services with six children, About, Checklist, Contact — and the note that Phase 3 inserts "Areas We Cover" once `/locations` resolves), §7.2 (the footer link columns and the legal row), §5 (the three ProcessSteps and the closed CTA vocabulary).
    - This plan's `<consumer_schemas>` block — the 17 routes every href must be drawn from, the fact that `NavItem.href` is optional, and the `Block` shape.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "The Route Inventory, Resolved" — consequence 1: descoping a route is not a smaller scope, it is a broken build.
  </read_first>
  <action>
**`web/content/nav.js`** — export three constants.

`NAV` — the Header `NavItem[]` for Phase 2:
- a `Services` item with **NO `href` of its own** and `children` set to the six service pages at
  their live slugs (UI-SPEC §13-C: today's slugs, not the D14 canonical ones — the four 301s are
  Phase 3's). The Services item is a disclosure, not a link: `/services` 404s in Phase 2 so a link
  to it would fail delta 6, and giving it its own child's href would put that href in the DOM twice
  and fail plan 02-10's no-duplicate-href assertion on the Header. Add a comment stating both
  reasons and pointing at plan 02-10 task 2, which renders a `NavItem` with `children` as a
  `<summary>` and never emits its `href`.
- `About` -> `/about-us`, `Checklist` -> `/checklist`, `Contact` -> `/contact-us`, each with an href.
- A comment recording that Phase 3 inserts `Areas We Cover` -> `/locations` here once that route
  resolves.

`FOOTER_COLUMNS` — the **three link columns** from §7.2, exactly matching the `FooterColumn` shape:
- `Services` — the six service pages
- `Company` — About Us, Work With Us, Contact Us, Gift Cards
- `Customers` — What's Included (`/checklist`), Get a Quote (`/get-a-quote`), Customer Login

`LEGAL` — the bottom row, exported SEPARATELY and not as a fourth column: Privacy Policy, Terms of
Service, Customer Service Agreement. §7.2 lists four rows in its table but the fourth is the legal
bottom row, which `Footer` passes to `NAPFooter` as `legal`, not as a column. Do not invent a fourth
`FooterColumn` — it would need hrefs outside the 17-route set.

Between the nav tree, the columns and the legal row, EVERY ONE of the 17 routes must be reachable
from every page. Add a header comment stating that invariant and naming delta 6 as its enforcement.
Do not add any href that is not one of the 17.

**`web/content/process.js`** — export `PROCESS_STEPS`, the three `{ title, body }` records from
UI-SPEC §5 verbatim: "Tell us about your home", "We match you with a local cleaner", "You get your
evening back". Add a comment recording that step 3's remedy wording is a commercial promise awaiting
Sam's confirmation (UI-SPEC §14-1), that the shipped default deliberately states no time window and
no re-clean commitment, and that a specific policy drops straight in as a data change.

**`web/content/faqs.js`** — export named FAQ sets as `{ question, answer }` arrays: a `GENERAL_FAQS`
set used on Home and the utility pages, and a `serviceFaqs(slug)` lookup returning the per-service
set. Author four to six questions per set in the UI-SPEC §5 voice: plain UK English, contractions,
concrete nouns, named towns, and UK vocabulary throughout (`tenancy`, `flat`, `skirting boards`,
`hoover`, `builders clean` — never `apartment`, `baseboards`, `vacuum`).

**`web/content/blocks.jsx`** — export `renderBlocks(blocks)`, the ONE mapping from the `Block` shape
in `<consumer_schemas>` to elements. Plans 02-08, 02-09 and 02-12 all import it; nothing duplicates
it inline. It is the only `.jsx` in `web/content/` and the header comment must say why: it returns
elements, so it cannot be a `.js` data module, but it carries no copy and — critically — **no string
manipulation**, so the `claude-seo` hook has nothing to reject. Rules:
- `p` -> `<p>`, `h2` -> `<h2>`, `h3` -> `<h3>`, `ul` -> `<ul>` with one `<li>` per entry of `text`.
- Key every element by its index; block arrays are authored data and have no natural id.
- Unknown `type` renders `null` rather than throwing, and unknown types are dropped with
  `.filter(Boolean)` — a data typo must not 500 a route in the root layout's page tree.
- **It must never emit an `<h1>`.** Lock 1 allows exactly one per page and it belongs to `Hero`.
  State that in the header comment and enforce it by simply not mapping an `h1` type.
- **Never write the client directive literally in any file under `web/content`, prose included.**
  Plan 02-02's delta 11 scans this directory as one of its three source roots with a plain
  substring grep, so even an explanatory comment naming it turns `check:html` red. Refer to it as
  "the client directive", following `web/app/page.jsx`'s convention.

All four files carry `geo.js`-style header comments. No imports from the design-system package.
  </action>
  <verify>
    <automated>npm run build && npm run check:html</automated>
  </verify>
  <acceptance_criteria>
    - Every href across `NAV`, `FOOTER_COLUMNS` and `LEGAL` is one of the 17 Phase 2 routes, and together they cover all 17: `node --input-type=module -e "import {NAV,FOOTER_COLUMNS,LEGAL} from './web/content/nav.js';const ROUTES=new Set(['/','/services/deep-cleaning','/services/standard-home-cleaning','/services/move-in-cleaning','/services/move-out-cleaning','/services/short-term-rental-cleaning','/services/post-construction-cleaning','/about-us','/checklist','/contact-us','/customer-login','/customer-service-agreement','/get-a-quote','/gift-cards','/privacy-policy','/terms-of-service','/work-with-us']);const seen=new Set();const walk=i=>{if(i.href){if(!ROUTES.has(i.href)){console.error('unknown href',i.href);process.exit(1)}seen.add(i.href)}(i.children||[]).forEach(walk)};NAV.forEach(walk);FOOTER_COLUMNS.forEach(c=>c.links.forEach(walk));LEGAL.forEach(walk);const missing=[...ROUTES].filter(r=>r!=='/'&&!seen.has(r));if(missing.length){console.error('routes not reachable from nav or footer:',missing);process.exit(1)}console.log('all reachable')"` prints `all reachable`.
    - The Services NavItem has six children and NO href of its own: `node --input-type=module -e "import {NAV} from './web/content/nav.js';const s=NAV.find(i=>i.label==='Services');if(!s){console.error('no Services item');process.exit(1)}if((s.children||[]).length!==6){console.error('expected 6 children, got',(s.children||[]).length);process.exit(1)}if(s.href!==undefined){console.error('Services must not carry its own href, got',s.href);process.exit(1)}console.log('ok')"` prints `ok`.
    - `grep -c '"/services"' web/content/nav.js` returns 0.
    - `FOOTER_COLUMNS` has exactly three entries and `LEGAL` exactly three: `node --input-type=module -e "import {FOOTER_COLUMNS,LEGAL} from './web/content/nav.js';if(FOOTER_COLUMNS.length!==3){console.error('expected 3 columns, got',FOOTER_COLUMNS.length);process.exit(1)}if(LEGAL.length!==3){console.error('expected 3 legal links, got',LEGAL.length);process.exit(1)}for(const c of FOOTER_COLUMNS){if(!c.heading||!Array.isArray(c.links)){console.error('bad column',c);process.exit(1)}for(const l of c.links)if(!l.label||!l.href){console.error('bad link',l);process.exit(1)}}console.log('ok')"` prints `ok`.
    - `PROCESS_STEPS` has exactly three entries, each with non-empty `title` and `body`.
    - `renderBlocks` maps all four types, drops an unknown type, and emits no `<h1>`: `node --input-type=module -e "import {renderToStaticMarkup as r} from 'react-dom/server';import {renderBlocks} from './web/content/blocks.jsx';const h=r(renderBlocks([{type:'h2',text:'What we clean'},{type:'p',text:'Skirting boards to oven interiors.'},{type:'h3',text:'Kitchens'},{type:'ul',text:['Hob','Oven','Splashback']},{type:'h1',text:'must be dropped'},{type:'nonsense',text:'x'}]));for(const t of ['<h2','<p','<h3','<ul','<li'])if(!h.includes(t)){console.error('missing',t);process.exit(1)}if((h.match(/<li/g)||[]).length!==3){console.error('list items');process.exit(1)}if(/<h1/.test(h)||h.includes('must be dropped')||h.includes('nonsense')){console.error('unknown or h1 block was rendered');process.exit(1)}console.log('ok')"` prints `ok`.
    - No phone digits or postcodes in the content modules: `node -e "const fs=require('fs');for(const f of ['web/content/nav.js','web/content/process.js','web/content/faqs.js','web/content/site.js','web/content/blocks.jsx']){const s=fs.readFileSync(f,'utf8');if(/\b[A-Z]{1,2}\d[A-Z\d]?\s?\d[A-Z]{2}\b/.test(s)){console.error('postcode-shaped string in',f);process.exit(1)}if(/447861936533|07861|07441|07575/.test(s)){console.error('phone digits in',f);process.exit(1)}}console.log('ok')"` prints `ok`.
    - UK vocabulary check on `faqs.js`: `grep -ci "apartment\|baseboard\|vacuum cleaner" web/content/faqs.js` returns 0.
    - `npm run build` and `npm run check:html` both exit 0 — no route consumes these modules yet, so the built output is unchanged.
    - **Negative proof that `web/content` is genuinely scanned** (run, record the failing assertion name in the SUMMARY, then revert): add the client directive to the top of a scratch file under `web/content/` and confirm `npm run check:html` exits non-zero. Plan 02-02 created `web/content/.gitkeep` in wave 1 so the root has existed since then, but nothing has yet proven the scan reaches it — without this check the third root is unverified for the rest of the project.
  </acceptance_criteria>
  <done>The nav tree, three footer columns and separate legal row between them reach all 17 routes and link to nothing else; the Services item is a childed disclosure with no href of its own; the three process steps and the FAQ sets exist as plain data; and one shared `renderBlocks` is available to all three consumer plans before any of them runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data module -> root layout -> every route | `nav.js` is consumed by `Header` and `Footer` inside the root layout, so a malformed entry has a 17-route (later ~410-route) blast radius |
| authored copy -> `renderBlocks` -> built HTML | FAQ answers, step bodies and prose blocks are the first real prose volume the postcode and street-line matchers meet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-30 | Denial of Service | `nav.js` malformed entry | mitigate | Data authored to the exact `FooterColumn`/`NavItem` shapes, and `NAPFooter`'s shipped `.filter(Boolean)` / `(col.links \|\| [])` guards absorb a bad entry rather than throwing during server render (task 2) |
| T-02-31 | Denial of Service | an unknown block `type` | mitigate | `renderBlocks` returns `null` for an unknown type and filters it out rather than throwing; a data typo must not 500 a route (task 2) |
| T-02-32 | Information disclosure | address or postcode in copy | mitigate | Explicit prohibition in the module headers, an acceptance check for postcode-shaped strings and phone digits across all five files, and plan 02-02's `SC-2d` scanning built HTML on every page (task 2) |
| T-02-33 | Tampering | a nav href to a route that does not exist | mitigate | Acceptance criterion enumerates the 17 routes and fails on any other href; delta 6 enforces it against the real manifest once the layout renders the nav (task 2) |
| T-02-34 | Repudiation | `areaServed` silently dropping out of the sitewide JSON-LD | mitigate | The four town names become a named export with a comment naming the failure mode, and plan 02-13 threads them through `Footer`; without this the structured data degrades with no lock to catch it (task 1) |
| T-02-35 | Tampering | a second `<h1>` from a prose block | mitigate | `renderBlocks` does not map an `h1` type at all, asserted directly in task 2's acceptance criteria |
| T-02-36 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase; `jsconfig.json` is configuration, not a dependency |
</threat_model>

<verification>
- `npm run verify` green end to end; the built output is byte-comparable to the previous wave because no route consumes these modules yet.
- `git diff web/package.json web/app/layout.jsx` is empty.
</verification>

<success_criteria>
- `@/content/...` resolves from a route file and the build succeeds.
- The rating, brand, served towns, hours, area forms, title form and description form are declared exactly once.
- Nav plus footer plus legal reach all 17 Phase 2 routes and reference no other href.
- The Services nav item carries children and no href of its own.
- `renderBlocks` maps `p`/`h2`/`h3`/`ul`, drops unknown types, and can never emit an `<h1>`.
- No address, postcode or phone number appears in any of the five modules.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-06-SUMMARY.md` when done
</output>
