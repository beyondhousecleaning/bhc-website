---
phase: 02-component-library-completion-core-templates
plan: 13
type: execute
wave: 6
depends_on: ["02-08", "02-09", "02-10", "02-11", "02-12"]
files_modified:
  - web/app/layout.jsx
  - web/app/not-found.jsx
  - web/scripts/check-html-locks.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every page carries exactly one header, one primary nav, one main, one footer and one skip link"
    - "The skip link moves focus to the main landmark"
    - "The 404 page is a real template with a real h1, a header and a footer"
    - "The lock suite fails if any of the 18 expected routes stops prerendering"
  artifacts:
    - path: "web/app/layout.jsx"
      provides: "the composed root layout — SkipLink, Header, main, Footer, StickyCallBar"
    - path: "web/app/not-found.jsx"
      provides: "a real 404 template, closing WR-07's finding that nothing had ever inspected _not-found.html"
  key_links:
    - from: "web/app/layout.jsx"
      to: "web/content/nav.js"
      via: "Header nav tree and Footer columns"
      pattern: "@/content/nav"
    - from: "web/scripts/check-html-locks.mjs"
      to: "the 18 prerendered app routes"
      via: "EXPECTED_APP_ROUTES raised from 3 to the full set"
      pattern: "EXPECTED_APP_ROUTES"
---

<objective>
Compose the root layout from `SkipLink`, `Header`, `Footer` and `StickyCallBar`; author the real
404 template; and land the three lock changes that only become assertable once all 18 routes and all
16 components exist — the `<header>` and `<nav aria-label="Primary">` landmark clauses, the
`EXPECTED_APP_ROUTES` raise, and delta 8's `data-bhc-photo-state="pending"` assertion.

Purpose: this is the wave the phase has been sequenced towards. `Header` and `Footer` link to every
one of the seventeen routes, so composing them earlier would have kept delta 6 red. Every route now
exists, so the navigation goes in with the gate green, and the floors that have been deliberately
low since wave 1 can be raised to their real values in the same change that makes them true.

Output: a composed layout, a real 404 page, and the final `check-html-locks.mjs` tightening.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-10-SUMMARY.md
@.planning/phases/02-component-library-completion-core-templates/02-12-SUMMARY.md

<measured_framework_page_facts>
<!-- Measured on this repo's build during planning. -->

/_not-found  BEFORE this plan: h1 text "404", 1 ld+json tag, 1 <footer>, 2 robots metas,
             lang="en-GB". It DOES render RootLayout, so from this plan it also gains a header,
             a skip link and a sticky call bar.
             AFTER this plan: h1 text "We Couldn't Find That Page", still 1 ld+json tag
             (no Breadcrumbs on the 404), so its PAGE_EXPECTATIONS entry becomes
             { h1: "We Couldn't Find That Page", hasBreadcrumbs: false, ldJsonBlocks: 1 }.

/_global-error  h1 text "This page couldn't load" (curly apostrophe), 0 ld+json, 0 <footer>,
                0 robots metas, no lang attribute. It renders Next's OWN error document, NOT
                RootLayout, and is unaffected by anything in this plan. It stays in
                FRAMEWORK_ONLY and out of PAGE_EXPECTATIONS. Do not assert its h1 TEXT — that
                string belongs to the framework and will change on a Next upgrade; only the
                one-<h1> COUNT applies to it.

The 18 expected app routes:
  /
  /services/{deep-cleaning, standard-home-cleaning, move-in-cleaning, move-out-cleaning,
             short-term-rental-cleaning, post-construction-cleaning}
  /about-us /checklist /contact-us /customer-login /customer-service-agreement
  /get-a-quote /gift-cards /privacy-policy /terms-of-service /work-with-us
  /_not-found
</measured_framework_page_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the root layout</name>
  <files>web/app/layout.jsx</files>
  <read_first>
    - web/app/layout.jsx — all of it as amended by plan 02-02. Three things must survive unchanged: the two CSS imports (this is the ONLY file in the app that imports CSS — a route-level import produces a second built stylesheet and destabilises SC-1b); the `metadataBase` and `robots: { index: false, follow: false }` metadata block (D-15); and the "no `phone` prop, ever" rule on `NAPFooter`.
    - design-system/src/components/SkipLink/SkipLink.jsx, Header.jsx, Footer.jsx, StickyCallBar.jsx and their `.d.ts` files — the exact prop names, from plan 02-10.
    - web/content/nav.js — `NAV`, `FOOTER_COLUMNS` and `LEGAL`.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.1 — the body shape and the per-page invariants.
    - .planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md §3.9 — the three things that must survive the rewrite, and the new body shape.
  </read_first>
  <action>
Rewrite the `<body>` of `web/app/layout.jsx` to exactly this shape:

`<SkipLink />` first — it must be the first element inside `<body>`, before `Header`, or it is not
the first focusable element and WCAG 2.4.1 is not satisfied. Then
`<Header nav={NAV} cta={{ label: 'Get a Free Quote', href: '/get-a-quote' }} />`, then the existing
`<main id="main" tabIndex={-1}>{children}</main>` (plan 02-02 added it — keep it and keep the
`tabIndex={-1}`, which is what actually moves focus in Safari and Chrome rather than merely scrolling
the viewport), then `<Footer columns={FOOTER_COLUMNS} legal={LEGAL} />`, then `<StickyCallBar />`.

Pass NO `phone` prop to `Header`, `StickyCallBar`, `Footer` or `NAPFooter`. All four default from
`design-system/src/phone.js`, and that single-sourcing is what makes REQ-nap-consistency structural
rather than cosmetic. Keep the existing comment recording the rule.

`Footer` emits no `<footer>` element — it composes `NAPFooter`, which owns the landmark. Do not add
a second `NAPFooter`; `Footer` renders the one that already exists in this layout, so the existing
`<NAPFooter areaServed={…} hours={…} />` call moves INSIDE `Footer` or is replaced by `Footer`'s own
composition. Whichever shape plan 02-10 chose, the result must be exactly one `<footer>` per page —
verify it in the built HTML before considering this task done.

Do not add any CSS import. Do not touch `metadataBase` or the `robots` block.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const m=JSON.parse(fs.readFileSync('web/.next/prerender-manifest.json','utf8'));let bad=0;for(const r of Object.keys(m.routes)){if(r==='/_global-error')continue;const f=r==='/'?'index.html':r.slice(1)+'.html';const h=fs.readFileSync('web/.next/server/app/'+f,'utf8');const c=re=>(h.match(re)||[]).length;for(const [n,re,want] of [['header',/<header/g,1],['nav primary',/<nav aria-label=\"Primary\"/g,1],['main',/<main id=\"main\"/g,1],['footer',/<footer/g,1],['skiplink',/class=\"bhc-skip-link\"/g,1],['h1',/<h1/g,1]]){const got=c(re);if(got!==want){console.error(r,n,'expected',want,'got',got);bad=1}}}process.exit(bad)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Every app page (all routes except `/_global-error`) has exactly one `<header`, one `<nav aria-label="Primary"`, one `<main id="main"`, one `<footer`, one `class="bhc-skip-link"` and one `<h1` — proven by the verify command above.
    - The skip link is the first element inside `<body>`: `node -e "const fs=require('fs');const h=fs.readFileSync('web/.next/server/app/index.html','utf8');const b=h.indexOf('<body');const after=h.slice(h.indexOf('>',b)+1,h.indexOf('>',b)+400);if(!/^\s*<a[^>]*class=\"bhc-skip-link\"/.test(after)){console.error('skip link is not first in body:',after.slice(0,120));process.exit(1)}console.log('ok')"` prints `ok`.
    - `<main>` carries `tabindex="-1"` on every app page.
    - `grep -c "import '@bhc/design-system/styles.css'" web/app/layout.jsx` returns 1 and no other file under `web/app` imports CSS: `grep -rc "\.css'" web/app | grep -v "layout.jsx:1" | grep -v ":0" || true` produces no output.
    - `grep -rc "447861936533" web/app` reports 0 for every file, and no `phone` prop is passed anywhere: `grep -rc "phone=" web/app` reports 0 for every file.
    - `robots: { index: false, follow: false }` and `metadataBase` are still present in `layout.jsx`.
    - Exactly one `.css` file exists under `.next/static` and every app page has exactly one `<link rel="stylesheet"` — plan 02-02's SC-1b Pitfall-7 guard passes.
  </acceptance_criteria>
  <done>Every page carries the five landmarks exactly once, the skip link is the first focusable element and targets a focusable `<main>`, and the number is still passed by no one.</done>
</task>

<task type="auto">
  <name>Task 2: The real 404 template</name>
  <files>web/app/not-found.jsx</files>
  <read_first>
    - web/app/page.jsx — as composed by plan 02-11; the shape analog, including the convention of not naming the client directive in prose.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.4 last bullet — `app/not-found.jsx` gets a real `<h1>`, the §5 error copy, and three links: `/services/deep-cleaning`, `/`, `/get-a-quote`. All three now exist.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 — the error-state copy: heading **We couldn't find that page**, body *That link may be out of date. Try our cleaning services, the areas we cover, or get a free quote in under a minute.* The `<h1>` deck gives the heading as `We Couldn't Find That Page`; use the deck's capitalisation for the `<h1>` and adapt the body so it does not promise "the areas we cover", which is a Phase 3 route.
    - .planning/phases/01-platform-foundation-design-system-integration/01-REVIEW.md § WR-07 — nothing had ever inspected `_not-found.html`.
    - This plan's `<measured_framework_page_facts>` block.
  </read_first>
  <action>
Create `web/app/not-found.jsx`. It renders `RootLayout`, so from this plan the 404 page also carries
a header, a footer, a skip link, a sticky call bar and three `tel:` links — which is exactly why WR-07
mattered and why the harness now globs every page.

Composition: no `Breadcrumbs` (a 404 has no position in the hierarchy to describe), then `Hero
align="centered"` with `heading="We Couldn't Find That Page"` and `rating={RATING}`, then a
`SectionBand` wrapping `Prose` with the §5 error body, then three links.

Adapt the §5 body: the shipped copy offers "the areas we cover", which is `/locations` and does not
exist until Phase 3. Offer the three destinations §9.4 names instead — `/services/deep-cleaning`,
`/` and `/get-a-quote` — and keep the tone and the first sentence intact. Record the adaptation in a
comment naming Phase 3 as the plan that can restore the areas link.

No `emitSchema`. No `CTABand` (the three links are the recovery path and a navy band on a 404 is
noise). No phone number written in `web/app`.

Then update the `/_not-found` entry in `web/scripts/check-html-locks.mjs`'s `PAGE_EXPECTATIONS` from
Next's built-in `404` to `{ h1: "We Couldn't Find That Page", hasBreadcrumbs: false, ldJsonBlocks: 1 }`,
and delete the comment naming this plan. This is one of three edits to that file in this plan;
task 3 makes the other two.
  </action>
  <verify>
    <automated>npm run build && npm run check:html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `npm run check:html` exits 0.
    - `/_not-found` has the real heading: `node -e "const fs=require('fs');const h=fs.readFileSync('web/.next/server/app/_not-found.html','utf8');const m=h.match(/<h1[^>]*>([\s\S]*?)<\/h1>/);const t=m&&m[1].replace(/<[^>]*>/g,'').trim();if(t!==\"We Couldn't Find That Page\"){console.error('got',JSON.stringify(t));process.exit(1)}console.log('ok')"` prints `ok`.
    - `_not-found.html` has exactly one `<header`, one `<nav aria-label="Primary"`, one `<main id="main"`, one `<footer`, one `<h1`, exactly one `<script type="application/ld+json"` block, and zero `class="…bhc-breadcrumbs__list"`.
    - All three recovery links resolve to prerendered routes — delta 6 passes over `_not-found.html`.
    - `grep -c "bhc-section--navy" web/.next/server/app/_not-found.html` returns 0.
    - `grep -rc "emitSchema" web/app` reports 0 for every file.
    - `_global-error.html` is unchanged: still exactly one `<h1`, zero `<footer`, zero `tel:` and zero robots metas, and it is still excluded from every landmark, NAP, robots and JSON-LD assertion.
  </acceptance_criteria>
  <done>The 404 page is a real template with a real heading, the site chrome, one JSON-LD block and three working recovery links — and the expectation table matches it.</done>
</task>

<task type="auto">
  <name>Task 3: Raise EXPECTED_APP_ROUTES and land delta 8</name>
  <files>web/scripts/check-html-locks.mjs</files>
  <read_first>
    - web/scripts/check-html-locks.mjs — the file as written by plan 02-02 and amended by plans 02-08 and 02-13 task 2. Specifically: `EXPECTED_APP_ROUTES`, seeded with three routes and carrying a comment naming THIS plan as the one that raises it; the delta 5 landmark test, carrying a comment saying the `<header>` and `<nav aria-label="Primary">` clauses arrive with this plan; `INTERLINK_LOCK_ACTIVE`, which stays `false`; and `PAGE_EXPECTATIONS`, now complete and accurate.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §12 deltas 1, 5 and 8.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § Pitfall 9 — a route that silently opts into dynamic rendering disappears from `prerender-manifest.json` and therefore from every assertion, reporting green. The route-count floor is the guard against exactly that, which is why delta 1's "assert the page count" clause is load-bearing rather than decorative.
    - This plan's `<measured_framework_page_facts>` block — the 18 expected app routes.
  </read_first>
  <action>
Three changes, all in this one file.

1. **Raise `EXPECTED_APP_ROUTES`** from the three wave-1 seeds to all 18 app routes listed in the
   `<measured_framework_page_facts>` block. Delete the comment naming this plan. The existing
   two-directional assertion then does the work: every member must be present in the manifest, and
   `APP_PAGES.length >= EXPECTED_APP_ROUTES.size`. Add a comment recording what this guards — a
   route that silently opts into dynamic rendering vanishes from the manifest and from every
   assertion, so without this floor the suite would go green by having less to check.

2. **Add the two remaining delta 5 clauses** to the landmark test, now that `Header` is composed:
   exactly one `<header` and exactly one `<nav aria-label="Primary"` per `APP_PAGE`. Add a third
   clause while there: exactly one `class="bhc-skip-link"` per `APP_PAGE`, since the skip link is
   the first focusable element and its absence is silent. Delete the comment saying these clauses
   arrive later.

3. **Add delta 8** — `data-bhc-photo-state="pending"` renders on every page that uses
   `BeforeAfterSlider` without pairs. In Phase 2 that is `/` and the six service pages. Drive it
   from a named constant, e.g. `PHOTO_PLACEHOLDER_ROUTES`, holding those seven routes, and assert
   both directions: each listed route has exactly one `data-bhc-photo-state="pending"`, and no
   unlisted app route has any `data-bhc-photo-state` attribute at all. The negative half is what
   keeps it meaningful and it is what Phase 4 inverts as real photo pairs land. Comment the Phase 4
   hand-off.

Change nothing else. `INTERLINK_LOCK_ACTIVE` stays `false` with its self-restoring inverse
assertion — Phase 3 flips it. Do not raise `MIN_TESTS` in `run-locks.mjs`; that floor covers the
design-system suite, this file is not counted by it, and plan 02-03 set it to its exact value.
  </action>
  <verify>
    <automated>npm run build && npm run check:html && npm run check:budget</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:html` exits 0.
    - `EXPECTED_APP_ROUTES` holds 18 entries: `node -e "const s=require('fs').readFileSync('web/scripts/check-html-locks.mjs','utf8');const m=s.match(/EXPECTED_APP_ROUTES\s*=\s*new Set\(\[[\s\S]*?\]\)/);if(!m){console.error('constant not found');process.exit(1)}const n=(m[0].match(/'\//g)||[]).length;if(n!==18){console.error('expected 18 routes, found',n);process.exit(1)}console.log('18')"` prints `18`.
    - `grep -c "PHOTO_PLACEHOLDER_ROUTES" web/scripts/check-html-locks.mjs` returns at least 1, and the constant holds exactly 7 routes.
    - `grep -c "INTERLINK_LOCK_ACTIVE = false" web/scripts/check-html-locks.mjs` returns 1 — the gate was not silently flipped or deleted.
    - Negative proof (run, record, revert, rebuild): temporarily renaming `web/app/gift-cards/` so the route stops prerendering makes `npm run check:html` exit non-zero naming the missing route.
    - Negative proof (run, record, revert, rebuild): temporarily removing `<SkipLink />` from the layout makes `npm run check:html` exit non-zero.
    - Negative proof (run, record, revert, rebuild): temporarily passing a `pairs` array to the home page's `BeforeAfterSlider` makes `npm run check:html` exit non-zero on delta 8 — proving the placeholder assertion is not vacuous.
    - `npm run check:budget` exits 0; record the worst route and both figures in the SUMMARY.
  </acceptance_criteria>
  <done>The route floor, the landmark clauses and the photo-placeholder assertion all reflect the shipped site, and each has been proven to fail when the thing it guards is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `nav.js` -> root layout -> all 18 pages | The layout is the one place a data defect has a whole-site blast radius |
| build output -> CI verdict | The route floor is the only thing standing between a silently-dropped route and a green run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-71 | Repudiation | a route silently dropping out of prerendering | mitigate | `EXPECTED_APP_ROUTES` raised to all 18, asserted in both directions, with a negative proof that removing a route turns the run red (task 3) |
| T-02-72 | Spoofing | a second `<header>` or `<footer>` landmark | mitigate | Delta 5 now asserts exactly one of each per app page across all 18 (tasks 1 and 3) |
| T-02-73 | Denial of Service | a malformed `nav.js` entry 500-ing every route | mitigate | `Header` and `Footer` carry the `NAPFooter` guard idiom from plan 02-10; the layout passes validated data authored in plan 02-06 |
| T-02-74 | Information disclosure | the 404 page leaking build or path detail | mitigate | `app/not-found.jsx` renders authored copy only — no request path, no stack, no error object (task 2) |
| T-02-75 | Information disclosure | pre-cutover indexing | mitigate | `robots: { index: false, follow: false }` and `metadataBase` survive the layout rewrite untouched, and the D-15 assertion now runs over all 18 app pages |
| T-02-76 | Tampering | a second built stylesheet | mitigate | `layout.jsx` remains the only file in the app importing CSS; plan 02-02's SC-1b Pitfall-7 guard asserts one `.css` under `.next/static` and one stylesheet link per page (task 1) |
| T-02-77 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run verify` green end to end across all 18 app pages.
- All three negative proofs in task 3 reproduce a red run and are reverted, with the failing assertion names recorded in the SUMMARY.
- `git diff web/package.json design-system/package.json` is empty.
</verification>

<success_criteria>
- Every app page has exactly one header, one primary nav, one main with `tabindex="-1"`, one footer, one skip link and one `<h1>`.
- The skip link is the first element inside `<body>` on every page.
- `/_not-found` is a real template with the UI-SPEC heading and three working recovery links.
- `EXPECTED_APP_ROUTES` holds all 18 routes and fails when one stops prerendering.
- Delta 8 asserts the photo placeholder on exactly the seven pages that use `BeforeAfterSlider`, and its absence everywhere else.
- `INTERLINK_LOCK_ACTIVE` is still `false` with its self-restoring inverse assertion intact.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-13-SUMMARY.md` when done
</output>
