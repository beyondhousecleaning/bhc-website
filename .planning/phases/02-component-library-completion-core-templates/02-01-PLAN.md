---
phase: 02-component-library-completion-core-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/src/phone.js
  - design-system/src/components/NAPFooter/formatPhone.js
  - design-system/src/components/NAPFooter/NAPFooter.jsx
  - design-system/src/components/NAPFooter/NAPFooter.d.ts
  - design-system/src/components/NAPFooter/NAPFooter.prompt.md
  - design-system/src/components/Breadcrumbs/Breadcrumbs.jsx
  - design-system/src/components/Breadcrumbs/Breadcrumbs.html
  - design-system/src/components/Breadcrumbs/Breadcrumbs.d.ts
  - design-system/src/components/Breadcrumbs/Breadcrumbs.prompt.md
  - design-system/src/components/RatingBadge/RatingBadge.jsx
  - design-system/src/components/RatingBadge/RatingBadge.d.ts
  - design-system/src/components/Button/Button.jsx
  - design-system/src/components/Button/Button.d.ts
  - design-system/src/components/InterlinkBlock/InterlinkBlock.jsx
  - design-system/src/components/Hero/Hero.prompt.md
  - design-system/styles.css
  - docs/design/design-system.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The canonical phone number +447861936533 exists in exactly one executable location in the package"
    - "A malformed phone value fails the build instead of rendering an empty tel: link"
    - "A breadcrumb trail marks exactly one crumb as the current page, whatever its length"
    - "ReviewCard can reuse the RatingBadge star glyph without duplicating its path data"
  artifacts:
    - path: "design-system/src/phone.js"
      provides: "CANONICAL_PHONE, package-internal, not re-exported from index.js"
      contains: "CANONICAL_PHONE"
    - path: "design-system/src/components/NAPFooter/formatPhone.js"
      provides: "toDial that throws on an invalid national length"
      contains: "throw"
  key_links:
    - from: "design-system/src/components/NAPFooter/NAPFooter.jsx"
      to: "design-system/src/phone.js"
      via: "import CANONICAL_PHONE for the phone default parameter"
      pattern: "import \\{ CANONICAL_PHONE \\} from '\\.\\./\\.\\./phone\\.js'"
    - from: "design-system/src/components/RatingBadge/RatingBadge.jsx"
      to: "design-system/src/components/ReviewCard/ReviewCard.jsx"
      via: "named export of the Star glyph"
      pattern: "export function Star"
---

<objective>
Harden the six shipped design-system components so the sixteen new ones can be authored against a
correct foundation, and close the four Phase-1 review findings that land inside this phase's blast
radius (WR-10, WR-12, plus the §13-R breadcrumb rule and the §7.0 phone single-sourcing).

Purpose: Phase 2 takes the number of `tel:` links per page from 1 to 3, the number of breadcrumb
trails from 1 to 17, and the number of `<h2>` blocks from 1 to 16. Every defect fixed here is a
defect that would otherwise be replicated sixteen or seventeen times.

Output: `src/phone.js`; a `toDial` that fails loudly; a `Breadcrumbs` that never double-marks the
current page; an exported `Star`; `as?: ElementType` on `Button`; three `styles.css` corrections;
and the package-doc heading remediation UI-SPEC §13-N commits to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/01-platform-foundation-design-system-integration/01-REVIEW.md

<interfaces>
<!-- Contracts this plan changes. Downstream plans code against the AFTER state. -->

design-system/src/phone.js (NEW — package-internal, NOT exported from src/index.js):
  export const CANONICAL_PHONE: string   // '+447861936533'

design-system/src/components/NAPFooter/formatPhone.js (AFTER this plan):
  export function toDial(value: string): string        // THROWS on invalid national length
  export function formatPhone(value: string): string   // inherits the throw via toDial

design-system/src/components/RatingBadge/RatingBadge.jsx (AFTER this plan):
  export function Star({ filled }: { filled: boolean }): JSX.Element   // newly exported
  export function RatingBadge(props): JSX.Element
  export default RatingBadge

design-system/src/components/Button/Button.jsx (AFTER this plan):
  export function Button({ children, href, variant, size, as, ... }): JSX.Element
  // `as` defaults to 'a' and applies only to the href branch

Breadcrumbs contract (AFTER this plan):
  - a non-last crumb with no `href` renders a plain <span> with NO aria-current
  - only the last crumb ever carries aria-current="page"
  - <li> carries no inline style; layout moves to .bhc-breadcrumbs__list
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-source the canonical phone number and make toDial fail loudly</name>
  <files>design-system/src/phone.js, design-system/src/components/NAPFooter/formatPhone.js, design-system/src/components/NAPFooter/NAPFooter.jsx, design-system/src/components/NAPFooter/NAPFooter.d.ts, design-system/src/components/NAPFooter/NAPFooter.prompt.md</files>
  <read_first>
    - design-system/src/jsonLd.js — the exact template for a pure, package-internal `.js` module that is deliberately NOT re-exported from `src/index.js`. Copy its header-comment structure (states both reasons it is not a `.jsx`).
    - design-system/src/components/NAPFooter/formatPhone.js — read all 68 lines. `toDial` currently returns `+44` for `''` and `undefined`; `formatPhone` falls back to the dial string on any non-UK-length national part.
    - design-system/src/components/NAPFooter/NAPFooter.jsx — line 34 holds `phone = '+447861936533',`; line 33 is its JSDoc. Lines 46-47 hold the `formatPhone`/`toDial` derivation. Line 82 holds the unguarded `mapsUrl` href.
    - design-system/src/components/NAPFooter/NAPFooter.d.ts — the JSDoc at line 29 mentions the number; it is documentation, not executable code, and stays.
    - .planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md §3.1 — the phone single-sourcing pattern and the import-path rules.
  </read_first>
  <action>
Create `design-system/src/phone.js` exporting `const CANONICAL_PHONE = '+447861936533'`. Give it a
header block comment in the `jsonLd.js` style stating: it is package-internal plumbing, deliberately
NOT re-exported from `src/index.js`; and that four components (`NAPFooter`, `Header`,
`StickyCallBar`, `QuoteFormEntry`) default from it, so three per-component defaults would be three
places the number can be wrong. Do not add any other export to this file.

In `NAPFooter.jsx`, add `import { CANONICAL_PHONE } from '../../phone.js';` alongside the existing
`import { formatPhone, toDial } from './formatPhone.js';`, and change the default parameter at
line 34 from the string literal to `phone = CANONICAL_PHONE,`. The JSDoc on line 33 stays. Delete
the literal — do not leave it in a comment.

In the same file, close WR-10: derive `const safeMapsUrl = /^https?:\/\//i.test(mapsUrl ?? '') ? mapsUrl : null;`
and render the "Find us on Google" block only when `safeMapsUrl` is truthy, with
`href={safeMapsUrl} target="_blank" rel="noopener noreferrer"`. `rel="noopener"` without
`target="_blank"` is inert, which is why both change together.

In `formatPhone.js`, make `toDial` throw a `TypeError` rather than degrade. The throw fires when
the value is not a string, is empty after stripping non-digits, or when the resolved national part
is not exactly `UK_NATIONAL_LEN` digits. The message must name the offending input, e.g.
`toDial: "call us" is not a dialable UK number`. Keep the existing explicit-non-UK-number branch
working — `toDial` must still not fabricate `+44` onto a number that already carries a different
country code, and the existing lock test at `locks.test.js:75` must stay green. `formatPhone` needs
no new guard: it calls `toDial` first and inherits the throw. Update the doc comment above
`formatPhone` — it currently promises a fallback that no longer exists.

Update `NAPFooter.d.ts` and `NAPFooter.prompt.md` to describe the new behaviour: the `phone` prop is
optional and defaults to the package's single canonical constant; `toDial`/`formatPhone` throw
rather than degrade; `mapsUrl` is scheme-validated and dropped when it is not `http(s)`.
Add a `## Don't` entry: don't restate the number in a component default.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - `design-system/src/phone.js` exists and `grep -c "CANONICAL_PHONE" design-system/src/phone.js` returns at least 1.
    - `grep -c "CANONICAL_PHONE" design-system/src/index.js` returns 0 — the constant is package-internal.
    - Executable-code scan returns exactly one hit: `node -e "const{readFileSync}=require('fs');const f=['design-system/src/phone.js','design-system/src/components/NAPFooter/NAPFooter.jsx','design-system/src/components/NAPFooter/formatPhone.js'];let n=0;for(const p of f){const s=readFileSync(p,'utf8').replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');n+=(s.match(/447861936533/g)||[]).length}console.log(n)"` prints `1`.
    - `grep -n "phone = CANONICAL_PHONE" design-system/src/components/NAPFooter/NAPFooter.jsx` returns a match.
    - `node --input-type=module -e "import {toDial} from './design-system/src/components/NAPFooter/formatPhone.js'; for (const v of ['', undefined, '+44', 'call us', '+4478619365333', '+4478619365']) { let threw=false; try { toDial(v) } catch { threw=true } if(!threw) { console.error('did not throw for', JSON.stringify(v)); process.exit(1) } } console.log('all six throw')"` prints `all six throw`.
    - `node --input-type=module -e "import {toDial,formatPhone} from './design-system/src/components/NAPFooter/formatPhone.js'; console.log(toDial('+447861936533'), '|', formatPhone('+447861936533'))"` prints `+447861936533 | +44 7861 936533`.
    - `grep -n 'rel="noopener noreferrer"' design-system/src/components/NAPFooter/NAPFooter.jsx` returns a match, and `grep -c 'rel="noopener">' design-system/src/components/NAPFooter/NAPFooter.jsx` returns 0.
    - `npm run test:locks` exits 0 with 14 passing tests (no test count change in this task).
  </acceptance_criteria>
  <done>The number exists once in executable package code, a malformed phone value throws at build time, and `mapsUrl` cannot carry a non-http scheme into an href.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Breadcrumbs for 17-page scale and correct three styles.css deviations</name>
  <files>design-system/src/components/Breadcrumbs/Breadcrumbs.jsx, design-system/src/components/Breadcrumbs/Breadcrumbs.html, design-system/src/components/Breadcrumbs/Breadcrumbs.d.ts, design-system/src/components/Breadcrumbs/Breadcrumbs.prompt.md, design-system/src/components/InterlinkBlock/InterlinkBlock.jsx, design-system/styles.css</files>
  <read_first>
    - design-system/src/components/Breadcrumbs/Breadcrumbs.jsx — read all of it. Line 43 is `<li key={item.href || item.label} style={{ display: 'contents' }}>`; the crumb branch is `isLast || !item.href ? <span … aria-current="page">`; line 29 builds the JSON-LD `item` as `` `${siteUrl}${item.href}` ``.
    - design-system/src/components/Breadcrumbs/Breadcrumbs.html — line 33 renders `<a href="/services">Services</a>`, a crumb the §13-R rule removes; every `<li>` carries `style="display:contents"`.
    - .planning/phases/01-platform-foundation-design-system-integration/01-REVIEW.md § WR-12 — the three defects and their fixes.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.3 and §13-R — the two-crumb service trail and the aria-current rule.
    - design-system/styles.css lines 219-250 — the InterlinkBlock block, including `.bhc-interlink__heading { font-size: var(--bhc-text-xl); }` at line 224 and the `.bhc-interlink__link` card treatment.
    - design-system/src/components/InterlinkBlock/InterlinkBlock.jsx line 48 — the intro paragraph borrows `.bhc-hero__lead`.
    - .planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md §6 findings 3, 4 and 10.
  </read_first>
  <action>
**Breadcrumbs.jsx** — three changes, all from WR-12 plus the §13-R rule:

1. Replace the current-crumb condition. `aria-current="page"` is applied on `isLast` ONLY. A
   non-last crumb with no `href` still renders as a plain `<span className="bhc-breadcrumbs__current">`
   but without `aria-current`. A non-last crumb with an `href` renders as `<a>` as today. This is the
   rule Phase 3 inherits at ~336-trail scale, so state it in a comment above the branch.
2. Remove `style={{ display: 'contents' }}` from the `<li>` entirely and change the key to
   `` key={`${i}-${item.href ?? item.label}`} ``.
3. Build the JSON-LD `item` value with `new URL(item.href, siteUrl).toString()` instead of string
   concatenation, so a missing leading slash or a trailing slash on `siteUrl` cannot produce an
   invalid URL.

**styles.css** — three corrections, each a named deviation from UI-SPEC:

1. Add `.bhc-breadcrumbs__list { display: flex; flex-wrap: wrap; align-items: center; gap: var(--bhc-space-2); list-style: none; margin: 0; padding: 0; }` and `.bhc-breadcrumbs__list li { display: flex; align-items: center; gap: var(--bhc-space-2); }` so the layout the removed `display: contents` was providing lives in CSS with the list semantics intact. Put it inside the existing Breadcrumbs section, and extend that section's banner comment with the WR-12 reason (Chrome and Safari drop `display: contents` elements from the accessibility tree, destroying the `ol`/`li` semantics that make a breadcrumb announceable).
2. Change line 224 `.bhc-interlink__heading` from `var(--bhc-text-xl)` to `var(--bhc-text-2xl)`. UI-SPEC §3's binding table assigns `--bhc-text-2xl` to every `<h2>`, and this is the only shipped `<h2>` that disagrees. Sixteen new `<h2>` blocks are authored in later waves and must not inherit the deviation. Record the decision in the section banner.
3. Add a new `.bhc-section__intro { font-size: var(--bhc-text-lg); line-height: 1.35; color: var(--bhc-ink-muted); margin-bottom: var(--bhc-space-6); max-width: var(--bhc-container-prose); }` block, and change `InterlinkBlock.jsx` line 48 to use `className="bhc-section__intro"` instead of `className="bhc-hero__lead"`. `SectionBand`, `CTABand` and `ReviewRail` all take an intro/lead in later waves; the cross-block borrow must not propagate into four more components.

**Breadcrumbs.html** — remove the `<a href="/services">Services</a>` crumb so the preview renders
the two-crumb `Home / Deep Cleaning` trail, and remove `style="display:contents"` from every `<li>`.
A remediated `.jsx` with an unremediated preview is exactly the design-to-build drift the four-file
shape exists to prevent.

**Breadcrumbs.d.ts / Breadcrumbs.prompt.md** — document the aria-current rule in the props JSDoc,
and rewrite the `## Trails per template` table so the Service row reads `Home / Deep Cleaning`
(two crumbs). Add a note that Phase 3 reinstates the middle crumb once a `/services` index exists,
and a `## Don't` entry forbidding `aria-current` on any crumb but the last.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "display: 'contents'" design-system/src/components/Breadcrumbs/Breadcrumbs.jsx` returns 0.
    - `grep -c "display:contents" design-system/src/components/Breadcrumbs/Breadcrumbs.html` returns 0.
    - `grep -c 'href="/services"' design-system/src/components/Breadcrumbs/Breadcrumbs.html` returns 0.
    - `grep -n "new URL(" design-system/src/components/Breadcrumbs/Breadcrumbs.jsx` returns a match.
    - `grep -n "bhc-interlink__heading" -A2 design-system/styles.css | grep -c "bhc-text-2xl"` returns 1.
    - `grep -c "bhc-hero__lead" design-system/src/components/InterlinkBlock/InterlinkBlock.jsx` returns 0, and `grep -c "bhc-section__intro" design-system/styles.css` returns at least 1.
    - Behaviour check — a three-crumb trail with an href-less middle crumb yields exactly one `aria-current`: `node --input-type=module -e "import {renderToStaticMarkup} from 'react-dom/server';import {Breadcrumbs} from './design-system/src/components/Breadcrumbs/Breadcrumbs.jsx';const h=renderToStaticMarkup(Breadcrumbs({items:[{label:'Home',href:'/'},{label:'Services'},{label:'Deep Cleaning'}]}));const n=(h.match(/aria-current=\"page\"/g)||[]).length;if(n!==1){console.error('expected 1 aria-current, got',n);process.exit(1)}console.log('ok')"` prints `ok`.
    - `npm run test:locks` exits 0 with 14 passing tests.
  </acceptance_criteria>
  <done>Breadcrumbs marks exactly one crumb current at any trail length, keeps its list semantics, emits normalised JSON-LD URLs, and its preview agrees with its implementation. The three token deviations are corrected before sixteen components inherit them.</done>
</task>

<task type="auto">
  <name>Task 3: Export the Star glyph, add `as` to Button, and remediate the heading docs</name>
  <files>design-system/src/components/RatingBadge/RatingBadge.jsx, design-system/src/components/RatingBadge/RatingBadge.d.ts, design-system/src/components/Button/Button.jsx, design-system/src/components/Button/Button.d.ts, design-system/src/components/Hero/Hero.prompt.md, docs/design/design-system.md</files>
  <read_first>
    - design-system/src/components/RatingBadge/RatingBadge.jsx — `Star` is declared at line 18 as a module-local function and is not exported. Read the whole file to see how the `role="img"` wrapper and the `aria-hidden` glyphs pair up.
    - design-system/src/components/Button/Button.jsx — lines 12-37. `VARIANTS`/`SIZES` lookup maps with a fallback, `...rest` spread at line 28, and the `href ? <a> : <button>` branch.
    - design-system/src/components/Button/Button.d.ts — the whole file (17 lines); it is the type-only-import precedent (`import type { ReactNode } from 'react'`).
    - design-system/src/components/Hero/Hero.prompt.md — the `## Heading per template` table (four rows) that UI-SPEC §13-N commits to replacing in full.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 — the `<h1>` deck, which supplies the replacement rows.
    - docs/design/design-system.md line 334 — reads `H1 Deep Cleaning Services in Warwickshire & the West Midlands`.
    - .planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md §6 findings 11 and 13.
  </read_first>
  <action>
**RatingBadge.jsx** — change `function Star({ filled })` to `export function Star({ filled })`. Do
not otherwise alter the glyph, its path data, or its `aria-hidden="true" focusable="false"`
attributes. Add a one-line comment above it recording why it is exported: `ReviewCard` renders the
same five-star row (UI-SPEC §7.13), and an unexported glyph would put the same path string in two
files. Add `export declare function Star(props: { filled: boolean }): JSX.Element;` to
`RatingBadge.d.ts` above the props interface.

**Button.jsx** — add `as` to the destructured props with a default of `'a'`, and use it as the
element for the href branch only: the `<button>` branch is unchanged. UI-SPEC §13-J says every
link-bearing component takes `as?: ElementType` defaulting to `'a'`; `Button` is link-bearing and
shipped, so it takes it too. The change is: destructure `as: As = 'a'`, and render `<As href={…} …>`
where the anchor is rendered today. `...rest` already spreads, so nothing else moves. Add the prop
to `Button.d.ts` as `as?: ElementType;` with a JSDoc naming the reason (a router link component may
be substituted without touching sixteen call sites), importing the type with
`import type { ElementType, ReactNode } from 'react';`.

**Hero.prompt.md** — replace all four rows of the `## Heading per template` table with UI-SPEC §5's
deck:

| Template | eyebrow | heading |
| Home | `DBS-checked, insured, local` | `Professional House Cleaning in Warwickshire & the West Midlands` |
| Service | `Top to bottom, once` | `Deep Cleaning in Warwickshire & the West Midlands` |
| Town hub | `Local, DBS-checked cleaners` | `House Cleaning in Warwick` |
| Combo | `Local, DBS-checked cleaners` | `Deep Cleaning in Warwick` |

Add a sentence below the table recording why the Home and Service rows changed: the previous Home
example carried neither a service nor a region, so it failed Lock 1 for its own template, and the
Service example carried the redundant word *Services*.

**docs/design/design-system.md line 334** — drop the word `Services` so the §3.2 Service template
line reads `H1 Deep Cleaning in Warwickshire & the West Midlands`. This is a wording correction
inside a template example, not a change to a token, a lock or a decision — say so in the commit
message.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function Star" design-system/src/components/RatingBadge/RatingBadge.jsx` returns 1.
    - `node --input-type=module -e "import {Star} from './design-system/src/components/RatingBadge/RatingBadge.jsx'; console.log(typeof Star)"` prints `function`.
    - `grep -c "as?: ElementType" design-system/src/components/Button/Button.d.ts` returns 1, and `grep -c "ElementType" design-system/src/components/Button/Button.d.ts` returns at least 1 on an `import type` line.
    - `node --input-type=module -e "import {renderToStaticMarkup} from 'react-dom/server';import {Button} from './design-system/src/components/Button/Button.jsx';const h=renderToStaticMarkup(Button({children:'Get a Free Quote',href:'/get-a-quote'}));if(!h.startsWith('<a')){console.error('default as is not an anchor:',h.slice(0,40));process.exit(1)}console.log('ok')"` prints `ok`.
    - `grep -c "Professional House Cleaning in Warwickshire" design-system/src/components/Hero/Hero.prompt.md` returns 1, and `grep -c "You Can Actually Rely On" design-system/src/components/Hero/Hero.prompt.md` returns 0.
    - `grep -c "Deep Cleaning Services in Warwickshire" docs/design/design-system.md` returns 0.
    - `npm run test:locks` exits 0 with 14 passing tests.
  </acceptance_criteria>
  <done>`Star` is importable by ReviewCard, `Button` honours the §13-J escape hatch, and the three documents that disagreed with UI-SPEC §5's heading deck now agree with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data module -> component prop -> rendered `href` | Phase 3 feeds `mapsUrl`, breadcrumb `href` and phone values from data files; today they are literals |
| component prop -> JSON-LD payload | Breadcrumb labels and URLs are serialised into structured data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `NAPFooter` `mapsUrl` href | mitigate | `/^https?:\/\//i` scheme test before render; drop the whole block when it fails (WR-10, task 1) |
| T-02-02 | Tampering | `NAPFooter` outbound link | mitigate | `rel="noopener noreferrer"` paired with `target="_blank"` (task 1) |
| T-02-03 | Denial of Service | `toDial` on malformed input | mitigate | Throw at build time so a bad value breaks CI, not 410 prerendered `tel:` links (task 1) |
| T-02-04 | Tampering | `Breadcrumbs` JSON-LD `item` URL | mitigate | `new URL(href, siteUrl)` normalisation rejects malformed relative paths instead of concatenating them (task 2) |
| T-02-05 | Information disclosure | new `styles.css` copy | accept | CSS carries no address or postcode; `locks.test.js` Lock 5 already scans `.css` under `src` and stays green |
| T-02-06 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installs zero packages (02-RESEARCH.md Package Legitimacy Audit). Any `npm install` in this plan is a misread and must be rejected |
</threat_model>

<verification>
- `npm run test:locks` green, still 14 tests (this plan adds no lock tests — 02-03 adds them).
- `npm run build` succeeds; `npm run check:html` and `npm run check:budget` unaffected by package-only changes.
- The executable-code phone scan returns exactly 1.
</verification>

<success_criteria>
- The canonical number appears once in executable package code, in `src/phone.js`.
- `toDial` throws for `''`, `undefined`, `'+44'`, `'call us'`, and both off-by-one national lengths.
- A three-crumb trail with an href-less middle crumb renders exactly one `aria-current="page"`.
- `Star` and `as` are both importable/usable by downstream plans.
- `Hero.prompt.md`, `docs/design/design-system.md` and UI-SPEC §5 state the same headings.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-01-SUMMARY.md` when done
</output>
