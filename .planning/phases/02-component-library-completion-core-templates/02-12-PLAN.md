---
phase: 02-component-library-completion-core-templates
plan: 12
type: execute
wave: 5
depends_on: ["02-02", "02-04", "02-05", "02-06", "02-07"]
files_modified:
  - web/content/services.js
  - web/app/services/[service]/page.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Six service pages prerender from one template and one data file"
    - "Each service page carries at least 800 unique body words"
    - "A slug that is not in the data file 404s rather than rendering"
    - "Each service breadcrumb trail is two crumbs and marks exactly one crumb as current"
  artifacts:
    - path: "web/app/services/[service]/page.jsx"
      provides: "the dynamic SSG route — Phase 3's engine rehearsed at 6x instead of 336x"
      contains: "generateStaticParams"
    - path: "web/content/services.js"
      provides: "six records: slug, h1, eyebrow, title, description, prose, faqs"
  key_links:
    - from: "web/app/services/[service]/page.jsx"
      to: "web/content/services.js"
      via: "generateStaticParams and bySlug, both reading the same records"
      pattern: "generateStaticParams"
    - from: "web/content/services.js"
      to: "web/content/nav.js"
      via: "the six slugs must match the nav hrefs exactly"
      pattern: "slug"
---

<objective>
Ship the six service pages as ONE dynamic statically-generated route driven by
`web/content/services.js`, rather than six hand-written directories.

Purpose two ways. It completes ROADMAP Success Criterion 2's service set. And it is Phase 3's engine
at 6x instead of 336x — `/location/[region]/[town]/[service]` is the same pattern with three
segments, so getting `generateStaticParams`, `await params`, `generateMetadata` and
`dynamicParams = false` right on six pages, under CI, is the cheapest possible rehearsal. It also
makes UI-SPEC §13-C true: the D14 canonical-slug rename is deferred to Phase 3 on the grounds that
"the template is data-driven, so Phase 3's rename is a data change, not a template change" — six
hand-written directories would make that sentence false.

Output: one data module carrying six records of roughly 800 to 1,100 words each, and one route file.

**Wave-mate note:** plan 02-11's home page links to all six of these routes in this same wave.
Delta 6 goes green once both plans have landed; run the full verify at the end of the wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-07-SUMMARY.md

<service_template_contract>
<!-- UI-SPEC §9.3, with §4's band ordering. -->

  Breadcrumbs        <- TWO crumbs: Home > {Service}. NOT Home > Services > {Service}.
  Hero               paper-warm, with the rating       <- the ONE <h1>
  Prose              "what's included", >= 800 unique words
  BeforeAfterSlider  paper, no pairs -> placeholder state
  ProcessSteps       paper-tint
  ReviewRail         paper, reviews={[]} -> returns null in Phase 2
  FAQAccordion       paper
  CTABand            navy   <- the one dark band, immediately above the footer

The InterlinkBlock "towns we cover for this service" is PHASE 3. Do not render it — plan 02-02's
SC-4d gate asserts no Phase 2 page renders `bhc-interlink__list`.

Why two crumbs: `/services` 404s and Phase 2 ships no index, so a LINKED middle crumb fails delta 6,
while an href-less middle crumb made TWO elements carry aria-current="page" on all six pages. Plan
02-01 fixed the component so only the last crumb ever carries aria-current; Phase 3 reinstates the
middle crumb when an index exists.

The six routes and their <h1>s, from UI-SPEC §5, already in plan 02-02's PAGE_EXPECTATIONS and
required CHARACTER FOR CHARACTER:

  slug                          eyebrow                     h1
  deep-cleaning                 Top to bottom, once         Deep Cleaning in Warwickshire & the West Midlands
  standard-home-cleaning        Weekly or fortnightly       Regular House Cleaning in Warwickshire & the West Midlands
  move-in-cleaning              Before the boxes arrive     Move-In Cleaning in Warwickshire & the West Midlands
  move-out-cleaning             Get the deposit back        Move-Out Cleaning in Warwickshire & the West Midlands
  short-term-rental-cleaning    Guest-ready between stays   Short-Term Rental Cleaning in Warwickshire & the West Midlands
  post-construction-cleaning    After the builders leave    Post-Construction Cleaning in Warwickshire & the West Midlands

The measured titles from UI-SPEC §5, all <= 60 chars — use these verbatim, do not regenerate them
from a pattern (the previous pattern measured 78 chars against the 60 cap):

  deep-cleaning                 Deep Cleaning in Warwickshire | Beyond House Cleaning       53
  standard-home-cleaning        Regular Cleaning in Warwickshire | Beyond House Cleaning    56
  move-in-cleaning              Move-In Cleaning in Warwickshire | Beyond House Cleaning    56
  move-out-cleaning             Move-Out Cleaning in Warwickshire | Beyond House Cleaning   57
  short-term-rental-cleaning    Rental Cleaning in Warwickshire | Beyond House Cleaning     55
  post-construction-cleaning    Builders Clean in Warwickshire | Beyond House Cleaning      54

Slugs stay at today's LIVE values (UI-SPEC §13-C). The four Americanised slugs and their 301s are
Phase 3's; D14's eight canonical concepts do not map onto six pages without a decision that belongs
to Phase 3.
</service_template_contract>

<verified_route_template>
<!-- 02-RESEARCH.md § "The service route — verified working end to end". Built in this worktree on
     Next 16.3.0: one .html per slug, correct <title>, <meta name="description"> and <h1>, and both
     routes present in prerender-manifest.json. -->

  export const dynamicParams = false;   // an unlisted slug 404s; the site stays fully static
  export function generateStaticParams() { return SERVICES.map((s) => ({ service: s.slug })); }
  export async function generateMetadata({ params }) {
    const { service } = await params;   // Next 16: params is async; sync access was REMOVED
    ...
  }
  export default async function Page({ params }) { const { service } = await params; ... }

`generateMetadata` IS correct here — this is a dynamic route. Static routes keep
`export const metadata` and must not reach for it.
</verified_route_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/content/services.js — records and copy for the first three services</name>
  <files>web/content/services.js</files>
  <read_first>
    - web/content/site.js — `RATING`, `titleFor`, `describe`, `AREA_LONG`, `AREA_SHORT`.
    - web/content/nav.js — the six service hrefs. The slugs authored here must match them exactly, or the nav links 404 and delta 6 fails.
    - web/content/faqs.js — the `serviceFaqs(slug)` lookup plan 02-06 authored.
    - web/content/utility.js — the record shape and the block-array `prose` convention plan 02-08 established. Match it.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 (the `<h1>` deck, the measured titles, the description form, the voice table) and §9.3 (the content-depth bar).
    - .planning/REQUIREMENTS.md § REQ-content-depth-bar — the live site medians 821 unique words per page and 1,183 on service pages; that margin is the measured reason 95 pages do not collide on similarity, and it must not be regressed.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § Assumptions Log A1 — this is the first time the `STREET_LINE` heuristic meets real copy at volume.
  </read_first>
  <action>
Create `web/content/services.js` exporting `SERVICES` (an ordered array) and `bySlug(slug)`.

Each record: `slug`, `h1`, `eyebrow`, `title`, `description`, `lead` (the Hero lead), `prose` (an
array of `{ type: 'h2' | 'h3' | 'p' | 'ul', text }` blocks matching `utility.js`'s convention), and
`faqs` (from `serviceFaqs(slug)`).

Author full copy for the FIRST THREE services in this task: `deep-cleaning`,
`standard-home-cleaning`, `move-in-cleaning`. `<h1>`, `eyebrow` and `title` come from the contract
block character for character; `description` comes from `describe()` and must be 155 characters or
fewer.

**Content depth: each `prose` block array must total at least 800 unique body words**, and the live
service pages already median 1,183 — do not regress them. Aim for 900 to 1,100. That is real,
specific, differentiated copy, not padding: what the service covers room by room, what is not
included, how long it typically takes, what to expect on the day, and who it suits. Three to five
`h2` sections per page. Every page must read differently from the other five — near-duplicate copy
across six pages is the exact similarity problem the depth bar exists to prevent.

Voice per UI-SPEC §5: plain UK English, contractions, concrete nouns ("skirting boards to oven
interiors"), named towns rather than "your local area". UK vocabulary throughout — `tenancy`,
`flat`, `skirting boards`, `hoover`, `builders clean`. Never `apartment`, `baseboards`, `vacuum`, or
`post-construction` as a visible noun (the slug keeps its existing spelling; the prose says
`builders clean`).

Prohibitions for the header comment and the copy: no street address, no UK postcode, no phone
number, no email address. **Avoid illustrative addresses entirely** — plan 02-02's `STREET_LINE`
matcher needs a leading number plus capitalised name words plus a thoroughfare noun, and a sentence
such as "we cleaned a three-bed on 84 Acacia Road" would trip it. Prices are also out of scope; the
site quotes, it does not price-list.

Add a header comment recording that the slugs are today's LIVE values per UI-SPEC §13-C, that the
four Americanised slugs 301 to their UK equivalents in Phase 3, and that because this file is the
only source of truth for the route, that rename is a data change rather than a template change.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import {SERVICES,bySlug} from './web/content/services.js';import {NAV} from './web/content/nav.js';const navSlugs=(NAV.find(i=>i.label==='Services').children||[]).map(c=>c.href.replace('/services/',''));const done=['deep-cleaning','standard-home-cleaning','move-in-cleaning'];for(const s of done){const r=bySlug(s);if(!r){console.error('missing record',s);process.exit(1)}if(!navSlugs.includes(r.slug)){console.error('slug not in nav:',r.slug);process.exit(1)}if(r.title.length>60){console.error(s,'title',r.title.length);process.exit(1)}if(r.description.length>155){console.error(s,'description',r.description.length);process.exit(1)}const words=r.prose.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ').split(/\s+/).filter(Boolean);const unique=new Set(words.map(w=>w.toLowerCase().replace(/[^a-z-]/g,'')).filter(Boolean));if(words.length<800){console.error(s,'has',words.length,'body words, need >= 800');process.exit(1)}console.log(s,words.length,'words,',unique.size,'unique')}console.log('ok')"` prints a line per service and then `ok`.
    - The three `<h1>` values match the contract block character for character, verified by the same script.
    - No address, postcode, phone or email — the same node check used in plan 02-09 task 1, applied to this file.
    - `grep -ci "apartment\|baseboard\|vacuum\|click here\|learn more" web/content/services.js` returns 0.
    - The three pages are not near-duplicates: compute the Jaccard similarity of their unique-word sets pairwise and record the three figures in the SUMMARY; any pair above 0.6 must be rewritten.
    - `npm run build` exits 0 (no route consumes the module yet in this task).
  </acceptance_criteria>
  <done>Three service records exist with the exact UI-SPEC headings and measured metadata, each carrying at least 800 unique body words of differentiated UK-English copy with no address, postcode, phone or price.</done>
</task>

<task type="auto">
  <name>Task 2: Copy for the remaining three services</name>
  <files>web/content/services.js</files>
  <read_first>
    - web/content/services.js — the three records authored in task 1. Match their structure, their section rhythm and their voice exactly; the six pages must read as one site.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 — the `<h1>`, eyebrow and measured title for each of the remaining three.
    - .planning/REQUIREMENTS.md § REQ-content-depth-bar.
    - This plan's `<service_template_contract>` block.
  </read_first>
  <action>
Author full records for `move-out-cleaning`, `short-term-rental-cleaning` and
`post-construction-cleaning`, to exactly the same standard as task 1: contract-block `<h1>`,
`eyebrow` and `title` character for character, `description` at 155 characters or fewer, and at
least 800 unique body words each, targeting 900 to 1,100.

Angle each one so the six pages do not converge:
- **move-out-cleaning** — the deposit. End-of-tenancy standards, what letting agents actually
  inspect, oven and appliance interiors, and the difference between this and a deep clean.
- **short-term-rental-cleaning** — turnaround between guests. Linen, consumables, the tight window
  between checkout and checkin, and consistency across visits.
- **post-construction-cleaning** — builders clean. Fine dust in places ordinary cleaning misses,
  paint and adhesive residue, why it usually needs two passes, and when to book it relative to
  snagging. Use `builders clean` as the visible noun throughout; the slug keeps its existing
  spelling.

Same prohibitions: no address, no postcode, no phone number, no email, no prices, no illustrative
street names.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `SERVICES` has exactly six records and `bySlug` resolves all six.
    - All six `<h1>` values, eyebrows and titles match the `<service_template_contract>` block character for character; all six titles are 60 characters or fewer and all six descriptions 155 or fewer — verified by extending the task-1 script to all six slugs.
    - All six records carry at least 800 body words; record each count in the SUMMARY.
    - Pairwise Jaccard similarity across all fifteen pairs of unique-word sets is below 0.6; record the highest pair and its figure in the SUMMARY.
    - `post-construction-cleaning`'s prose uses `builders clean` and never `post-construction` as a visible noun: `node --input-type=module -e "import {bySlug} from './web/content/services.js';const t=bySlug('post-construction-cleaning').prose.flatMap(b=>Array.isArray(b.text)?b.text:[b.text]).join(' ');if(/post-construction/i.test(t)){console.error('US noun in body copy');process.exit(1)}if(!/builders clean/i.test(t)){console.error('missing the UK noun');process.exit(1)}console.log('ok')"` prints `ok`.
    - No address, postcode, phone or email anywhere in the file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Six differentiated service records exist, each clearing the 800-word depth bar, with the exact UI-SPEC headings and measured metadata.</done>
</task>

<task type="auto">
  <name>Task 3: The dynamic service route</name>
  <files>web/app/services/[service]/page.jsx</files>
  <read_first>
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "The service route — verified working end to end" — the template was built and its artifacts read in this worktree. Follow it exactly: `dynamicParams = false`, `generateStaticParams`, `await params` in BOTH `generateMetadata` and the default export.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "State of the Art" — Next 16 REMOVED synchronous `params` access. A sync read is a build error, not a warning.
    - web/app/about-us/page.jsx (from plan 02-08) — the composition shape, the single barrel import, the top-level fragment, and the convention of not naming the client directive in prose.
    - web/content/services.js — `SERVICES` and `bySlug`.
    - The `.d.ts` files for `SectionBand`, `Prose`, `BeforeAfterSlider`, `ProcessSteps`, `ReviewRail`, `FAQAccordion`, `CTABand` and `Breadcrumbs`.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.3 and this plan's `<service_template_contract>` block.
  </read_first>
  <action>
Create `web/app/services/[service]/page.jsx` following the verified template.

`export const dynamicParams = false;` — an unlisted slug 404s rather than rendering on demand, which
keeps the site fully static and keeps `prerender-manifest.json` complete. Plan 02-02's harness
asserts `compute === 'static'` for every route, so a route that silently went dynamic would fail.

`generateStaticParams()` returns `SERVICES.map((s) => ({ service: s.slug }))`.

`generateMetadata({ params })` is `async`, does `const { service } = await params`, looks the record
up with `bySlug`, and returns `{ title, description }` from the record. `generateMetadata` is
correct HERE because this is a dynamic route; the static routes in plans 02-08 and 02-09 keep
`export const metadata` and must not be changed to match.

The default export is `async`, does `const { service } = await params`, and composes per the
contract block:
- `<Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: <service short name> }]} />` — TWO
  crumbs. Do not add a `Services` crumb: `/services` 404s in Phase 2 and delta 6 would fail a link
  to it, while an href-less middle crumb is exactly the two-`aria-current` defect plan 02-01 fixed.
- `Hero` with the record's `eyebrow`, `h1`, `lead` and `rating={RATING}`, plus one `primary`
  `Get a Free Quote` -> `/get-a-quote` and one `secondary` `See What's Included` -> `/checklist`.
  **No `emitSchema`.**
- `SectionBand` wrapping `Prose` with the record's blocks, mapped exactly as the legal routes map
  theirs. `Prose` must never emit an `<h1>`; its outline starts at `<h2>`.
- `BeforeAfterSlider` with NO `pairs` — the placeholder state, ROADMAP SC-3, on six more pages.
- `ProcessSteps` from `@/content/process.js`.
- `ReviewRail` with `reviews={[]}` — returns `null` in Phase 2; Phase 4 supplies the data.
- `FAQAccordion` with the record's `faqs`. No `FAQPage` schema, ever.
- `CTABand` last, `primary` to `/get-a-quote` and `ghost` (not `secondary`) to `/checklist`.
- **No `InterlinkBlock`** — Phase 3 populates it, and the SC-4d gate asserts its absence now.
- **No `RatingBadge` inside `CTABand`** — it is light-surface only.

Alternate band tones strictly per the contract block; never two identical tones adjacent and never a
second navy band. Keep the single barrel import, the top-level fragment, and no phone number
anywhere under `web/app`.
  </action>
  <verify>
    <automated>npm run build && npm run check:html && npm run check:budget</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and all six `/services/<slug>` routes appear in `.next/prerender-manifest.json` with `compute: "static"`.
    - `npm run check:html` exits 0 — with plan 02-11 landed, delta 6, the `<h1>` expectations, the JSON-LD counts and the breadcrumb assertions all pass.
    - Each of the six built pages has exactly one `<h1`, one `<main id="main"`, one `<footer`, exactly two `<script type="application/ld+json"` blocks, exactly one `class="…bhc-breadcrumbs__list"`, exactly one `aria-current="page"`, exactly one `bhc-section--navy`, and one `data-bhc-photo-state="pending"`.
    - The breadcrumb trail is two crumbs: `node -e "const fs=require('fs');const h=fs.readFileSync('web/.next/server/app/services/deep-cleaning.html','utf8');const m=h.match(/<ol class=\"bhc-breadcrumbs__list\"[\s\S]*?<\/ol>/);if(!m){console.error('no trail');process.exit(1)}const n=(m[0].match(/<li/g)||[]).length;if(n!==2){console.error('expected 2 crumbs, got',n);process.exit(1)}if((m[0].match(/aria-current=\"page\"/g)||[]).length!==1){console.error('aria-current count');process.exit(1)}if(/href=\"\/services\"/.test(m[0])){console.error('links to the non-existent /services index');process.exit(1)}console.log('ok')"` prints `ok`.
    - `FAQPage` appears zero times in any of the six pages.
    - `zzz-not-a-service` is not prerendered and `dynamicParams` is false: `grep -c "dynamicParams = false" web/app/services/\[service\]/page.jsx` returns 1, and the prerender manifest holds exactly six `/services/*` keys.
    - `grep -rc "emitSchema" web/app` reports 0 for every file, and `grep -rc "447861936533" web/app` reports 0 for every file.
    - `npm run check:budget` exits 0 with worst-page JS under 500 KB and worst-page total under 1024 KB; record the worst route and both figures in the SUMMARY.
  </acceptance_criteria>
  <done>Six service pages prerender from one template and one data file, each with a two-crumb trail marking exactly one crumb current, one `<h1>`, two JSON-LD blocks, a visible photo placeholder and no interlink block — and the Phase 3 pattern is proven under CI at 6x.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path segment -> `bySlug` lookup -> rendered page | The `[service]` segment is the first user-controlled input in the build |
| `services.js` copy -> built HTML | Roughly 6,000 words, the largest block of authored prose in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-65 | Tampering | an unlisted `[service]` slug | mitigate | `dynamicParams = false` makes an unknown slug a 404 rather than an on-demand render, so no attacker-chosen segment ever reaches `bySlug` at runtime (task 3) |
| T-02-66 | Repudiation | a route silently going dynamic | mitigate | Plan 02-02's harness asserts `compute === 'static'` for every manifest route; a dynamic route would otherwise vanish from every assertion (task 3) |
| T-02-67 | Information disclosure | address or postcode in 6,000 words of prose | mitigate | Source-level postcode, street-line, phone and email checks in tasks 1 and 2, plus plan 02-02's `SC-2d` scan over every built page. This is the copy volume assumption A1 flagged |
| T-02-68 | Tampering | a third JSON-LD block | mitigate | No `emitSchema`; plan 02-02's SC-4f replacement asserts exactly two on these pages |
| T-02-69 | Tampering | a breadcrumb linking to the non-existent `/services` index | mitigate | Two-crumb trail asserted directly in the built HTML, and delta 6 would fail the link independently (task 3) |
| T-02-70 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run verify` green end to end at the close of wave 5.
- The prerender manifest holds 18 app routes plus `/_global-error`: `/`, ten utility, six service, `/_not-found`.
- Every service page clears the 800-word depth bar and no pair of pages exceeds 0.6 Jaccard similarity.
</verification>

<success_criteria>
- Six service routes prerender statically from one dynamic route and one data file.
- Each `<h1>`, eyebrow and title matches UI-SPEC §5 character for character, every title 60 chars or fewer and every description 155 or fewer.
- Each page carries at least 800 unique body words and reads differently from the other five.
- `dynamicParams = false`, `generateStaticParams` and `await params` are all in place, proving the Phase 3 pattern under CI.
- Two-crumb trails with exactly one `aria-current="page"`, no `/services` link, and no `InterlinkBlock`.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-12-SUMMARY.md` when done
</output>
