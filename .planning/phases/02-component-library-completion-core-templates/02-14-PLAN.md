---
phase: 02-component-library-completion-core-templates
plan: 14
type: execute
wave: 6
depends_on: ["02-10"]
files_modified:
  - design-system/test/locks.test.js
  - design-system/README.md
  - design-system/.design-sync/NOTES.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A component whose preview file silently disappears fails the lock suite"
    - "The package README describes the twenty-two components that exist, not the six that used to"
    - "The design-sync notes name the real group distribution and the real preview coverage"
  artifacts:
    - path: "design-system/README.md"
      provides: "a component table covering all 22 components"
    - path: "design-system/.design-sync/NOTES.md"
      provides: "corrected group enumeration and preview-coverage statement"
  key_links:
    - from: "design-system/test/locks.test.js"
      to: "the 22 component preview files"
      via: "the anti-vacuity floor raised from 6 to 22"
      pattern: "previews.length"
---

<objective>
Raise the preview-count floor to its real post-phase value and bring the three package documents
that describe the component set back into agreement with it.

Purpose: `locks.test.js`'s `previews.length >= 6` floor walks EVERY `.html` under `src`, so today it
is exactly the component count. Left at 6, a component whose preview silently disappeared would
still read green — which is precisely the class of regression the floor exists to catch. The three
documents are the human-facing half of the same problem: a README claiming six components and a
NOTES file enumerating four group assignments are both actively misleading after this phase.

Output: one raised floor and three corrected documents. No component or route changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md
@.planning/phases/02-component-library-completion-core-templates/02-10-SUMMARY.md

<known_stale_statements>
<!-- 02-PATTERNS.md §6 findings 5, 8 and 9, each verified in place during pattern mapping. -->

design-system/test/locks.test.js:219   assert.ok(previews.length >= 6, 'expected a preview per component')
                                      -> 22 (6 shipped + 16 new). The walk counts EVERY .html under
                                         src, so the number is the component count, not an estimate.

design-system/test/locks.test.js:331   assert.ok(emitters.length >= 3, ...)
                                      -> STAYS 3. None of the sixteen emits JSON-LD, the assertion
                                         is `>=`, and it is safe unchanged. Do NOT "helpfully" bump it.

design-system/README.md               ## Components table is a 6-row enumeration with
                                      `Component | Group | Lock` columns -> needs 16 more rows.
                                      Its closing line reads "First sync is foundations plus the
                                      five locked components. The remaining ~15 follow…" -> stale.
                                      README quotes NO heading, so UI-SPEC §5's remediation item 3
                                      is verified as needing no action on that count.

design-system/.design-sync/NOTES.md:65   hardcodes "Current: Button→Actions, Hero→Content,
                                         Breadcrumbs/NAPFooter/InterlinkBlock→Navigation,
                                         RatingBadge→Trust" -> stale.
design-system/.design-sync/NOTES.md:107  "All 6 components have authored previews" -> stale.

The 22 components and their groups after this phase:
  Navigation  Breadcrumbs, NAPFooter, InterlinkBlock, Header, Footer, SkipLink
  Content     Hero, SectionBand, Prose, ServiceCard, TownCard, ProcessSteps, FAQAccordion, CTABand
  Trust       RatingBadge, TrustBar, ReviewCard, ReviewRail, BeforeAfterSlider
  Actions     Button, QuoteFormEntry, StickyCallBar
</known_stale_statements>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise the preview floor to 22</name>
  <files>design-system/test/locks.test.js</files>
  <read_first>
    - design-system/test/locks.test.js lines 209-224 — the `every preview declares a @dsCard group on line 1` test, its `walk` over `.html` files under `src`, the exact line-1 regex, and the `assert.ok(previews.length >= 6, …)` floor.
    - design-system/test/locks.test.js lines 325-335 — the `emitters.length >= 3` floor, which STAYS at 3.
    - design-system/test/run-locks.mjs — `MIN_TESTS`, set to its exact value by plan 02-03. This task adds no test, so `MIN_TESTS` does not change.
    - This plan's `<known_stale_statements>` block.
  </read_first>
  <action>
Change `assert.ok(previews.length >= 6, 'expected a preview per component')` to `>= 22`, and extend
the message so the number is self-explaining — name it as six shipped plus sixteen added in Phase 2,
and state that the walk counts every `.html` under `src`, so this figure IS the component count
rather than an approximation. Anyone adding a component in Phase 3 raises it again.

Leave `emitters.length >= 3` at 3. None of the sixteen new components emits JSON-LD, the assertion
is a `>=` floor, and bumping it would assert emitters that do not and should not exist.

Add no new test, so `MIN_TESTS` in `run-locks.mjs` is unchanged — plan 02-03 set it to the exact
suite size and this task tightens an existing assertion rather than adding one. Do not touch that
file.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:locks` exits 0 and the passing test count equals `MIN_TESTS` in `run-locks.mjs` — unchanged by this task.
    - `grep -c "previews.length >= 22" design-system/test/locks.test.js` returns 1 and `grep -c "previews.length >= 6" design-system/test/locks.test.js` returns 0.
    - `grep -c "emitters.length >= 3" design-system/test/locks.test.js` returns 1 — the JSON-LD floor was not bumped.
    - There really are 22 previews: `node -e "const {readdirSync}=require('fs');const {join}=require('path');const walk=(d,o=[])=>{for(const e of readdirSync(d,{withFileTypes:true})){const p=join(d,e.name);if(e.isDirectory())walk(p,o);else if(e.name.endsWith('.html'))o.push(p)}return o};const n=walk('design-system/src').length;if(n!==22){console.error('expected 22 previews, found',n);process.exit(1)}console.log('22')"` prints `22`.
    - Negative proof (run, record, revert): temporarily renaming any one `.html` preview makes `npm run test:locks` exit non-zero on the floor.
    - `git diff design-system/test/run-locks.mjs` is empty.
  </acceptance_criteria>
  <done>The preview floor equals the real component count, so a silently vanished preview fails the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Bring README.md and NOTES.md back into agreement with the package</name>
  <files>design-system/README.md, design-system/.design-sync/NOTES.md</files>
  <read_first>
    - design-system/README.md — the whole file, especially the `## Components` table with its `Component | Group | Lock` columns and the closing line about the first sync covering "foundations plus the five locked components. The remaining ~15 follow…".
    - design-system/.design-sync/NOTES.md — the whole file, especially line 65's hardcoded group enumeration, line 107's "All 6 components have authored previews", and the "Re-sync risks" section whose item 3 states that an unregistered component will not sync with no error.
    - design-system/.design-sync/config.json — the final `componentSrcMap` and `docsMap`, and the `overrides` block after plan 02-10 added `cardMode: "column"` entries.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § Assumptions Log A6 and A7 — no `src/index.d.ts` barrel and no `types` field are added this phase, and authored `.design-sync/previews/*.tsx` files are optional for the sixteen new components.
    - This plan's `<known_stale_statements>` block — the authoritative 22-component group listing.
  </read_first>
  <action>
**`design-system/README.md`.** Grow the `## Components` table from 6 rows to 22, keeping the existing
`Component | Group | Lock` columns and the existing row format. Groups come from the
`<known_stale_statements>` block. The `Lock` column names the numbered lock a component serves, or is
left as the existing "no numbered lock" form for those that serve none — `Button`'s row is the model
for that. Components serving a named Phase 2 criterion should say so: `FAQAccordion` serves
ROADMAP SC-4 (present without `FAQPage` schema) and `BeforeAfterSlider` serves SC-3
(placeholder-capable).

Replace the closing line. It currently promises that "the remaining ~15 follow"; after this phase all
sixteen have shipped. Replace it with a statement of the real position: 22 components, the four
groups, and the fact that the package still has no `src/index.d.ts` barrel and no `types` field in
`package.json` — a deliberate deferral, which is exactly why all 22 must be registered in both
`.design-sync` maps and why the delta 9 lock makes an unregistered component a hard failure.

**`design-system/.design-sync/NOTES.md`.** Two corrections:
- Line 65's hardcoded enumeration becomes the full 22-component group distribution from the
  `<known_stale_statements>` block. Better still, state the four group NAMES and the per-group counts
  and point at `config.json` as the enumeration, so this line cannot go stale again in Phase 3.
- Line 107's "All 6 components have authored previews" becomes an accurate statement: all 22 have an
  authored `.html` preview under `src/components/`, while `.design-sync/previews/*.tsx` exists for
  the original six only. Record the decision and its consequence — the sixteen new components get
  auto-generated Claude Design previews rather than authored ones, which is cosmetic and reversible
  (RESEARCH A7), and anyone who wants an authored preview adds a `.tsx` without touching anything
  else.

Also record, in whichever of the two documents fits better, the `cardMode: "column"` overrides plan
02-10 added for `Header`, `Footer` and `StickyCallBar`, and why: they are full-width by nature, the
same `[GRID_OVERFLOW]` shape that produced the original `NAPFooter` override.

Do not change `config.json` in this plan — the per-component registrations were made by the plans
that authored the components, and delta 9 already asserts them in both directions.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - The README component table has 22 data rows: `node -e "const s=require('fs').readFileSync('design-system/README.md','utf8');const i=s.indexOf('## Components');const sec=s.slice(i, s.indexOf('\n## ', i+5)>-1?s.indexOf('\n## ', i+5):s.length);const rows=sec.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*-+/.test(l));if(rows.length-1!==22){console.error('expected 22 data rows, found',rows.length-1);process.exit(1)}console.log('22')"` prints `22`.
    - Every component directory name appears in the README table: `node -e "const {readdirSync}=require('fs');const s=require('fs').readFileSync('design-system/README.md','utf8');const dirs=readdirSync('design-system/src/components',{withFileTypes:true}).filter(e=>e.isDirectory()).map(e=>e.name);const missing=dirs.filter(d=>!s.includes(d));if(missing.length){console.error('missing from README:',missing);process.exit(1)}console.log('all',dirs.length,'listed')"` prints `all 22 listed`.
    - `grep -c "The remaining ~15 follow" design-system/README.md` returns 0.
    - `grep -c "All 6 components have authored previews" design-system/.design-sync/NOTES.md` returns 0, and `grep -c "Button→Actions" design-system/.design-sync/NOTES.md` returns 0.
    - `NOTES.md` names all four groups and either the per-group counts or `config.json` as the enumeration, and states the `.tsx` preview position accurately.
    - `git diff design-system/.design-sync/config.json` is empty in this plan.
    - `npm run test:locks` exits 0.
  </acceptance_criteria>
  <done>The README lists all 22 components with their groups and locks, and the design-sync notes describe the real group distribution, the real preview coverage and the three new card-mode overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package documentation -> the next author's mental model | A stale component table is how a Phase 3 author ships an unregistered component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-78 | Repudiation | a silently deleted preview | mitigate | The floor is raised to the exact component count with a negative proof that removing one turns the suite red (task 1) |
| T-02-79 | Repudiation | documentation drifting from the package | mitigate | The README table is asserted against the real directory listing, so a component missing from the docs is detectable rather than merely wrong (task 2) |
| T-02-80 | Tampering | a bumped JSON-LD emitter floor asserting emitters that should not exist | mitigate | `emitters.length >= 3` explicitly stays at 3, with an acceptance check that it was not raised (task 1) |
| T-02-81 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run test:locks` green, passing count unchanged and equal to `MIN_TESTS`.
- `npm run verify` green end to end.
- `git diff design-system/package.json design-system/test/run-locks.mjs design-system/.design-sync/config.json` is empty.
</verification>

<success_criteria>
- The preview floor is 22 and fails when any preview is removed.
- The JSON-LD emitter floor is still 3.
- The README lists every one of the 22 component directories with its group and lock.
- `NOTES.md` states the real group distribution, the real `.tsx` preview coverage, and the three new `cardMode` overrides.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-14-SUMMARY.md` when done
</output>
