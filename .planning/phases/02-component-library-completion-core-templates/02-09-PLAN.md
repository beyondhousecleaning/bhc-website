---
phase: 02-component-library-completion-core-templates
plan: 09
type: execute
wave: 4
depends_on: ["02-02", "02-04", "02-06"]
files_modified:
  - web/content/legal.js
  - web/app/privacy-policy/page.jsx
  - web/app/terms-of-service/page.jsx
  - web/app/customer-service-agreement/page.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The three legal routes render, each with exactly one h1 and a readable narrow measure"
    - "No legal page contains a street address, a postcode or a second phone number"
    - "Every footer legal-row link has a destination that resolves"
  artifacts:
    - path: "web/content/legal.js"
      provides: "the three legal documents as structured prose blocks"
    - path: "web/app/customer-service-agreement/page.jsx"
      provides: "the route the footer legal row links to — descoping it would break delta 6"
  key_links:
    - from: "web/content/nav.js LEGAL"
      to: "the three routes in this plan"
      via: "footer legal-row hrefs that plan 02-13's Footer renders and delta 6 asserts"
      pattern: "privacy-policy"
    - from: "web/app/*/page.jsx"
      to: "web/content/blocks.jsx"
      via: "renderBlocks — the one shared block mapping, created in wave 3 by plan 02-06"
      pattern: "renderBlocks"
---

<objective>
Ship the three legal routes — `/privacy-policy`, `/terms-of-service` and
`/customer-service-agreement` — and the data module holding their text.

Purpose: these complete ROADMAP Success Criterion 2's utility set, and they are the reason the real
utility-page count is ten rather than the roadmap's "~8". The footer's Legal row links all three, so
shipping fewer is not a smaller scope — it is a broken build, because plan 02-02's delta 6 asserts
every internal href resolves to a prerendered route.

Output: one data module and three route files. No lock-harness changes: plan 02-02's
`PAGE_EXPECTATIONS` already carries all three entries with `hasBreadcrumbs: true` and
`ldJsonBlocks: 2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-06-SUMMARY.md

<route_contract>
<!-- UI-SPEC §9.4, legal-page variation. -->

Template: Breadcrumbs -> Hero align="centered" (no image, rating INCLUDED) -> Prose width="narrow"
NO CTABand. NO BeforeAfterSlider. NO QuoteFormEntry.

Prose blocks are rendered with `renderBlocks` from `@/content/blocks.jsx`, created in wave 3 by plan
02-06. Do not write a block-to-element mapping in a route file and do not introduce a second shared
renderer — there is exactly one, and plans 02-08 and 02-12 use the same one.

The exact <h1> for each, already present in plan 02-02's PAGE_EXPECTATIONS table and required to
match CHARACTER FOR CHARACTER:
  /privacy-policy               Privacy Policy
  /terms-of-service             Terms of Service
  /customer-service-agreement   Customer Service Agreement

Breadcrumb trail: two crumbs, `Home` -> `/` then the page label with no href. Plan 02-01's rule
means only the last crumb carries aria-current="page".

Titles follow `{H1} | Beyond House Cleaning` via `titleFor` from `@/content/site.js`, each checked
at 60 characters or fewer.

Class-name counting rule for any acceptance criterion over BUILT HTML: use the `class="[^"]*bhc-…`
form. A bare grep double-counts because the RSC flight payload is inlined in the HTML. Zero-
assertions are safe in either form.
</route_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/content/legal.js — the three documents</name>
  <files>web/content/legal.js</files>
  <read_first>
    - web/content/site.js — `titleFor`, `describe`, `RATING`, `BRAND`. Reuse them; do not restate the brand or the rating.
    - web/content/blocks.jsx — the `Block` shape (`{ type: 'p'|'h2'|'h3'|'ul', text }`, where a `ul` block carries its items as an array in `text`). Legal documents need headings and lists, so this is the shape `prose` is authored against.
    - web/content/utility.js — the record shape plan 02-08 established (`path`, `h1`, `eyebrow`, `lead`, `title`, `description`, `prose`). Match it so both modules read the same way.
    - design-system/src/components/InterlinkBlock/geo.js — the plain-`.js` module-shape analog with a header comment stating why it lives apart from the `.jsx`.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 (the `<h1>` deck and the voice table), §9.4 (the legal-page variation).
    - .planning/PROJECT.md — decision D4: no street address or postcode anywhere, ever. Legal boilerplate templates habitually include a registered-address block; this one must not.
  </read_first>
  <action>
Create `web/content/legal.js` exporting `LEGAL_PAGES` — a keyed object of three records — and a
`byPath(path)` lookup, matching `utility.js`'s record shape so the two modules read identically.

`prose` is an array of `Block` objects from `blocks.jsx`'s shape: `{ type: 'h2' | 'h3' | 'p' | 'ul', text }`
where a `ul` block's `text` is an array of item strings. `Prose` styles all of these through its
`:where()` descendant rules, so no new class is needed.

Author the three documents for a UK domestic cleaning company operating in Warwickshire and the West
Midlands:
- **Privacy Policy** — what data is collected via the quote and booking flow, why, the lawful basis,
  how long it is kept, third-party processors in general terms, and the UK GDPR data-subject rights
  with a contact route for exercising them. Route contact through `/contact-us`, not through an
  email address or phone number written into this file.
- **Terms of Service** — the terms under which the website is used: accuracy of information,
  intellectual property, links to third-party sites, and limitation of liability. This is the
  website's terms, not the cleaning contract.
- **Customer Service Agreement** — the cleaning-service terms: what a booking includes, access
  arrangements, cancellation and rescheduling, payment, and the satisfaction promise. Keep the
  satisfaction wording aligned with the `ProcessSteps` step 3 default from `@/content/process.js` —
  no time window and no re-clean commitment — and add a comment recording that UI-SPEC §14-1 has
  this pending Sam's confirmation, so both places change together when he answers.

Voice: plain UK English per UI-SPEC §5. Legal registers drift towards "the aforementioned party
shall"; keep it readable. UK vocabulary throughout.

**Hard prohibitions for the header comment and for the copy itself:** no street address, no UK
postcode, no company registration address block, no phone number, no email address. D4 forbids the
address permanently; the phone number lives once in `design-system/src/phone.js` and reaches these
pages only through `NAPFooter`. Avoid any illustrative address — plan 02-02's `STREET_LINE`
heuristic matches a leading number plus capitalised name words plus a thoroughfare noun, and legal
boilerplate is the single most likely place in the whole site to trip it.

Each `description` must be 155 characters or fewer and each `title` 60 or fewer.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import {LEGAL_PAGES} from './web/content/legal.js';const want={'/privacy-policy':'Privacy Policy','/terms-of-service':'Terms of Service','/customer-service-agreement':'Customer Service Agreement'};const recs=Object.values(LEGAL_PAGES);if(recs.length!==3){console.error('expected 3, got',recs.length);process.exit(1)}for(const r of recs){if(r.h1!==want[r.path]){console.error('h1 mismatch',r.path,r.h1);process.exit(1)}if(r.title.length>60){console.error(r.path,'title',r.title.length);process.exit(1)}if(r.description.length>155){console.error(r.path,'description',r.description.length);process.exit(1)}if(!Array.isArray(r.prose)||!r.prose.length){console.error(r.path,'empty prose');process.exit(1)}for(const b of r.prose){if(!['p','h2','h3','ul'].includes(b.type)){console.error(r.path,'bad block type',b.type);process.exit(1)}if(b.type==='ul'&&!Array.isArray(b.text)){console.error(r.path,'ul block text must be an array');process.exit(1)}}const h2s=r.prose.filter(b=>b.type==='h2').length;if(h2s<3){console.error(r.path,'has only',h2s,'h2 sections');process.exit(1)}}console.log('ok')"` prints `ok`.
    - No address, postcode, phone or email: `node -e "const s=require('fs').readFileSync('web/content/legal.js','utf8');const fail=(m)=>{console.error(m);process.exit(1)};if(/\b[A-Z]{1,2}\d[A-Z\d]?\s?\d[A-Z]{2}\b/.test(s))fail('postcode-shaped string');if(/447861936533|07861|07441|07575/.test(s))fail('phone digits');if(/[\w.+-]+@[\w-]+\.[\w.]+/.test(s))fail('email address');if(/\b\d{1,4}[a-zA-Z]?\s+(?:[A-Z][a-z]+\s+){1,2}(?:Road|Street|Avenue|Lane|Close|Drive|Way|Court|Crescent|Terrace|Grove|Gardens|Place|Park|Hill|Walk|Row|Mews)\b/.test(s))fail('street-line-shaped string');console.log('ok')"` prints `ok`.
    - `grep -ci "apartment\|baseboard\|aforementioned" web/content/legal.js` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Three structured legal documents exist as valid prose blocks, in readable UK English, each with at least three sections, and with no address, postcode, phone number or email address anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: The three legal routes</name>
  <files>web/app/privacy-policy/page.jsx, web/app/terms-of-service/page.jsx, web/app/customer-service-agreement/page.jsx</files>
  <read_first>
    - web/app/page.jsx — as trimmed by plan 02-02: the single barrel import from `@bhc/design-system`, the top-level fragment, `export const metadata`, and lines 11-15's convention of not naming the client directive in prose because the CI gate is a substring grep over `web/app`.
    - web/content/blocks.jsx — `renderBlocks`, the one permitted block-to-element mapping. It never emits an `<h1>` and drops unknown types.
    - design-system/src/components/Prose/Prose.jsx and its `.d.ts` — the `width` prop, and the rule that `Prose` never emits an `<h1>`; its outline starts at `<h2>`.
    - design-system/src/components/SectionBand/SectionBand.jsx — the `width` prop mapping to `bhc-container--narrow`.
    - web/content/legal.js — the records this task renders.
    - web/content/site.js — `RATING`.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.4 legal variation, and this plan's `<route_contract>` block.
  </read_first>
  <action>
Create one `page.jsx` per route under `web/app/<slug>/`. Each is static, so use
`export const metadata = { title, description }` read from the `legal.js` record — do not reach for
`generateMetadata`.

Composition per `<route_contract>`:
`<Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: record.h1 }]} />`, then
`<Hero align="centered" heading={record.h1} lead={record.lead} rating={RATING} />`, then a
`<SectionBand width="narrow">` wrapping `<Prose width="narrow">{renderBlocks(record.prose)}</Prose>`.
**No `CTABand`, no `BeforeAfterSlider`, no `QuoteFormEntry`** — a legal page is not a conversion
surface, and §9.4 says so explicitly.

`renderBlocks` is imported from `@/content/blocks.jsx`. Do not write a local mapping in any of these
three files, and do not add a new shared renderer — plan 02-06 already shipped the only one, plans
02-08 and 02-12 use the same one, and a second would drift.

Rules for every file:
- Single barrel import from `@bhc/design-system`. No deep path into the package.
- Top-level fragment. Landmarks live in the layout.
- No `emitSchema` — plan 02-02's SC-4f replacement expects exactly two JSON-LD blocks on these pages.
- No phone number written in `web/app`.
- No internal href beyond `/` (the first breadcrumb). `/services/*` does not exist until wave 5.
  </action>
  <verify>
    <automated>npm run build && npm run check:html && npm run check:budget</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and all three routes appear in `.next/prerender-manifest.json` with `compute: "static"`.
    - `npm run check:html` exits 0 without any change to `PAGE_EXPECTATIONS`.
    - Each of the three built pages has exactly one `<h1`, one `<main id="main"`, one `<footer`, exactly two `<script type="application/ld+json"` blocks, exactly one `class="…bhc-breadcrumbs__list"` counted with the `class="` form, and at most one `aria-current="page"`.
    - The `<h1>` text matches exactly, using the tag-stripping extractor: `node -e "const fs=require('fs');for(const [f,w] of [['privacy-policy.html','Privacy Policy'],['terms-of-service.html','Terms of Service'],['customer-service-agreement.html','Customer Service Agreement']]){const h=fs.readFileSync('web/.next/server/app/'+f,'utf8');const m=h.match(/<h1[^>]*>([\s\S]*?)<\/h1>/);const t=m&&m[1].replace(/<[^>]*>/g,'').trim();if(t!==w){console.error(f,'got',JSON.stringify(t));process.exit(1)}}console.log('ok')"` prints `ok`.
    - No CTABand on any legal page: `grep -c "bhc-section--navy" web/.next/server/app/privacy-policy.html` returns 0, and the same for the other two. (A zero-assertion, so the bare form is safe here.)
    - `Prose` renders at the narrow measure: each page's HTML contains `bhc-container--narrow`.
    - No route file defines its own block mapping: `grep -rc "case 'h2'\|type === 'h2'" web/app` reports 0 for every file, and each of the three route files contains `renderBlocks`.
    - No postcode, street line or extra phone number reaches the built HTML — plan 02-02's `SC-2d` and delta 2 both pass.
    - `grep -rc "emitSchema" web/app` reports 0 for every file.
    - `npm run check:budget` exits 0 with worst-page JS under 500 KB and worst-page total under 1024 KB.
  </acceptance_criteria>
  <done>All ten utility routes now prerender. The three legal pages render at a narrow measure with one `<h1>`, a two-crumb trail, two JSON-LD blocks, no conversion furniture, and their prose mapped through the one shared renderer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored legal copy -> built HTML | Legal boilerplate is the single most likely source of a registered-address block, which D4 forbids permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-48 | Information disclosure | registered-address or postcode block in legal copy | mitigate | Explicit prohibition in the module header, a source-level check for postcode, street-line, phone and email shapes in task 1, and plan 02-02's `SC-2d` scan over built HTML in task 2 |
| T-02-49 | Information disclosure | an email address or second phone number as a contact route | mitigate | Data-subject and complaint contact routes point at `/contact-us`; the canonical number reaches these pages only through `NAPFooter` from `design-system/src/phone.js` |
| T-02-50 | Repudiation | the satisfaction promise drifting between the agreement and ProcessSteps | mitigate | Both default to the same wording, with a comment in each naming UI-SPEC §14-1 as the pending confirmation so they change together (task 1) |
| T-02-51 | Tampering | a third JSON-LD block from a stray `emitSchema` | mitigate | Plan 02-02's SC-4f replacement asserts exactly two on these pages; the acceptance criteria check it directly |
| T-02-52 | Denial of Service | a malformed prose block | mitigate | `renderBlocks` drops unknown types rather than throwing, and task 1's acceptance validates every authored block type |
| T-02-53 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run verify` green end to end.
- The prerender manifest holds 13 routes: `/`, the ten utility routes, `/_global-error`, `/_not-found`.
- Delta 6 green across all eleven app pages.
</verification>

<success_criteria>
- Three legal routes prerender statically with the exact UI-SPEC `<h1>`s.
- Each renders `Prose width="narrow"` and no `CTABand`, `BeforeAfterSlider` or `QuoteFormEntry`.
- Every prose block goes through the one shared `renderBlocks`; no route file defines a second mapping.
- No address, postcode, phone number or email address appears in the module or in the built HTML.
- All ten utility routes from UI-SPEC §5 now exist, so no footer legal-row link can point at a 404.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-09-SUMMARY.md` when done
</output>
