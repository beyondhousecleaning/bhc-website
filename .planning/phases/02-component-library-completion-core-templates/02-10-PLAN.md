---
phase: 02-component-library-completion-core-templates
plan: 10
type: execute
wave: 5
depends_on: ["02-01", "02-07"]
files_modified:
  - design-system/src/components/SkipLink/SkipLink.jsx
  - design-system/src/components/SkipLink/SkipLink.html
  - design-system/src/components/SkipLink/SkipLink.d.ts
  - design-system/src/components/SkipLink/SkipLink.prompt.md
  - design-system/src/components/Footer/Footer.jsx
  - design-system/src/components/Footer/Footer.html
  - design-system/src/components/Footer/Footer.d.ts
  - design-system/src/components/Footer/Footer.prompt.md
  - design-system/src/components/Header/Header.jsx
  - design-system/src/components/Header/Header.html
  - design-system/src/components/Header/Header.d.ts
  - design-system/src/components/Header/Header.prompt.md
  - design-system/src/components/StickyCallBar/StickyCallBar.jsx
  - design-system/src/components/StickyCallBar/StickyCallBar.html
  - design-system/src/components/StickyCallBar/StickyCallBar.d.ts
  - design-system/src/components/StickyCallBar/StickyCallBar.prompt.md
  - design-system/styles.css
  - design-system/src/index.js
  - design-system/.design-sync/config.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "The mobile navigation opens and closes with JavaScript disabled and is unconditionally visible at desktop width"
    - "Every navigation link exists in the DOM exactly once"
    - "The footer composition adds no second footer landmark"
    - "The sticky call bar offers a phone action whose displayed digits, where present, equal its href digits"
  artifacts:
    - path: "design-system/src/components/Header/Header.jsx"
      provides: "the one header landmark and the one nav aria-label=Primary per page"
      contains: "details"
    - path: "design-system/src/components/Footer/Footer.jsx"
      provides: "a composition layer that emits no footer element"
  key_links:
    - from: "design-system/src/components/Header/Header.jsx"
      to: "design-system/src/phone.js"
      via: "the desktop tel: link defaults from CANONICAL_PHONE"
      pattern: "CANONICAL_PHONE"
    - from: "design-system/styles.css"
      to: "Header's sibling-combinator nav mechanism"
      via: "[open] ~ .bhc-nav__list"
      pattern: "\\[open\\] ~ \\.bhc-nav__list"
---

<objective>
Author the four Navigation and Actions components the root layout composes: `SkipLink`, `Footer`,
`Header` and `StickyCallBar`. These are the last four of the sixteen.

Purpose: they are deliberately last. `Header` and `Footer` link to every one of the seventeen
routes, and plan 02-02's delta 6 fails any internal href that does not resolve — so composing them
before the routes existed would have kept CI red for three waves. All seventeen routes land in this
wave or earlier, so plan 02-13 can wire these into the layout with the gate green.

Output: sixteen files across four component directories, four `styles.css` blocks appended above the
focus-override banner, four `index.js` exports and eight `.design-sync/config.json` entries. No app
file changes — plan 02-13 composes them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-07-SUMMARY.md

<component_authoring_contract>
<!-- Identical to plans 02-04, 02-05 and 02-07. Repeated so this plan is self-contained. -->

Each component is a directory `design-system/src/components/<Name>/` holding EXACTLY the four
canonical files, plus a CSS block in `design-system/styles.css` **appended ABOVE the focus-override
banner, which must remain the last section of the file**, a named export in
`design-system/src/index.js`, `<!-- @dsCard group="…" -->` on LINE 1 of the `.html` followed by
`<link rel="stylesheet" href="../../../styles.css" />`, `category: <same group>` frontmatter on the
`.prompt.md`, and an entry in BOTH `componentSrcMap` and `docsMap` in `.design-sync/config.json`.

Groups here: `SkipLink`, `Footer` and `Header` are `Navigation`; `StickyCallBar` is `Actions`.

Hard rules: no `import React`; sibling relative paths with the extension always written; no client
directive, `useState`, `useEffect` or event-handler props anywhere; never `&&` in JSX (ternary to
`null`); the `.filter(Boolean).join(' ')` className merge; variant maps with a fallback;
`export default <Name>;` at the end of every file; pure string manipulation in a plain `.js`
sibling, never in a `.jsx`; values exclusively `var(--bhc-*)` with no literal hex; comments state
measured evidence; every `<svg>` carries `aria-hidden="true"` or `role="img"` plus a non-empty
`aria-label`, and never `alt`.

Phone rule (Pattern S5): `Header`, `StickyCallBar` and `QuoteFormEntry` all default `phone` from
`CANONICAL_PHONE` in `../../phone.js`. `formatPhone`/`toDial` are imported sibling-relative from
`'../NAPFooter/formatPhone.js'`, NEVER through `src/index.js`, which would be circular. There is no
prop for the displayed text: `formatPhone()` supplies it and `toDial()` supplies the href, both from
the same value.

Consider `cardMode: "column"` in `.design-sync/config.json` `overrides` for `Header`, `Footer` and
`StickyCallBar` — all full-width by nature, the same `[GRID_OVERFLOW]` shape that produced the
existing `NAPFooter` override. Leave the `NAPFooter` override itself untouched.
</component_authoring_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SkipLink and Footer</name>
  <files>design-system/src/components/SkipLink/SkipLink.jsx, design-system/src/components/SkipLink/SkipLink.html, design-system/src/components/SkipLink/SkipLink.d.ts, design-system/src/components/SkipLink/SkipLink.prompt.md, design-system/src/components/Footer/Footer.jsx, design-system/src/components/Footer/Footer.html, design-system/src/components/Footer/Footer.d.ts, design-system/src/components/Footer/Footer.prompt.md, design-system/styles.css, design-system/src/index.js, design-system/.design-sync/config.json</files>
  <read_first>
    - design-system/styles.css line 51 onwards — `.bhc-skip-link` ALREADY EXISTS. `SkipLink` is a wrapper over shipped CSS, not a new stylesheet. Do not restyle it.
    - design-system/src/components/Button/Button.jsx — the smallest shipped component, the shape analog for `SkipLink`.
    - design-system/src/components/NAPFooter/NAPFooter.jsx — the whole file, especially the `columns`/`legal`/`social`/`mapsUrl` props, the `<footer>` landmark it owns, the single `tel:` link at lines 73-76, the `HomeAndConstructionBusiness` JSON-LD emitter at lines 124-127, and the guard idiom at lines 89-107 (`columns.filter(Boolean)` and `(col.links || [])`) with its comment about a malformed entry 500-ing every route.
    - design-system/src/components/NAPFooter/NAPFooter.d.ts lines 1-2 — the `FooterLink` and `FooterColumn` shapes `Footer` passes through.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §7.2 (Footer emits NO `<footer>` element and why), §7.3 (SkipLink placement and the `tabIndex={-1}` target), §13-E.
    - web/content/nav.js — the `FOOTER_COLUMNS` and `LEGAL` data plan 02-06 authored, which this component consumes.
  </read_first>
  <action>
**SkipLink** — group `Navigation`. The whole component is
`<a className="bhc-skip-link" href="#main">Skip to content</a>` with an optional `href` prop
defaulting to `'#main'`, an optional `children` defaulting to `Skip to content`, and `className`.

`.bhc-skip-link` already exists in `styles.css` — **do not restyle it and do not add a CSS block for
this component.** Record in the `.prompt.md` that it must be the FIRST element inside `<body>`,
before `Header`, and that the layout must render `<main id="main" tabIndex={-1}>`; the `tabIndex={-1}`
is what actually moves focus in Safari and Chrome, and without it the link jumps the viewport but
not the focus ring. Plan 02-02 already added the `<main id="main" tabIndex={-1}>` wrapper.

**Footer** — group `Navigation`. Props per §7.2: `columns`, `legal`, `social`, `mapsUrl`, `as`,
`className`.

**`Footer` emits no `<footer>` element.** It is a composition layer that supplies sitewide
column, legal and social data to `NAPFooter`, which owns the landmark, the single `tel:` link and the
`HomeAndConstructionBusiness` JSON-LD. `check-html-locks.mjs` asserts exactly one `<footer>` per
page and that must stay true; a second landmark would also break the structural argument behind
REQ-nap-consistency. Put that in the header comment, in the `.d.ts` JSDoc and in `## Don't`.

Copy the `NAPFooter` guard idiom verbatim in spirit: `columns.filter(Boolean)` and
`(col.links || [])` before mapping, with the comment explaining that this component sits in the root
layout, so a single malformed entry in a Phase 3 nav data file would 500 EVERY route rather than
break one list.

Pass `mapsUrl` through only when it is an absolute `https://` URL — `NAPFooter` applies the WR-10
scheme guard plan 02-01 added, so `Footer` must not defeat it by normalising or defaulting the value.

Pass no `phone` prop. The number lives once in `design-system/src/phone.js`.

Previews: `SkipLink.html` uses Shape B with two labelled cells — the link in its visually-hidden
resting state and in its focused state (simulate the focused state with an inline style on a wrapper,
since the preview is a static document). `Footer.html` uses Shape A: the composed footer full-bleed,
then a caption explaining that this component emits no landmark and why.

Register both in `index.js` and both `.design-sync` maps; add the `cardMode: "column"` override for
`Footer`.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - Both directories hold exactly the four canonical files; `@dsCard group="Navigation"` on line 1 of each `.html`; `category: Navigation` in each `.prompt.md`; both registered in `index.js` and both `.design-sync` maps.
    - `SkipLink` adds no CSS: `node -e "const s=require('fs').readFileSync('design-system/styles.css','utf8');const n=(s.match(/\/\* --- SkipLink/g)||[]).length;if(n!==0){console.error('SkipLink added a CSS block; .bhc-skip-link already exists');process.exit(1)}console.log('ok')"` prints `ok`.
    - `SkipLink` renders the shipped class and targets `#main`: rendering `SkipLink({})` produces markup containing `class="bhc-skip-link"` and `href="#main"`.
    - `Footer` emits no `<footer>`: `node --input-type=module -e "import {renderToStaticMarkup as r} from 'react-dom/server';import {Footer} from './design-system/src/components/Footer/Footer.jsx';const h=r(Footer({columns:[{heading:'Services',links:[{label:'Deep Cleaning',href:'/services/deep-cleaning'}]}],legal:[{label:'Privacy Policy',href:'/privacy-policy'}]}));const n=(h.match(/<footer/g)||[]).length;if(n!==1){console.error('expected exactly 1 footer (NAPFooter\\'s own), got',n);process.exit(1)}console.log('ok')"` prints `ok`.
    - `Footer` survives a malformed column: rendering with `columns: [null, { heading: 'Company' }]` does not throw and produces markup.
    - `Footer` drops a non-http `mapsUrl`: rendering with `mapsUrl: 'javascript:alert(1)'` produces markup containing no `javascript:`.
    - `grep -c "447861936533" design-system/src/components/Footer/Footer.jsx` returns 0.
    - The focus-override banner is still the last section of `styles.css`.
    - `npm run test:locks` exits 0.
  </acceptance_criteria>
  <done>`SkipLink` is a four-line wrapper over shipped CSS pointing at the layout's `<main id="main">`, and `Footer` supplies sitewide data into the one `NAPFooter` without adding a second landmark or defeating the scheme guard.</done>
</task>

<task type="auto">
  <name>Task 2: Header — the zero-JavaScript navigation</name>
  <files>design-system/src/components/Header/Header.jsx, design-system/src/components/Header/Header.html, design-system/src/components/Header/Header.d.ts, design-system/src/components/Header/Header.prompt.md, design-system/styles.css, design-system/src/index.js, design-system/.design-sync/config.json</files>
  <read_first>
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §7.1 IN FULL — the props, the rejected-routes table, the exact `<nav>`/`<details>`/`<ul>` markup sketch, the exact CSS sketch, the wordmark decision, and the nav data for Phase 2.
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "CSS: what survives Turbopack + lightningcss" — `.zz-nav__disclosure[open] ~ .zz-nav__list { display: block }` was authored, built and read back out of the emitted chunk unchanged. The mechanism is verified, not theorised. Also § "`::details-content` — the UI-SPEC's rejection stands" (Baseline Newly available Sept 2025, not Widely available until roughly March 2028).
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "Accessibility caveats" item 3 — a `<details>` that controls a sibling it does not contain is unusual and must be documented, or someone will later "tidy" it by moving the `<ul>` inside and silently break desktop nav.
    - design-system/src/components/NAPFooter/NAPFooter.jsx lines 89-107 — the guard idiom `Header` copies verbatim for its `NavItem[]` tree, which has the same 500-every-route blast radius.
    - design-system/src/components/NAPFooter/formatPhone.js and design-system/src/phone.js — the phone derivation and the canonical constant.
    - assets/logo/ — it holds only `beyond-hc-01.png` (189 KB) and a `.jpg`. Confirm before authoring; UI-SPEC §13-F rejects shipping it.
    - web/content/nav.js — the `NAV` tree plan 02-06 authored, which this component consumes.
  </read_first>
  <action>
Root `<header class="bhc-header">` containing exactly one `<nav aria-label="Primary" class="bhc-nav">`.
Group `Navigation`. Props per §7.1: `logo` (`ReactNode`), `nav` (`NavItem[]`), `cta`
(`{ label, href }`), `phone` (omit at call sites — defaults to `CANONICAL_PHONE`), `as`
(`ElementType`, default `'a'`), `className`.

**No `<h1>` in the header.** The brand is an `<a>` carrying
`aria-label="Beyond House Cleaning — home"`. A `<div>` or `<span>` styled at heading size is the
exact live-site bug that cost 101 of 115 pages their `<h1>`; if it looks like a heading it is a
heading element, and here it is neither.

**The logo is a typographic wordmark, not the raster file.** `assets/logo/` holds only a 189 KB PNG
of the mark `brand-brief.md` is replacing, and it renders about 20px tall in a 48px header. Shipping
it would put a superseded logo on roughly 410 pages. Set the brand in `--bhc-font-display` at
`--bhc-text-xl` / 700, `--bhc-ink` on `--bhc-paper`. The `logo` prop takes a `ReactNode` so the SVG
lockup drops in later with no API change and no page edits.

**The responsive mechanism — implement exactly this, and document why both alternatives were
rejected.** `<details>` is a STATE CARRIER WITH NO CONTENT, and the one `<ul>` is its SIBLING:

- `<nav aria-label="Primary" class="bhc-nav">` contains
  `<details class="bhc-nav__disclosure">` holding ONLY a
  `<summary class="bhc-nav__toggle" aria-controls="primary-nav" aria-label="Menu">` with an
  `aria-hidden="true" focusable="false"` SVG glyph, followed by a SIBLING
  `<ul class="bhc-nav__list" id="primary-nav">` rendered ONCE.
- CSS: `.bhc-nav__list { display: none; }`,
  `.bhc-nav__disclosure[open] ~ .bhc-nav__list { display: block; }`, and at `min-width: 1024px`
  `.bhc-nav__disclosure { display: none; }` with `.bhc-nav__list { display: flex; }`.

Rejected route 1 — putting the `<ul>` inside the `<details>` and hiding `<summary>` at desktop: a
closed `<details>`'s non-summary children are hidden by the UA through the details-content slot, not
by a `display` rule a stylesheet can override, so hiding the toggle leaves the nav permanently
hidden on desktop. Rejected route 2 — rendering a second desktop `<ul>`: it breaks the one
`<nav aria-label="Primary">` per page invariant and duplicates every nav link in the DOM, which
delta 6 then counts twice. Put the rejected-routes table in `Header.prompt.md` verbatim, with the
explicit warning that someone will later try to "tidy" the empty `<details>` by moving the `<ul>`
inside it. **Never rely on `::details-content`** — Baseline Newly available since September 2025,
not Widely available until around March 2028.

The **Services submenu is a nested `<details>` that DOES contain its own `<ul>`** — ordinary usage,
no hidden-content problem — absolutely positioned below its summary at 1024px and above. It needs no
`/services` index page (which 404s in Phase 2) and puts six keyword-bearing links in the global nav.

Desktop (1024px and above) additionally shows a `tel:` link labelled `+44 7861 936533`, derived via
`formatPhone(phone)` for the text and `toDial(phone)` for the href, plus the primary CTA
`Get a Free Quote` -> `/get-a-quote`.

Header height: 64px below 1024px, 72px at 1024px and above. Every nav link and the `<summary>` row
are at least 44px tall. Do NOT add a bespoke `:focus-visible` style — `<summary>` and `<a>` are both
already covered by the shipped base rule.

Copy the `NAPFooter` guard idiom for the nav tree: `.filter(Boolean)` on the array and
`(item.children || [])` before recursing. `Header` sits in the root layout, so a malformed entry in
a Phase 3 nav data file would 500 every route.

Preview uses Shape C: one real instance, since mobile versus desktop is entirely CSS-driven. Add a
caption naming the sibling-combinator mechanism.

Register in `index.js` and both `.design-sync` maps; add the `cardMode: "column"` override.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - The directory holds exactly the four canonical files; `@dsCard group="Navigation"` on line 1; `category: Navigation`; registered in `index.js` and both `.design-sync` maps.
    - Exactly one `<nav aria-label="Primary">`, one `<ul class="bhc-nav__list">`, no `<h1>`, and every link present once: `node --input-type=module -e "import {renderToStaticMarkup as r} from 'react-dom/server';import {Header} from './design-system/src/components/Header/Header.jsx';import {NAV} from './web/content/nav.js';const h=r(Header({nav:NAV,cta:{label:'Get a Free Quote',href:'/get-a-quote'}}));const c=re=>(h.match(re)||[]).length;if(c(/<nav aria-label=\"Primary\"/g)!==1){console.error('nav count');process.exit(1)}if(c(/class=\"bhc-nav__list\"/g)!==1){console.error('nav list rendered more than once');process.exit(1)}if(c(/<h1/g)!==0){console.error('header emits an h1');process.exit(1)}const hrefs=[...h.matchAll(/href=\"(\/[^\"]*)\"/g)].map(m=>m[1]);const dupes=hrefs.filter((x,i)=>hrefs.indexOf(x)!==i);if(dupes.length){console.error('duplicate nav hrefs in the DOM:',dupes);process.exit(1)}console.log('ok')"` prints `ok`.
    - The state carrier holds only a `<summary>`: the outer `<details class="bhc-nav__disclosure">` contains no `<ul>` — verify by inspecting the rendered markup and record the finding in the SUMMARY.
    - The sibling combinator is present in the stylesheet: `grep -c "bhc-nav__disclosure\[open\] ~ .bhc-nav__list" design-system/styles.css` returns 1.
    - `grep -c "details-content" design-system/styles.css` returns 0.
    - The desktop phone link is canonical and single-sourced: rendered markup contains `href="tel:+447861936533"` and the text `+44 7861 936533`, while `grep -c "447861936533" design-system/src/components/Header/Header.jsx` returns 0.
    - The brand is not the raster logo: `grep -c "beyond-hc-01" design-system/src/components/Header/Header.jsx` returns 0.
    - The menu glyph SVG carries `aria-hidden="true"` and no `alt` — `npm run test:locks` delta 14 passes.
    - `Header` survives a malformed nav entry: rendering with `nav: [null, { label: 'Services' }]` does not throw.
    - The focus-override banner is still the last section of `styles.css`; no new `:focus-visible` rule was added.
    - `npm run test:locks` exits 0.
  </acceptance_criteria>
  <done>One header, one primary nav, one nav list, every link in the DOM once, a disclosure that works with JavaScript disabled and is unconditionally visible at desktop width — and the rejected alternatives are documented so nobody re-introduces them.</done>
</task>

<task type="auto">
  <name>Task 3: StickyCallBar</name>
  <files>design-system/src/components/StickyCallBar/StickyCallBar.jsx, design-system/src/components/StickyCallBar/StickyCallBar.html, design-system/src/components/StickyCallBar/StickyCallBar.d.ts, design-system/src/components/StickyCallBar/StickyCallBar.prompt.md, design-system/styles.css, design-system/src/index.js, design-system/.design-sync/config.json</files>
  <read_first>
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §7.12 (props, dimensions, the `display: none` above 768px rule, the `body` padding rule), §5 (the CTA vocabulary: `Get a Quote` and `Call` with `aria-label="Call Beyond House Cleaning"`, and the rule that a label with digits must render exactly `+44 7861 936533`), §2 (the 44px minimum touch target).
    - .planning/phases/02-component-library-completion-core-templates/02-RESEARCH.md § "CSS: what survives Turbopack + lightningcss" — `height: calc(64px + env(safe-area-inset-bottom))` and `inset-inline: 0` both survive verbatim.
    - design-system/src/components/NAPFooter/NAPFooter.jsx lines 46-47 and 73-76 — the phone derivation idiom.
    - design-system/src/phone.js and design-system/src/components/NAPFooter/formatPhone.js.
    - design-system/styles.css — the shipped media-query idiom is `min-width` only. `StickyCallBar` is the ONE documented `max-width` exception (`max-width: 767px`), by UI-SPEC §7.12.
  </read_first>
  <action>
Root `<div class="bhc-callbar">`, group `Actions`. Props per §7.12: `quoteHref` (default
`/get-a-quote`), `phone` (omit at call sites — defaults to `CANONICAL_PHONE`), `as`, `className`.

Two 50/50 actions, both at least 44px tall:
- `Call` — `Button variant="secondary"`, href `` `tel:${toDial(phone)}` ``, carrying
  `aria-label="Call Beyond House Cleaning"`. **The visible label is the single word `Call` with NO
  digits.** A one-word link is ambiguous out of context for screen-reader users listing links, which
  is why the `aria-label` exists. It also matters for the lock: plan 02-02's delta 2(d) reads an
  anchor's rendered TEXT CONTENT, so a digit-free label is simply out of scope for that clause,
  while a label of `Call 07861 936533` would fail the digit-equality check because it normalises a
  leading `44` but not a leading `0`. If a digit-bearing label is ever wanted here, it must render
  exactly `+44 7861 936533`.
- `Get a Quote` — `Button variant="primary"`, href `quoteHref`. The short form is used because the
  bar is 375px wide; `Get a Free Quote` is the label everywhere else.

CSS: `position: fixed; inset-inline: 0; bottom: 0;`, height
`calc(64px + env(safe-area-inset-bottom))`, `--bhc-shadow-lg`, `--bhc-paper` ground with a
`--bhc-line` top border. Inside a single `@media (max-width: 767px)` block also set
`body { padding-bottom: 64px; }` so the bar never covers the footer's last row, and above 768px set
`display: none` — **removed from the accessibility tree, not merely visually hidden**, so desktop
keyboard users do not tab into an invisible bar.

Record in the `.prompt.md` that this is a deliberate competitive addition: cleaning enquiries are
mobile and phone-first, and the reference competitor has no equivalent. Also record that it is one
of the three `tel:` links a Phase 2 page carries (header, call bar, footer), which is exactly why
the page-wide "exactly one `tel:`" assertion had to be re-scoped to "exactly one inside `<footer>`,
at most three per page".

Preview uses Shape A: the bar rendered full-bleed at mobile width, then a caption explaining the
64px body padding and the desktop removal.

Register in `index.js` and both `.design-sync` maps; add the `cardMode: "column"` override.
  </action>
  <verify>
    <automated>npm run test:locks</automated>
  </verify>
  <acceptance_criteria>
    - The directory holds exactly the four canonical files; `@dsCard group="Actions"` on line 1; `category: Actions`; registered in `index.js` and both `.design-sync` maps.
    - The call action is digit-free and correctly labelled, and the href is canonical: `node --input-type=module -e "import {renderToStaticMarkup as r} from 'react-dom/server';import {StickyCallBar} from './design-system/src/components/StickyCallBar/StickyCallBar.jsx';const h=r(StickyCallBar({}));if(!h.includes('href=\"tel:+447861936533\"')){console.error('no canonical tel href');process.exit(1)}if(!h.includes('aria-label=\"Call Beyond House Cleaning\"')){console.error('missing aria-label');process.exit(1)}const m=h.match(/<a[^>]*href=\"tel:[^\"]*\"[^>]*>([\s\S]*?)<\/a>/);const text=(m?m[1]:'').replace(/<[^>]*>/g,'');if(/\d/.test(text)){console.error('call label contains digits:',text);process.exit(1)}if(!h.includes('/get-a-quote')){console.error('no quote action');process.exit(1)}console.log('ok')"` prints `ok`.
    - `grep -c "447861936533" design-system/src/components/StickyCallBar/StickyCallBar.jsx` returns 0 — the number comes from `phone.js`.
    - The CSS uses the documented exception exactly once and hides the bar above 768px: `grep -c "max-width: 767px" design-system/styles.css` returns 1, and the `StickyCallBar` block contains `display: none` inside a `min-width: 768px` query or its equivalent.
    - `grep -c "env(safe-area-inset-bottom)" design-system/styles.css` returns at least 1 and `grep -c "inset-inline: 0" design-system/styles.css` returns at least 1.
    - Both actions meet the 44px minimum — verify the CSS sets a `min-height` of at least 44px on the bar's action elements.
    - No literal hex in the new CSS block; the focus-override banner is still the last section of `styles.css`.
    - `npm run test:locks` exits 0, and delta 9 now passes over all SIXTEEN new component directories plus the six shipped ones — twenty-two in total.
  </acceptance_criteria>
  <done>All sixteen Phase 2 components exist. The call bar offers a digit-free, correctly labelled phone action whose href comes from the one canonical constant, and it disappears from the accessibility tree above 768px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `nav.js` data -> root layout -> every route | `Header` and `Footer` both sit in the root layout, so a malformed entry has a 17-route, later ~410-route, blast radius |
| `mapsUrl` data -> `NAPFooter` href | `Footer` is the pass-through; it must not defeat the scheme guard plan 02-01 added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-52 | Denial of Service | malformed `nav` or `columns` entry | mitigate | `.filter(Boolean)` plus `(children \|\| [])` and `(links \|\| [])` guards copied from `NAPFooter.jsx:89-107`, asserted by a render-with-null test in tasks 1 and 2 |
| T-02-53 | Tampering | `mapsUrl` scheme | mitigate | `Footer` passes the value through unmodified so `NAPFooter`'s `/^https?:\/\//i` guard applies; asserted by rendering a `javascript:` value and finding it absent (task 1) |
| T-02-54 | Spoofing | a second `<footer>` landmark | mitigate | `Footer` emits no landmark; asserted by counting `<footer>` in its rendered output (task 1) |
| T-02-55 | Elevation of privilege | a client boundary for the nav toggle | mitigate | UA-driven `<details>` plus a sibling combinator, both probe-verified; plan 02-02's delta 11 scans `design-system/src` for the client directive (task 2) |
| T-02-56 | Information disclosure | a wrong or unreachable phone number | mitigate | Header and StickyCallBar both default from `CANONICAL_PHONE`, derive href and text from the same value, and inherit `toDial`'s throw on malformed input (tasks 2 and 3) |
| T-02-57 | Information disclosure | a hidden but focusable call bar on desktop | mitigate | `display: none` above 768px removes it from the accessibility tree rather than only hiding it visually (task 3) |
| T-02-58 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run test:locks` green, with delta 9 passing over twenty-two component directories and the `previews.length` floor still at 6 (plan 02-14 raises it).
- `npm run build && npm run check:html && npm run check:budget` green. No app file renders these components yet — plan 02-13 composes them — so the built output is unchanged.
- `git diff design-system/package.json` is empty.
</verification>

<success_criteria>
- All sixteen Phase 2 components now exist in the four-file shape, registered in `index.js` and both `.design-sync` maps.
- `Header` renders one `<nav aria-label="Primary">`, one `<ul>`, no `<h1>`, and no duplicated href.
- The `[open] ~ .bhc-nav__list` sibling combinator is in the stylesheet and `::details-content` is not.
- `Footer` emits no `<footer>` element and does not defeat the `mapsUrl` scheme guard.
- `StickyCallBar`'s call action is digit-free with an `aria-label`, and its href is the canonical number from `phone.js`.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-10-SUMMARY.md` when done
</output>
