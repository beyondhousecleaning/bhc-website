---
phase: 02-component-library-completion-core-templates
plan: 11
type: execute
wave: 5
depends_on: ["02-02", "02-04", "02-05", "02-06", "02-07"]
files_modified:
  - web/content/home.js
  - web/app/page.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The home page renders the full Arbor-Trail section sequence with one h1 and one JSON-LD block"
    - "The before-and-after section shows a labelled placeholder rather than disappearing"
    - "Six keyword-bearing service links sit on the home page"
    - "No two adjacent sections share a surface tone and there is exactly one navy band"
  artifacts:
    - path: "web/app/page.jsx"
      provides: "the composed Home template — ROADMAP SC-2's home page"
    - path: "web/content/home.js"
      provides: "home hero copy, the six service card records and the home FAQ set"
  key_links:
    - from: "web/app/page.jsx"
      to: "the six /services/* routes"
      via: "ServiceCard hrefs, which plan 02-12 makes resolvable in this same wave"
      pattern: "/services/"
---

<objective>
Replace plan 02-02's trimmed scaffold with the real Home page: the full section sequence from
UI-SPEC §9.2, composed from the sixteen components, driven by a `web/content/home.js` data module.

Purpose: this is the last of ROADMAP Success Criterion 2's non-programmatic pages and the site's
highest-traffic template. It is also where three of the phase's four Success Criteria become
visible at once — the component library (SC-1), the single `<h1>` (SC-2), and the
`BeforeAfterSlider` placeholder (SC-3).

Output: one data module and one rewritten route file.

**Wave-mate dependency, stated explicitly:** the six `ServiceCard` hrefs point at `/services/<slug>`
routes that plan 02-12 creates IN THIS SAME WAVE. Plan 02-02's delta 6 asserts every internal href
resolves, so `npm run check:html` only goes green once both plans have landed. Run the full verify
at the end of the wave, not after this plan alone, and record that in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md
@.planning/phases/02-component-library-completion-core-templates/02-PATTERNS.md
@.planning/phases/02-component-library-completion-core-templates/02-07-SUMMARY.md
@.planning/phases/02-component-library-completion-core-templates/02-06-SUMMARY.md

<home_template_contract>
<!-- UI-SPEC §9.2 section order, with §4's band-ordering tones. This is Arbor Trail's proven
     sequence with orange actions and warm neutrals rather than their cold blue. -->

  Hero               paper-warm   (shipped default)   <- the ONE <h1>, with the rating
  TrustBar           paper
  ServiceCard x6     paper-warm   cards on paper
  BeforeAfterSlider  paper                            <- placeholder state, SC-3
  ProcessSteps       paper-tint
  ReviewRail         paper        cards on paper-tint <- returns null in Phase 2
  FAQAccordion       paper
  CTABand            navy         <- the one dark band, immediately above the footer

Never two identical tones adjacent. Never two navy bands on one page.
NO Breadcrumbs on `/` — UI-SPEC §9.2, and plan 02-02's PAGE_EXPECTATIONS sets
`hasBreadcrumbs: false, ldJsonBlocks: 1` for this route permanently.

The locations teaser (`InterlinkBlock` variant `nearby`) is PHASE 3. Do not add it — plan 02-02's
SC-4d gate asserts no Phase 2 page renders `bhc-interlink__list`.

Hero copy, from UI-SPEC §5, required CHARACTER FOR CHARACTER (plan 02-02's PAGE_EXPECTATIONS
already holds the h1):
  eyebrow  DBS-checked, insured, local
  h1       Professional House Cleaning in Warwickshire & the West Midlands
  lead     The same trusted cleaner each visit, a fixed price before we start, and an evening back to yourself.

Section headings, from UI-SPEC §5's <h2> deck:
  TrustBar           Why people book us again
  ServiceCard grid   What we clean
  BeforeAfterSlider  The difference, on real jobs
  ProcessSteps       How it works
  ReviewRail         What Warwickshire customers say
  FAQAccordion       Questions we get asked
  CTABand            Ready for a properly clean home?

Metadata: title `House Cleaning in Warwickshire | Beyond House Cleaning` (54 chars), description
from `describe()` in `@/content/site.js`.

COUNTING RULE for every criterion in this plan that counts a `bhc-` class in BUILT HTML: use the
`class="[^"]*bhc-…` form. A bare grep DOUBLE-COUNTS, because the RSC flight payload is inlined in
the HTML and serialises the same value as `\"className\":\"bhc-…\"` — measured at 2 vs 1 during
planning. Zero-assertions are safe in either form.
</home_template_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/content/home.js</name>
  <files>web/content/home.js</files>
  <read_first>
    - web/content/site.js — `RATING`, `titleFor`, `describe`, `AREA_LONG`. Reuse them.
    - web/content/faqs.js — `GENERAL_FAQS`, the FAQ set the home page renders.
    - web/content/process.js — `PROCESS_STEPS`, rendered as-is.
    - web/content/nav.js — the six service hrefs and labels, so the card records and the nav cannot drift. Note the `Services` item carries its children under `children` and has no `href` of its own.
    - design-system/src/components/ServiceCard/ServiceCard.d.ts — the `title`, `href`, `summary`, `includes` prop shape the card records must match.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §5 (voice table, the `<h2>` deck, the CTA vocabulary closed set), §7.6.
    - This plan's `<home_template_contract>` block.
  </read_first>
  <action>
Create `web/content/home.js` as a plain `.js` module with a `geo.js`-style header comment.

Export:
- `HOME_HERO` — `{ eyebrow, heading, lead }`, character for character from the contract block above.
- `HOME_SERVICE_CARDS` — six records matching the `ServiceCardProps` shape: `title`, `href`,
  `summary` (one or two sentences), and `includes` (up to THREE bullets — §7.6 caps it). Titles use
  the UK service nouns: Deep Cleaning, Regular House Cleaning, Move-In Cleaning, Move-Out Cleaning,
  Short-Term Rental Cleaning, Builders Clean. Hrefs are the six live slugs from `nav.js` —
  **import them from `nav.js` rather than restating them**, reading them off the `Services` item's
  `children`, so the cards and the navigation cannot drift apart.
- `HOME_INTRO` — the one-paragraph `intro` for the `What we clean` band.
- `HOME_FAQS` — either a re-export of `GENERAL_FAQS` or a home-specific subset of four to six items.

Voice per UI-SPEC §5: plain UK English, contractions, concrete nouns ("skirting boards to oven
interiors", not "comprehensive cleaning solutions"), named towns rather than "your local area". UK
vocabulary only — `tenancy`, `flat`, `skirting boards`, `hoover`, `builders clean`; never
`apartment`, `baseboards`, `vacuum`, or `post-construction` in body copy (the URL slug keeps its
existing spelling per UI-SPEC §13-C, but the visible noun is `Builders Clean`).

CTA labels come from §5's CLOSED set only: `Get a Free Quote` -> `/get-a-quote` and
`See What's Included` -> `/checklist`. Never "Submit", "Click here" or "Learn more". Do NOT add
`Read Our Reviews` -> `#reviews`: `ReviewRail` returns `null` without data, so that anchor
dead-ends on every page, and delta 6 only checks `href^="/"` so nothing would catch it. It returns
in Phase 4 with the data.

Same prohibitions as every other content module: no street address, no UK postcode, no phone number,
no email address anywhere in this file.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import {HOME_HERO,HOME_SERVICE_CARDS} from './web/content/home.js';import {NAV} from './web/content/nav.js';if(HOME_HERO.heading!=='Professional House Cleaning in Warwickshire & the West Midlands'){console.error('h1 mismatch:',HOME_HERO.heading);process.exit(1)}if(HOME_HERO.eyebrow!=='DBS-checked, insured, local'){console.error('eyebrow mismatch');process.exit(1)}if(HOME_SERVICE_CARDS.length!==6){console.error('expected 6 cards, got',HOME_SERVICE_CARDS.length);process.exit(1)}const navHrefs=new Set((NAV.find(i=>i.label==='Services').children||[]).map(c=>c.href));for(const c of HOME_SERVICE_CARDS){if(!navHrefs.has(c.href)){console.error('card href not in nav:',c.href);process.exit(1)}if((c.includes||[]).length>3){console.error('more than 3 includes on',c.title);process.exit(1)}}console.log('ok')"` prints `ok`.
    - No dead-end anchor: `grep -c "#reviews" web/content/home.js` returns 0.
    - No banned CTA copy: `grep -ci "click here\|learn more\|submit\|read our reviews" web/content/home.js` returns 0.
    - UK vocabulary: `grep -ci "apartment\|baseboard\|vacuum" web/content/home.js` returns 0.
    - No address, postcode, phone or email — the same node check as plan 02-09 task 1, applied to this file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Home copy exists as plain data, six card records whose hrefs are read off the nav rather than restated, and no dead-end anchor or banned CTA label.</done>
</task>

<task type="auto">
  <name>Task 2: Compose web/app/page.jsx</name>
  <files>web/app/page.jsx</files>
  <read_first>
    - web/app/page.jsx — the trimmed scaffold from plan 02-02. Its structure, its `export const metadata`, and lines 11-15's convention of not naming the client directive in prose because the CI gate is a substring grep over `web/app`. The scaffold's single `Get a Free Quote` hero action is extended in this task.
    - web/app/layout.jsx — the landmarks live here; the page renders a top-level fragment.
    - design-system/.design-sync/conventions.md § "An idiomatic composition" — the composition idiom this page follows.
    - The `.d.ts` files for `SectionBand`, `TrustBar`, `ServiceCard`, `BeforeAfterSlider`, `ProcessSteps`, `ReviewRail`, `FAQAccordion` and `CTABand` — the exact prop names, since this page uses all eight.
    - .planning/phases/02-component-library-completion-core-templates/02-UI-SPEC.md §9.2 (section order), §4 (the band-ordering block and the rule that `RatingBadge` is light-surface only), §5 (the `<h2>` deck).
    - This plan's `<home_template_contract>` block, including its counting rule.
  </read_first>
  <action>
Rewrite `web/app/page.jsx` to compose the full sequence in the contract block's order and tones.
Everything is driven from `@/content/home.js`, `@/content/process.js`, `@/content/faqs.js` and
`@/content/site.js` — no copy literals in the `.jsx`, both because the components are
framework-agnostic and because the `claude-seo` hook rejects a `.jsx` write containing the substring
`REPLACE`, which ordinary prose contains.

Composition notes:
- `Hero` takes `eyebrow`, `heading`, `lead`, `rating={RATING}` and `actions` — one `primary`
  `Get a Free Quote` -> `/get-a-quote` and one `secondary` `See What's Included` -> `/checklist`.
  **No `emitSchema`** anywhere on this page: plan 02-02's SC-4f replacement asserts exactly ONE
  JSON-LD block on `/`, and a second turns CI red.
- Wrap each section in `SectionBand` with the tone from the contract block. Alternate strictly;
  never place two identical tones adjacent, and never a second navy band.
- The six `ServiceCard`s render inside a `SectionBand tone="warm"` with heading `What we clean`.
  Cards take `headingLevel={3}` so the outline under the band's `<h2>` stays legal.
- `BeforeAfterSlider` is rendered with NO `pairs` prop. That is deliberate and it is ROADMAP SC-3:
  it renders `data-bhc-photo-state="pending"` with the labelled placeholder rather than disappearing.
  Add a comment saying so, and naming Phase 4 as the plan that supplies real pairs.
- `ReviewRail` is rendered with `reviews={[]}`, so it returns `null` in Phase 2. Add a comment
  saying that is expected and that Phase 4 supplies the data. It costs nothing and means Phase 4
  changes a data file rather than this template.
- `FAQAccordion` takes `HOME_FAQS`. It emits no structured data — ever.
- `CTABand` is the last section before the footer, with heading `Ready for a properly clean home?`,
  a `primary` action to `/get-a-quote` and a `ghost` action to `/checklist`. **`ghost`, not
  `secondary`** — on navy the outlined-ink secondary disappears.
- **No `RatingBadge` inside `CTABand`.** The badge sets `--bhc-ink` directly rather than inheriting
  `currentColor`, so it is light-surface only and illegible on navy. The `Hero` badge is the only
  one on the page.
- **No `Breadcrumbs`** and **no `InterlinkBlock`**.

Keep the single barrel import from `@bhc/design-system`, the top-level fragment, and
`export const metadata = { title, description }`. Never write the phone number in `web/app`.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('web/.next/server/app/index.html','utf8');const c=r=>(h.match(r)||[]).length;const checks=[['h1',/<h1/g,1],['ldjson',/<script[^>]*type=\"application\/ld\+json\"/g,1],['crumbs',/class=\"[^\"]*bhc-breadcrumbs__list/g,0],['interlink',/class=\"[^\"]*bhc-interlink__list/g,0],['navy bands',/class=\"[^\"]*bhc-section--navy/g,1],['photo pending',/data-bhc-photo-state=\"pending\"/g,1]];let bad=0;for(const [n,re,want] of checks){const got=c(re);if(got!==want){console.error(n,'expected',want,'got',got);bad=1}}if(/FAQPage/.test(h)){console.error('FAQ schema present');bad=1}process.exit(bad)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Built `index.html` has exactly one `<h1`, and its text — extracted with the tag-stripping form `/<h1[^>]*>([\s\S]*?)<\/h1>/` and with `&amp;` decoded — is `Professional House Cleaning in Warwickshire & the West Midlands`.
    - Built `index.html` has exactly ONE `<script type="application/ld+json"` block, zero `class="…bhc-breadcrumbs__list"`, zero `class="…bhc-interlink__list"`, exactly one `class="…bhc-section--navy"`, and one `data-bhc-photo-state="pending"`. **All four class counts use the `class="` form** — the bare form double-counts on the inlined flight payload.
    - `FAQPage` appears zero times in built `index.html`.
    - All six service links are present and distinct: `node -e "const fs=require('fs');const h=fs.readFileSync('web/.next/server/app/index.html','utf8');const s=new Set([...h.matchAll(/href=\"(\/services\/[a-z-]+)\"/g)].map(m=>m[1]));if(s.size!==6){console.error('expected 6 distinct service links, got',s.size,[...s]);process.exit(1)}console.log('ok')"` prints `ok`.
    - No rating badge on the navy band, checked against the MARKUP HALF ONLY (everything before the first `self.__next_f` script, since the flight payload repeats every class name): `node -e "const fs=require('fs');const full=fs.readFileSync('web/.next/server/app/index.html','utf8');const markup=full.slice(0, full.indexOf('self.__next_f')>-1?full.indexOf('self.__next_f'):full.length);const i=markup.search(/class=\"[^\"]*bhc-section--navy/);if(i<0){console.error('no navy band in markup');process.exit(1)}const end=markup.indexOf('</section>', i);const band=markup.slice(i, end<0?markup.length:end);if(/bhc-rating/.test(band)){console.error('RatingBadge inside the navy CTABand — it is light-surface only');process.exit(1)}console.log('ok')"` prints `ok`.
    - Adjacent bands never share a tone — extract the ordered list of `bhc-section` modifier classes from the MARKUP HALF (same slice as above) and confirm no two consecutive entries are equal. Record the observed order in the SUMMARY.
    - `grep -rc "447861936533" web/app` reports 0 for every file, and `grep -rc "emitSchema" web/app` reports 0 for every file.
    - `npm run check:html` is green ONCE plan 02-12 has landed in this same wave; if it is run before that, the only permitted failure is delta 6 on the six `/services/*` hrefs. Record which state was observed.
  </acceptance_criteria>
  <done>The home page renders the full section sequence with one `<h1>`, one JSON-LD block, one navy band, six service links, a visible before-and-after placeholder, and no breadcrumbs, interlink block or FAQ schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `home.js` copy -> built HTML | The highest-traffic page, and the largest single block of authored prose outside the service pages |
| card hrefs -> internal navigation | Six links whose targets are created by a wave-mate plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-63 | Information disclosure | address or postcode in home copy | mitigate | Source-level checks in task 1 plus plan 02-02's `SC-2d` postcode and street-line scan over built HTML |
| T-02-64 | Tampering | a second JSON-LD block on `/` | mitigate | No `emitSchema` anywhere; plan 02-02's SC-4f replacement asserts exactly one block on `/` and the acceptance criteria check it directly |
| T-02-65 | Tampering | a dead internal link | mitigate | Card hrefs are read off `nav.js` rather than restated, and delta 6 asserts resolution once plan 02-12 lands in the same wave |
| T-02-66 | Information disclosure | an illegible rating badge on the navy band | mitigate | `RatingBadge` appears only in `Hero`, asserted against the markup half of the built HTML so the flight payload cannot mask it |
| T-02-67 | Tampering | FAQ structured data | mitigate | `FAQAccordion` emits none by construction, and plan 02-02's delta 7 greps every built page for zero occurrences |
| T-02-68 | Repudiation | a class count silently doubled by the flight payload | mitigate | Every class count in this plan uses the `class="` form, and the two positional checks are scoped to the markup half explicitly |
| T-02-69 | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages this phase |
</threat_model>

<verification>
- `npm run verify` green at the END of wave 5, once plan 02-12 has also landed.
- The prerender manifest holds 19 route keys: 18 app routes plus `/_global-error`.
- SC-3 is demonstrably satisfied: `data-bhc-photo-state="pending"` renders on `/`.
</verification>

<success_criteria>
- `/` renders the full UI-SPEC §9.2 sequence with the exact §5 copy.
- Exactly one `<h1>`, one JSON-LD block, one navy band, and no breadcrumbs or interlink block.
- Six distinct `/services/*` links, read off the nav data rather than restated.
- `BeforeAfterSlider` renders its labelled placeholder, satisfying ROADMAP SC-3.
- No page copy lives in the `.jsx`, and no phone number appears anywhere under `web/app`.
</success_criteria>

<output>
Create `.planning/phases/02-component-library-completion-core-templates/02-11-SUMMARY.md` when done
</output>
