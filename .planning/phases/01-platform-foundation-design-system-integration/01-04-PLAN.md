---
phase: 01-platform-foundation-design-system-integration
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - design-system/src/components/NAPFooter/NAPFooter.jsx
  - web/scripts/check-html-locks.mjs
autonomous: false
requirements: [REQ-nap-consistency]

must_haves:
  truths:
    - "D-01: a Next.js app built from design-system/tokens.css is deployed to Vercel and reachable at a live URL"
    - "D-03: the deployment is a Vercel-assigned URL only — www.beyondhousecleaning.com is still served by Webflow and was never touched"
    - "D-15: the served deployment is not indexable — the live /robots.txt disallows all crawling and every page carries a noindex meta tag"
    - "D-10: the served HTML contains no UK postcode, no streetAddress field and no address JSON key — both halves of the no-address rule"
    - "D-09: the canonical number +447861936533 is confirmed by a human as the number that actually reaches BHC, or a replacement is recorded"
    - "SC-4: the live URL serves the design-system components in its HTML response, without running JavaScript"
  artifacts:
    - path: "design-system/src/components/NAPFooter/NAPFooter.jsx"
      provides: "the canonical phone number as the sole package default (line 33) — edited ONLY if Sam supplies a different number in Task 3"
      contains: "phone = '+44"
    - path: "web/scripts/check-html-locks.mjs"
      provides: "the retired-number assertion list — extended ONLY if Sam supplies a different number, so the superseded number can never reappear"
      contains: "07441918832"
  key_links:
    - from: "Vercel project"
      to: "web/"
      via: "Root Directory setting = web, Node 22"
      pattern: "Root Directory"
    - from: "live deployment URL"
      to: "web/.next/server/app/index.html"
      via: "curl of the deployed HTML matching the locally built HTML"
      pattern: "bhc-hero__heading"
---

<objective>
Close the two Phase 1 items that no agent can do: connect the Vercel project, and confirm the
canonical phone number with a human.

Purpose: Success Criterion 1 — "deployed to Vercel and reachable at a live URL" — is the phase's one
hard external dependency. No Vercel project exists for this repo, and creating or authorising one
requires Sam's account. Every other Phase 1 criterion is verifiable locally against the built HTML
on disk, which is exactly why this plan is isolated: nothing else in the phase waits on it.

Output: a live Vercel deployment URL, verified from the outside to serve the design-system
components and to refuse indexing, plus a recorded human confirmation of the canonical number.

**Scheduling — why wave 4 and not wave 3.** Task 3 has a conditional write path: if Sam supplies a
different phone number, it edits `NAPFooter.jsx:33` *and* extends the retired-number list in
`web/scripts/check-html-locks.mjs`, a file 01-03 authors. Running this plan alongside 01-03 would
put two concurrent writers on that file, and a lost edit there means CI goes green on a wrong phone
number — the exact failure this phase exists to prevent. Task 3 also invokes `npm run verify`, a
root script 01-03 Task 3 creates. Both are hard dependencies, so this plan declares
`depends_on: ["01-02", "01-03"]` and both files in `files_modified`.

The property this plan was isolated to protect still holds: **01-05 depends on 01-03, not on 01-04.**
No autonomous work waits on the Vercel human gate — 01-05 can prove and bind the CI gate while this
plan sits pending Sam. 01-04 and 01-05 share wave 4 and have zero file overlap: 01-05 touches only
`design-system/test/__ci-gate-probe.test.js`, a transient probe file it creates and deletes within
one task.

Hard boundary: nothing in this plan may take the live site down. `www.beyondhousecleaning.com` stays
pointed at Webflow until a later cutover phase that no phase currently owns (D-03). Sam must not
add a custom domain to the Vercel project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md
@.planning/phases/01-platform-foundation-design-system-integration/01-RESEARCH.md
@.planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md
@.planning/phases/01-platform-foundation-design-system-integration/01-02-SUMMARY.md

<interfaces>
<!-- Settings and expected values. No codebase exploration required. -->

Repository:            github.com/beyondhousecleaning/bhc-website
Vercel Root Directory: web           (the Next.js app package; NOT the repo root)
Vercel Framework:      Next.js       (auto-detected)
Vercel Node version:   22            (Next.js 16 requires >= 20.9.0; local dev is 22.23.1)
Vercel Build Command:  leave at the default `next build` — do NOT chain the lock tests into it.
                       Vercel's Root Directory sandbox cannot use `..` to reach the sibling
                       design-system package, and the gate lives in GitHub Actions (Plan 03).
Vercel Install:        auto-detected as npm from the committed root package-lock.json; install runs
                       from the workspace root, which is what links @bhc/design-system
Custom domain:         NONE. Do not add one. D-03.

Strings the deployed HTML must contain (identical to the local build output):
  bhc-hero__heading · bhc-breadcrumbs__list · bhc-rating__value · bhc-interlink__list
  bhc-btn--primary  · class="bhc-footer__phone" · href="tel:+447861936533"
  a name="robots" tag whose content includes noindex
  exactly 3 × <script type="application/ld+json">

Phone numbers:
  canonical  +447861936533   NAPFooter package default; what the live Webflow site DISPLAYS
  retired    07441918832     what the live Webflow footer actually DIALS on all 115 pages
  retired    +447575709361   a third number on /get-a-quote
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Sam connects the Vercel project</name>

  <files>none — this is an external account action</files>

  <read_first>
    - .planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md — Open Question 3,
      which records that this is the phase's one true external dependency
    - .planning/phases/01-platform-foundation-design-system-integration/01-RESEARCH.md —
      § Environment Availability ("Missing dependencies with no fallback") and Pitfall 4, which is
      why the Build Command must stay at its default
    - .planning/phases/01-platform-foundation-design-system-integration/01-02-SUMMARY.md — confirm
      the app builds locally before asking Sam to do anything
  </read_first>

  <action>
    This is a blocking human gate — do not attempt it autonomously. There is no Vercel CLI installed
    and no Vercel project for this repo; creating one requires an account action Sam alone can take.

    Before pausing, do the automatable preparation. Confirm the branch is pushed to
    `github.com/beyondhousecleaning/bhc-website`. Confirm `npm ci && npm run build --workspace @bhc/web`
    still succeeds from a clean checkout, so a failed Vercel build can be attributed to project
    settings rather than to the code. Capture the current DNS resolution of
    `www.beyondhousecleaning.com` to `/tmp/bhc-apex-baseline.txt`, so D-03 compliance can afterwards
    be proven by comparison against a recorded baseline rather than by assertion.

    Then present the `<how-to-verify>` steps to Sam verbatim and stop. The four settings that matter
    are Root Directory `web`, Node.js version 22, the framework-default Build Command, and no custom
    domain — all four are recorded in this plan's `<interfaces>` block.

    Record Sam's answers in the summary: the deployment URL, and whether Vercel Deployment Protection
    is enabled. The latter determines whether Task 2's curl needs an `x-vercel-protection-bypass`
    header, and a 401 from an unauthenticated curl must not be misread as a broken deployment.

    Export the deployment URL as `DEPLOY_URL` for Task 2. Do not proceed to Task 2 without it.
  </action>

  <verify>
    <automated>git log --oneline -1 "origin/$(git branch --show-current)"</automated>
    <automated>dig +short www.beyondhousecleaning.com | tee /tmp/bhc-apex-baseline.txt</automated>
  </verify>

  <what-built>
    Everything Vercel needs is already in the repo and pushed: a two-package npm workspace with a
    committed root `package-lock.json` (which is how Vercel auto-detects npm and installs from the
    workspace root), a `web/` Next.js 16 app that builds in seconds with no design-system build step,
    a root layout carrying a `noindex, nofollow` robots meta tag, and a `web/public/robots.txt`
    disallowing all crawling. `npm ci && npm run build --workspace @bhc/web` has been verified to
    succeed from a clean checkout.

    There is no Vercel CLI installed and no Vercel project for this repo. Creating one requires an
    account action that cannot be automated.
  </what-built>

  <how-to-verify>
    1. Go to vercel.com and create a new project, importing `beyondhousecleaning/bhc-website`.
    2. In the project's build settings, set **Root Directory** to `web`. This is the one setting that
       must not be left at its default — the repo root is a workspace root, not the app.
    3. Leave **Framework Preset** as the auto-detected Next.js, and leave **Build Command**,
       **Install Command** and **Output Directory** at their defaults. Do not chain the lock tests
       into the Vercel build; the gate is GitHub Actions.
    4. Set the **Node.js Version** to **22**.
    5. Do **NOT** add a custom domain. `www.beyondhousecleaning.com` must stay pointed at Webflow —
       Phase 1 ships to a Vercel-assigned URL only, and nothing here may take the live site down.
    6. Deploy the branch `worktree-phase-1-platform-foundation` (or `main`, if the phase branch has
       already merged).
    7. Paste back the deployment URL, and confirm whether Vercel **Deployment Protection** is on or
       off for this project — that affects whether the verification in Task 2 needs a bypass token.

    Expected result: the deployment build log shows npm installing from the workspace root and
    `next build` prerendering the `/` route as static content, in well under a minute.
  </how-to-verify>

  <acceptance_criteria>
    - Sam supplies a deployment URL of the form `https://<something>.vercel.app`
    - The Vercel project's Root Directory is `web` and its Node.js version is 22
    - The Vercel project has no custom domain attached
    - The Vercel Build Command is the framework default, not a custom command containing `..`
    - `dig +short www.beyondhousecleaning.com` still resolves to the Webflow infrastructure it
      resolved to before this plan started — the live site is untouched
  </acceptance_criteria>

  <resume-signal>
    Paste the deployment URL and state whether Deployment Protection is enabled, or describe the
    error if the build failed.
  </resume-signal>

  <done>
    A Vercel project exists for this repo with Root Directory `web` and Node 22, a deployment has
    completed, and its URL is known.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the live deployment from the outside</name>

  <files>none — read-only verification against the deployment URL</files>

  <read_first>
    - web/.next/server/app/index.html — the locally built HTML. The deployed response must contain
      the same design-system strings; comparing against the local build is what makes this a real
      equivalence check rather than a smoke test
    - web/public/robots.txt — the crawl block that must actually be served, not merely present on disk
    - .planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md — rows SC-1c
      and D-15, which define exactly what this task proves
  </read_first>

  <action>
    Using the deployment URL Sam supplied in Task 1, verify from outside the build that the live
    response carries the design system and refuses indexing. Everything here is a plain `curl` — no
    browser, no headless runner, no JavaScript execution. That is deliberate: if these strings are
    present in the raw HTTP response body, the output is genuinely server-rendered, which is the
    property CI Locks 3 and 9 exist to guarantee.

    If Sam reported that Vercel Deployment Protection is enabled, the URL will return a 401 to an
    unauthenticated `curl`. In that case, ask Sam for a Protection Bypass token and send it as the
    `x-vercel-protection-bypass` header, or ask Sam to disable protection for this project. Do not
    conclude the deployment is broken from a 401 — record which case applied.

    Verify, in order:
      - the URL returns HTTP 200
      - the response body contains `bhc-hero__heading`, `bhc-breadcrumbs__list`,
        `bhc-rating__value`, `bhc-interlink__list`, `bhc-btn--primary` and `bhc-footer__phone`
      - the body contains exactly one `href="tel:` occurrence, and its digits equal the digits of the
        `bhc-footer__phone` label text
      - the body contains none of `07441918832`, `447441918832`, `447575709361`
      - the body contains exactly three `<script type="application/ld+json"` occurrences
      - the body contains a `name="robots"` tag whose content includes `noindex` (D-15, meta level)
      - the UK postcode regex `/\b[A-Z]{1,2}\d{1,2}[A-Z]?\s?\d[A-Z]{2}\b/` does not match the body,
        AND the body contains neither `streetAddress` nor an `address` JSON key — D-10 forbids a
        street address as well as a postcode, and the postcode regex alone would pass either one
      - `<deployment-url>/robots.txt` returns 200 and its body, ignoring `#` comment lines, contains
        `User-agent: *` and `Disallow: /` (D-15, host level)

    Finally, confirm the live site is untouched: resolve `www.beyondhousecleaning.com` and confirm it
    is still served by Webflow, not by Vercel (D-03). Record the result either way.
  </action>

  <verify>
    <automated>curl -sS -o /tmp/bhc-live.html -w "%{http_code}\n" "$DEPLOY_URL" | grep -q '^200$' && echo "deployment reachable"</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const h=readFileSync('/tmp/bhc-live.html','utf8'); const digits=(s)=>String(s).replace(/\D/g,''); for(const s of ['bhc-hero__heading','bhc-breadcrumbs__list','bhc-rating__value','bhc-interlink__list','bhc-btn--primary','bhc-footer__phone']) if(!h.includes(s)) throw new Error('deployed HTML missing: '+s); const tels=h.match(/href=\"tel:[^\"]+\"/g)||[]; if(tels.length!==1) throw new Error('expected 1 tel: link live, found '+tels.length); const href=tels[0].match(/tel:([^\"]+)/)[1]; const label=h.match(/class=\"bhc-footer__phone\"[^>]*>([^<]+)</)[1]; if(digits(href).replace(/^44/,'')!==digits(label).replace(/^44/,'')) throw new Error('live href '+href+' != label '+label); for(const bad of ['07441918832','447441918832','447575709361']) if(h.includes(bad)) throw new Error('retired number live: '+bad); const ld=h.match(/<script type=\"application\\/ld\\+json\"/g)||[]; if(ld.length!==3) throw new Error('expected 3 JSON-LD blocks live, found '+ld.length); if(!/name=\"robots\"[^>]*noindex|noindex[^>]*name=\"robots\"/.test(h)) throw new Error('D-15: no noindex meta tag in the live response'); const m=h.match(/\b[A-Z]{1,2}\d{1,2}[A-Z]?\s?\d[A-Z]{2}\b/); if(m) throw new Error('UK postcode in live HTML: '+m[0]); if(h.includes('streetAddress')) throw new Error('D-10: streetAddress present in the live HTML'); if(/\"address\"\s*:/.test(h)) throw new Error('D-10: an address key is present in the live HTML'); console.log('live HTML OK');"</automated>
    <automated>curl -sS "$DEPLOY_URL/robots.txt" | grep -v '^#' | grep -q 'Disallow: /' && echo "D-15 crawl block served"</automated>
    <automated>curl -sSI "https://www.beyondhousecleaning.com" | head -1 && dig +short www.beyondhousecleaning.com | head -3</automated>
  </verify>

  <acceptance_criteria>
    - `curl -o /dev/null -w '%{http_code}' "$DEPLOY_URL"` prints `200`
    - The raw response body (no JavaScript executed) contains all six design-system class-name
      markers listed in the action
    - The body contains exactly one `href="tel:` and its digits, leading `44` stripped, equal the
      digits of the `bhc-footer__phone` label text
    - The body contains none of `07441918832`, `447441918832`, `447575709361`
    - The body contains exactly three `<script type="application/ld+json"` occurrences
    - The body contains a `robots` meta tag whose content includes `noindex`
    - The UK postcode regex does not match the body, and the body contains neither `streetAddress`
      nor an `address` JSON key
    - `$DEPLOY_URL/robots.txt` returns 200 and, ignoring comment lines, contains `Disallow: /`
    - `dig +short www.beyondhousecleaning.com` does not resolve to any Vercel address — the live
      Webflow site is untouched
  </acceptance_criteria>

  <done>
    The Vercel deployment serves the design-system components in server-rendered HTML with a single
    correct `tel:` link and no postcode, refuses indexing at both the meta and robots.txt level, and
    the production domain is unaffected.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sam confirms the canonical phone number</name>

  <files>none — a factual confirmation only</files>

  <read_first>
    - design-system/src/components/NAPFooter/NAPFooter.jsx — line 33, where the canonical number
      lives as the package default and the only place it appears
    - design-system/src/components/NAPFooter/formatPhone.js — the mechanism: the displayed string is
      derived from the dial string, so there is no prop for displayed text and the two cannot diverge
    - .planning/REQUIREMENTS.md — REQ-nap-consistency, the requirement this closes
    - .planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md — Open Question 1
  </read_first>

  <action>
    This is a blocking human gate. Code cannot determine which phone number actually reaches Beyond
    House Cleaning — the live Webflow site has been displaying one number and dialling a different
    one on all 115 pages, so the code is not evidence of what customers have been reaching.

    Before pausing, confirm the current state so the question is precise: assert that
    `+447861936533` is the `phone` default at `NAPFooter.jsx:33`, that no `phone` prop is passed
    anywhere under `web/app`, and that the built HTML dials exactly that number.

    Then present the `<how-to-verify>` steps to Sam and stop.

    If Sam confirms, record "confirmed" in the summary and in STATE.md's Accumulated Context and stop
    — no code change.

    If Sam supplies a different number, this task takes its conditional write path and touches
    exactly two files, both declared in this plan's `files_modified`:

      1. `design-system/src/components/NAPFooter/NAPFooter.jsx` — update the `phone` default at
         line 33 to the new value in full international form. This is a single-line change and it is
         the ONLY place the number may live. Do NOT add a `phone` prop in `web/app/layout.jsx`, and
         do NOT introduce a constant, env var, or config entry for it anywhere in `web/` — any of
         those would create the second source of truth this requirement exists to eliminate.
      2. `web/scripts/check-html-locks.mjs` — add `+447861936533` (and its bare-digit form
         `447861936533`) to the retired-number assertion list, so the now-superseded number can
         never reappear in built output. This file is authored by 01-03; because this plan runs in
         wave 4 and declares `depends_on: ["01-03"]`, that file is final and unowned by any
         concurrent plan by the time this task runs. Extend the existing list — do not rewrite the
         file or restructure its assertions.

    Then rebuild and run `npm run verify` (the root script created by 01-03 Task 3), and commit the
    change with a message naming the number that was retired and the human confirmation that
    prompted it.

    One sequencing guard: 01-05 shares wave 4 and pushes a deliberately-failing commit followed by a
    reverting commit onto the same branch. If 01-05's red/green cycle is in flight, wait for its
    green commit to land before pushing this change. The two plans have no file overlap — 01-05
    touches only its transient probe test — but interleaving commits would make 01-05's SHA-matched
    green run inspect a tree that also contains this edit, muddying the evidence that the CI gate,
    and nothing else, produced the red-then-green result.
  </action>

  <verify>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const s=readFileSync('design-system/src/components/NAPFooter/NAPFooter.jsx','utf8'); const m=[...s.matchAll(/phone\s*=\s*'(\+?\d+)'/g)]; if(m.length!==1) throw new Error('expected exactly one phone default in NAPFooter.jsx, found '+m.length); console.log('single phone default:', m[0][1]);"</automated>
    <automated>node --input-type=module -e "import {readdirSync,readFileSync,statSync} from 'node:fs'; import {join} from 'node:path'; const walk=(d)=>readdirSync(d).flatMap(f=>{const q=join(d,f); return statSync(q).isDirectory()?walk(q):[q];}); for(const f of walk('web/app')){ const t=readFileSync(f,'utf8'); if(/phone\s*=/.test(t)) throw new Error('phone assignment leaked into the app at '+f+' — the number must live only at NAPFooter.jsx:33'); if(/\+?44\d{9,}|\b07\d{9}\b/.test(t)) throw new Error('a literal phone number appears in '+f); } console.log('single source of truth intact — no phone prop or literal under web/app');"</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const pkg=readFileSync('design-system/src/components/NAPFooter/NAPFooter.jsx','utf8').match(/phone\s*=\s*'(\+?\d+)'/)[1]; const h=readFileSync('web/.next/server/app/index.html','utf8'); const tels=h.match(/href=\"tel:([^\"]+)\"/g)||[]; if(tels.length!==1) throw new Error('expected 1 tel: link, found '+tels.length); const dialled=tels[0].match(/tel:([^\"]+)/)[1]; const d=(x)=>String(x).replace(/\D/g,'').replace(/^44/,'').replace(/^0/,''); if(d(dialled)!==d(pkg)) throw new Error('built HTML dials '+dialled+' but the package default is '+pkg); console.log('built HTML dials the package default');"</automated>
    <automated>npm run verify</automated>
  </verify>

  <what-built>
    The new site displays and dials exactly one number, `+447861936533`, everywhere. It lives in one
    place — the `NAPFooter` package default at `NAPFooter.jsx:33` — and the app passes no `phone`
    prop, so there is no second place it can be set. `formatPhone.js` derives the displayed string
    from the dial string, so the live site's bug (display `+44 7861 936533`, dial `07441918832` on
    all 115 pages) is now structurally unexpressible rather than merely fixed. CI asserts the href
    digits equal the displayed digits on every push, and asserts that neither retired number
    (`07441918832`, `+447575709361`) appears anywhere.

    `+447861936533` was chosen because it is both the design system's default and the number the
    live Webflow site *displays* on all 115 pages. Code cannot determine whether it is the number
    that actually *reaches* BHC — the live site has been dialling a different one.
  </what-built>

  <how-to-verify>
    1. Call `+44 7861 936533` and confirm it reaches Beyond House Cleaning.
    2. Then call `07441918832` — the number the current live footer actually dials — and note where
       it goes. If that is the real business line and `+447861936533` is not, say so now.
    3. Reply with either "confirmed" or the correct number in full international form.

    This is non-blocking for the rest of Phase 1: the architecture is number-agnostic, and changing
    it is a one-line edit to the `phone` default in `NAPFooter.jsx` plus updating the assertion
    constants. It must be settled before domain cutover, not before Phase 1 ships to a Vercel URL.
  </how-to-verify>

  <acceptance_criteria>
    - Sam replies either "confirmed" for `+447861936533`, or supplies a replacement number in full
      international form
    - `NAPFooter.jsx` contains exactly one `phone = '<number>'` default assignment — whichever branch
      was taken, the number has exactly one home
    - No file under `web/app` contains a `phone =` assignment or a literal UK phone number
    - The single `tel:` href in `web/.next/server/app/index.html` dials the same digits as the
      package default, normalising a leading `44` or `0`
    - If a replacement was supplied: `git diff --numstat` shows exactly 1 changed line in
      `NAPFooter.jsx`, and `web/scripts/check-html-locks.mjs` now lists both `+447861936533` and
      `447861936533` among its retired numbers with its existing assertions otherwise unchanged
    - If a replacement was supplied: `npm run verify` exits 0 after the rebuild
    - If no replacement was supplied: `git status --porcelain` shows no changes to either file listed
      in this plan's `files_modified`
    - The outcome — confirmed or replaced — is recorded in this plan's SUMMARY and in STATE.md, so
      the cutover phase does not have to re-ask
  </acceptance_criteria>

  <resume-signal>
    Reply "confirmed", or give the correct number in full international form (e.g. +447xxxxxxxxx).
  </resume-signal>

  <done>
    The canonical number is confirmed by a human, or replaced with the confirmed one and re-verified,
    and the answer is written down where the cutover phase will find it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Vercel (public internet) | This plan is the moment Phase 1's output becomes publicly reachable. Everything in the built HTML, including all JSON-LD payloads, becomes world-readable. |
| Vercel project settings → build behaviour | Root Directory, Node version and Build Command are configured by a human in a dashboard, outside version control, and can silently drift. |
| Vercel deployment → search engine crawlers | An indexable copy of the site competes with the live Webflow site for the exact keywords the project exists to win. |
| this plan → live production DNS | Deliberately NOT crossed. `www.beyondhousecleaning.com` stays on Webflow (D-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Information Disclosure | Search engines indexing the pre-cutover Vercel deployment | mitigate | D-15 is a disclosure control here, not merely an SEO measure: an unfinished build should not be publicly discoverable while Webflow serves production. Task 2 verifies the crawl block is actually *served* — a `noindex` meta tag in every response plus `/robots.txt` returning `Disallow: /` — rather than merely present on disk. Vercel Deployment Protection is the stronger option and is recorded either way in Task 1's resume signal. |
| T-01-20 | Information Disclosure (PII) | Residential postcode CV32 6EQ reaching the public internet | mitigate | Task 2 runs the verbatim Lock 5 UK-postcode regex against the live HTTP response body, closing the loop that the on-disk checks in Plan 03 open. Two independent surfaces, same assertion. |
| T-01-21 | Denial of Service / availability | The live Webflow site at www.beyondhousecleaning.com | mitigate | D-03 is enforced procedurally: Task 1 instructs Sam explicitly not to add a custom domain, and Task 2's acceptance criteria assert the apex still does not resolve to Vercel. Nothing in this plan touches DNS. |
| T-01-22 | Tampering (configuration drift) | Vercel project settings, which live outside version control | mitigate | Task 1's acceptance criteria pin the settings that matter — Root Directory `web`, Node 22, default Build Command, no custom domain — and the summary records them, so a later drift is detectable by comparison rather than by memory. |
| T-01-23 | Spoofing (misrouted customer contact) | The canonical phone number | mitigate | Task 3 escalates to a human, because code cannot determine which number reaches the business. Publishing a number that reaches the wrong party is a real customer-facing failure, not a cosmetic one. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. The Vercel build installs from the committed `package-lock.json` audited in Plan 02, whose packages are all `[OK]` per 01-RESEARCH.md § Package Legitimacy Audit with `postinstall: none`. |
</threat_model>

<verification>
1. `curl -o /dev/null -s -w '%{http_code}\n' "$DEPLOY_URL"` → `200`
2. `curl -sf "$DEPLOY_URL" | grep -c 'bhc-hero__heading'` → at least 1
3. `curl -sf "$DEPLOY_URL" | grep -o 'href="tel:[^"]*"' | sort -u` → exactly one line,
   `href="tel:+447861936533"` (or the confirmed replacement)
4. `curl -sf "$DEPLOY_URL" | grep -oE '\b[A-Z]{1,2}[0-9]{1,2}[A-Z]?\s?[0-9][A-Z]{2}\b'` → no output
5. `curl -sf "$DEPLOY_URL/robots.txt" | grep -v '^#' | grep 'Disallow: /'` → matches
6. `dig +short www.beyondhousecleaning.com` → does not resolve to Vercel
7. The canonical-number answer is recorded in the SUMMARY and in STATE.md
</verification>

<success_criteria>
- A Vercel project exists for `beyondhousecleaning/bhc-website` with Root Directory `web`, Node 22,
  the default Build Command, and no custom domain (D-01, D-03).
- The deployment is reachable at a Vercel-assigned URL and returns HTTP 200 (SC-1c).
- The raw HTTP response body — with no JavaScript executed — carries Hero, Breadcrumbs, RatingBadge,
  InterlinkBlock, Button and NAPFooter markup and three JSON-LD blocks (SC-4, proof of integration).
- Exactly one `tel:` link is served, its digits match the displayed digits, and no retired number
  appears (D-08, D-09, REQ-nap-consistency).
- No UK postcode, `streetAddress` field or `address` JSON key appears in the served HTML (D-10).
- The served deployment is not indexable: `noindex` meta tag plus `/robots.txt` returning
  `Disallow: /` (D-15).
- `www.beyondhousecleaning.com` still resolves to Webflow and was never modified (D-03).
- The canonical number is confirmed by Sam, or replaced and re-verified, with the answer recorded.
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation-design-system-integration/01-04-SUMMARY.md` when done.
Record: the deployment URL, the Vercel project settings as configured (Root Directory, Node version,
Build Command, Deployment Protection on/off), the Vercel build duration, the full curl verification
output, the `dig` result for the apex domain, and Sam's answer on the canonical phone number.
Also append the phone-number answer to STATE.md's Accumulated Context so the cutover phase inherits it.
</output>
