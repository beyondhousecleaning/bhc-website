---
phase: 01-platform-foundation-design-system-integration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - web/package.json
  - web/next.config.mjs
  - web/app/layout.jsx
  - web/app/page.jsx
  - web/public/robots.txt
autonomous: true
requirements: [REQ-nap-consistency]

must_haves:
  truths:
    - "D-01/D-05/D-06: `npm ci && npm run build --workspace @bhc/web` succeeds from a clean checkout with no design-system build step"
    - "D-02: the app resolves `@bhc/design-system` through an npm workspace symlink, not a relative path and not a copied directory"
    - "D-04: the built CSS carries the design tokens from `tokens.css`, imported unmodified and exactly once"
    - "D-11/D-08: the built page HTML contains exactly one `<footer>` and exactly one `href=\"tel:\"`, whose digits equal the displayed digits"
    - "D-09: neither retired number (07441918832, +447575709361) appears anywhere in the built output"
    - "D-07: zero `use client` directives exist in `web/app/**` and none appear in the built HTML"
    - "SC-4: Hero, Breadcrumbs, RatingBadge, InterlinkBlock, Button and NAPFooter all render from the package onto one real page, with 3 server-rendered JSON-LD blocks"
    - "D-15: the deployment is non-indexable — a robots noindex meta tag ships in every page and `robots.txt` disallows all crawling"
    - "D-14: no third-party or analytics script is imported"
  artifacts:
    - path: "package.json"
      provides: "npm workspace root declaring web and design-system"
      contains: "workspaces"
    - path: "package-lock.json"
      provides: "committed lockfile — Vercel auto-detects npm from it; `npm ci` reproducibility"
    - path: "web/next.config.mjs"
      provides: "transpilePackages for the untranspiled .jsx package"
      contains: "@bhc/design-system"
    - path: "web/app/layout.jsx"
      provides: "the single global CSS import site and the single NAPFooter instance"
      min_lines: 25
    - path: "web/app/page.jsx"
      provides: "the proof-of-integration page rendering all 5 remaining components"
      min_lines: 25
    - path: "web/public/robots.txt"
      provides: "D-15 crawl block for the pre-cutover Vercel deployment"
      contains: "Disallow: /"
  key_links:
    - from: "web/app/layout.jsx"
      to: "@bhc/design-system/styles.css"
      via: "global CSS import (styles.css @imports tokens.css — never import both)"
      pattern: "@bhc/design-system/styles\\.css"
    - from: "web/app/layout.jsx"
      to: "@bhc/design-system/fonts/fonts.css"
      via: "global CSS import, unblocked by Plan 01's exports-map fix"
      pattern: "@bhc/design-system/fonts/fonts\\.css"
    - from: "web/app/layout.jsx"
      to: "NAPFooter"
      via: "rendered once in the root layout with no phone prop"
      pattern: "<NAPFooter"
    - from: "web/app/page.jsx"
      to: "@bhc/design-system"
      via: "named component imports from the package barrel"
      pattern: "from '@bhc/design-system'"
---

<objective>
Turn this repo into an npm workspace, stand up the Next.js application, and render one real page
that proves all six design-system components work from the package under React Server Components.

Purpose: this is the phase's centre of gravity. It delivers Success Criteria 2 and 4 outright, and
everything needed for Criterion 1 except the Vercel connection itself. Because every assertion in
this phase reads the static HTML `next build` writes to disk, this plan makes those assertions
possible — Plan 03 then automates them.

Output: a root workspace manifest and committed lockfile, a `web/` Next.js app, a root layout that
owns the global CSS and the single NAPFooter, a proof-of-integration page, and the D-15 crawl block.

Non-negotiable: nothing here may take the live Webflow site down (D-03). This plan ships code to a
branch; it touches no DNS and no live host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md
@.planning/phases/01-platform-foundation-design-system-integration/01-RESEARCH.md
@.planning/phases/01-platform-foundation-design-system-integration/01-PATTERNS.md
@.planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md
@.planning/phases/01-platform-foundation-design-system-integration/01-01-SUMMARY.md

<interfaces>
<!-- Component contracts, extracted from design-system source. Use these directly — do not explore. -->

Package barrel `@bhc/design-system` (design-system/src/index.js) exports:
  Button, Hero, Breadcrumbs, RatingBadge, NAPFooter, InterlinkBlock,
  formatPhone, buildInterlinks, nearestTowns, distanceMiles

Package CSS subpaths (after Plan 01's exports fix):
  '@bhc/design-system/styles.css'      -> its ONLY @import is './tokens.css' (styles.css:6)
  '@bhc/design-system/tokens.css'      -> DO NOT import; already pulled in by styles.css
  '@bhc/design-system/fonts/fonts.css' -> NOT in styles.css's import closure; must be imported
                                          separately. (.design-sync/conventions.md claims otherwise
                                          — that claim is stale and verified wrong.)

NAPFooter({ phone = '+447861936533', businessName, siteUrl = 'https://www.beyondhousecleaning.com',
            areaServed = [], hours, ... })
  - Derives BOTH the href (`tel:${toDial(phone)}`) and the visible label (`formatPhone(phone)`)
    from the single `phone` prop. There is no prop for displayed text. (NAPFooter.jsx:45-46, 73-75)
  - Renders <a className="bhc-footer__phone" href="tel:...">
  - Emits HomeAndConstructionBusiness JSON-LD with `telephone` + `areaServed`, no `address`
  - Renders `© {new Date().getFullYear()}` — legal in an RSC; baked at build time; accepted for
    Phase 1, do NOT add "use client" to fix it

Hero({ eyebrow, heading, lead, rating, actions, media, align, className })
  - `heading` is ALWAYS the page's single <h1>, rendered as <h1 className="bhc-hero__heading">
  - Hero.jsx:33-35 THROWS if `heading` is missing — Lock 1 is enforced at build time
  - `eyebrow` renders as <p className="bhc-hero__eyebrow">, never a heading
  - `rating` renders an internal <RatingBadge {...rating} bare /> — `bare` and emitSchema defaults
    to false, so the Hero's badge emits NO schema

Breadcrumbs({ items })  items: [{ label, href? }]
  - Renders <ol className="bhc-breadcrumbs__list"> + BreadcrumbList JSON-LD

RatingBadge({ rating = 4.9, count = 175, source = 'Google', bare = false, emitSchema = false, ... })
  - Renders <span className="bhc-rating__value">{rating}</span>
  - `emitSchema` emits AggregateRating/LocalBusiness JSON-LD. Use it AT MOST ONCE per page.

InterlinkBlock({ variant, heading, intro, links })  links: [{ href, label, meta? }]
  - Renders <ul className="bhc-interlink__list">

Button({ href, variant = 'primary', size, children, ... })
  - Renders class list `bhc-btn bhc-btn--primary bhc-btn--md`; an <a> when `href` is given

Layout/page utility classes that actually exist (design-system/.design-sync/conventions.md:21-25).
Nothing else exists — never invent a `bhc-` name, never hardcode a hex a token already holds:
  bhc-container, bhc-container--narrow, bhc-container--wide,
  bhc-section, bhc-section--warm, bhc-section--tint, bhc-section--navy,
  bhc-visually-hidden, bhc-skip-link
Caveat: RatingBadge is light-surface only — it sets --bhc-ink directly, so it renders illegibly
on bhc-section--navy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the repo to an npm workspace and scaffold the web package</name>

  <files>package.json, package-lock.json, web/package.json, web/next.config.mjs</files>

  <read_first>
    - design-system/package.json — the formatting and conventions the new manifests must match, and
      the confirmation that `react` is a peerDependency only
    - .gitignore — confirm `node_modules/` and `.next/` are already ignored and that
      `package-lock.json` is NOT ignored (it must be committed)
    - .planning/phases/01-platform-foundation-design-system-integration/01-RESEARCH.md — § Standard
      Stack for the verified versions, § Package Legitimacy Audit, § Code Examples for the verified
      root manifest, Pattern 2 for next.config, Pitfall 8 for why not pnpm, and § State of the Art
      for what Next.js 16 removed
    - .planning/phases/01-platform-foundation-design-system-integration/01-PATTERNS.md — section
      "`package.json` (repo root), `web/package.json` — partial analog (formatting only)"
  </read_first>

  <action>
    Create the root `package.json`: `name` "bhc-website", `private` true, `workspaces`
    `["web", "design-system"]`, and exactly two scripts — `test:locks` running
    `npm run test --workspace @bhc/design-system`, and `build` running
    `npm run build --workspace @bhc/web`. Do not set `"type": "module"` at the root (the two
    workspace packages declare their own module type; leaving the root unset keeps ad-hoc
    `node -e` one-liners working in either mode). Do not add a `dependencies` block at the root.

    Create `web/package.json`: `name` "@bhc/web", `version` "0.1.0", `private` true,
    `"type": "module"`, scripts `dev` = `next dev`, `build` = `next build`, `start` = `next start`.
    Dependencies: `next`, `react`, `react-dom`, and `@bhc/design-system` at `"*"` (the workspace
    protocol npm resolves to the local package). No `devDependencies` — no ESLint, no TypeScript,
    no `size-limit`. Plain JavaScript, not TypeScript: the design-system's `exports` map has no
    `types` condition, so TypeScript would force a further package change that D-02/D-05 do not
    sanction, and the app matches the package's own `.jsx` idiom.

    Versions: research pinned and built against `next` 16.3.0, `react` 19.2.8, `react-dom` 19.2.8 on
    2026-08-08. Confirm the current published versions at execution time with `npm view next version`,
    `npm view react version` and `npm view react-dom version`. If npm reports a newer patch within the
    same major, take it and record the deviation in the summary; if it reports a newer major, stop and
    surface it rather than upgrading blind. All four runtime dependencies are already cleared:
    01-RESEARCH.md § Package Legitimacy Audit records `next`, `react`, `react-dom` (and every optional
    package considered) as `[OK]` under `slopcheck --ecosystem npm`, each with `scripts.postinstall`
    absent. There are zero `[ASSUMED]`, zero `[SUS]` and zero `[SLOP]` packages, so no pre-install
    legitimacy checkpoint is required for this plan.

    Install with `npm install` run from the repo root. Use npm, not pnpm: `@bhc/design-system`
    declares `react` only as a `peerDependency` with no `devDependencies`, so under pnpm's isolated
    `node_modules` layout the JSX transform's `react/jsx-runtime` import resolves from
    `design-system/` rather than from the app and only works via `auto-install-peers`. npm's hoisted
    layout removes the question. The install must produce a root `package-lock.json` — commit it;
    Vercel auto-detects npm from its presence, and CI's `npm ci` requires it.

    Create `web/next.config.mjs` exporting a default config object whose only key is
    `transpilePackages: ['@bhc/design-system']`. Turbopack already auto-transpiles workspace packages
    (the research probe built without this line), but set it anyway: it is the documented lever, it is
    a no-op when auto-detection works, and it is the only thing that saves the build if the workspace
    linkage is ever replaced by a plain `file:` dependency. Do NOT add a `webpack` key — under
    Next.js 16 a custom webpack config makes `next build` fail unless `--webpack` is passed. Do NOT
    add a `--turbopack` or `--turbo` flag anywhere; Turbopack is the Next.js 16 default and the flag
    is redundant. Do NOT add `eslint` or `typescript` config keys — the `eslint` key was removed in
    Next.js 16, as was the `next lint` command.
  </action>

  <verify>
    <automated>npm ls --workspaces --depth=0</automated>
    <automated>test -L node_modules/@bhc/design-system && echo "workspace symlink present"</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const r=JSON.parse(readFileSync('package.json','utf8')); if(JSON.stringify(r.workspaces)!==JSON.stringify(['web','design-system'])) throw new Error('root workspaces wrong: '+JSON.stringify(r.workspaces)); const w=JSON.parse(readFileSync('web/package.json','utf8')); if(w.name!=='@bhc/web') throw new Error('web package name wrong'); if(w.devDependencies) throw new Error('web must have no devDependencies in Phase 1'); if(w.dependencies['@bhc/design-system']!=='*') throw new Error('design-system dep must be \"*\"'); const c=readFileSync('web/next.config.mjs','utf8'); if(!c.includes(\"transpilePackages\")) throw new Error('transpilePackages missing'); if(c.includes('webpack')) throw new Error('webpack key forbidden under Next 16'); console.log('manifests OK');"</automated>
    <automated>test -f package-lock.json && ! git check-ignore -q package-lock.json && echo "lockfile present and tracked"</automated>
  </verify>

  <acceptance_criteria>
    - `npm ls --workspaces --depth=0` exits 0 and lists both `@bhc/web` and `@bhc/design-system`
    - `node_modules/@bhc/design-system` is a symlink (not a copied directory) pointing at `../../design-system`
    - Root `package.json` has `workspaces: ["web","design-system"]` and exactly the two scripts
      `test:locks` and `build`
    - `web/package.json` has exactly four dependencies — `next`, `react`, `react-dom`,
      `@bhc/design-system` — and no `devDependencies` key at all
    - `web/next.config.mjs` contains the string `transpilePackages` and does not contain the string `webpack`
    - No `--turbopack` or `--turbo` flag appears in any npm script
    - `package-lock.json` exists at the repo root and is not gitignored
    - `design-system/package.json` is byte-identical to its post-Plan-01 state (this task must not
      add `react` as a direct dependency there, nor a `devDependencies` block)
    - `node --test design-system/test/*.test.js` still exits 0 with `# pass 10`, `# fail 0`
  </acceptance_criteria>

  <done>
    The repo is a two-package npm workspace with a committed lockfile, the app package resolves the
    design system through a symlink, and Next.js is configured to compile its raw `.jsx` with no
    build step in the design-system package.
  </done>
</task>

<task type="auto">
  <name>Task 2: Root layout and the proof-of-integration page</name>

  <files>web/app/layout.jsx, web/app/page.jsx</files>

  <read_first>
    - design-system/styles.css — confirm with your own eyes that line 6 is its only `@import`, and
      that it pulls in `tokens.css`
    - design-system/fonts/fonts.css — the separately-imported font layer
    - design-system/src/components/NAPFooter/NAPFooter.jsx — the `phone` default at line 33, the
      derived href/label at lines 45-46 and 73-75, and the JSON-LD it emits
    - design-system/src/components/Hero/Hero.jsx — the `heading`-is-the-h1 rule and the throw at lines 33-35
    - design-system/src/components/RatingBadge/RatingBadge.jsx — the `emitSchema` and `bare` props
    - design-system/src/components/Breadcrumbs/Breadcrumbs.jsx, InterlinkBlock/InterlinkBlock.jsx,
      Button/Button.jsx — prop shapes and emitted class names
    - design-system/.design-sync/conventions.md — the composition reference and the complete list of
      layout classes that exist. Ignore its claim that styles.css imports the fonts; that is stale
    - .planning/phases/01-platform-foundation-design-system-integration/01-RESEARCH.md — Pattern 1
      (layout), Pattern 3 (the probe-built page body), Pitfall 2 (why no "use client"), and
      § Anti-Patterns to Avoid
    - .planning/phases/01-platform-foundation-design-system-integration/01-PATTERNS.md — sections on
      `web/app/layout.jsx` and `web/app/page.jsx`
  </read_first>

  <action>
    Create `web/app/layout.jsx`.

    It imports, in this order and nothing else CSS-wise: `'@bhc/design-system/fonts/fonts.css'` then
    `'@bhc/design-system/styles.css'`. Do NOT also import `'@bhc/design-system/tokens.css'` —
    `styles.css` already `@import`s it, and importing both emits the entire token block twice. The
    fonts import is separate because `fonts.css` is genuinely not in `styles.css`'s import closure;
    that is precisely why Plan 01's exports-map fix exists. This is the only file in the app that
    imports CSS.

    It imports `{ NAPFooter }` from `'@bhc/design-system'`.

    It exports a `metadata` object with: `metadataBase` set to `new URL('https://www.beyondhousecleaning.com')`
    — `.com`, not `.co.uk`; PROJECT.md's opening line is a typo and the live sitemap, 120 doc
    occurrences and `NAPFooter.jsx:26`'s `SITE` constant all agree on `.com` — a `title`, and
    `robots: { index: false, follow: false }`. That robots entry is D-15: it emits
    `<meta name="robots" content="noindex, nofollow">` into every page's server-rendered HTML, which
    is the assertable half of the crawl block. Add a comment naming D-15 and stating that this
    deliberately differs from ROADMAP Phase 5's "robots.txt allows crawling" criterion — the two
    apply to different hosts (pre-cutover Vercel deployment vs. the production apex), and the
    cutover phase is what flips it.

    It default-exports `RootLayout({ children })` returning `<html lang="en-GB">` wrapping a `<body>`
    that renders `{children}` followed by a single `<NAPFooter />`.

    Pass `areaServed` (an array of Warwickshire/Coventry town names, e.g. Leamington Spa, Warwick,
    Kenilworth, Coventry) and `hours` (e.g. "Mon–Sat, 8am–7pm"). Pass NO `phone` prop. The canonical
    `+447861936533` is the package default at `NAPFooter.jsx:33`; passing it from the app would create
    a second place the number can be wrong, which is the exact defect REQ-nap-consistency exists to
    eliminate. Note that `.design-sync/previews/NAPFooter.tsx:38` does pass it — that is a prop demo,
    not app guidance; do not copy it.

    `<NAPFooter />` is rendered here and nowhere else in the app, ever. That is what makes D-11
    structural rather than a convention someone has to remember, and it is what the
    "exactly one `<footer>`" assertion checks. Do not render a second footer, a page-local phone
    number, or any hardcoded `tel:` anywhere.

    Then create `web/app/page.jsx` — the proof-of-integration page. Import `Hero`, `Breadcrumbs`,
    `RatingBadge`, `InterlinkBlock` and `Button` from `'@bhc/design-system'` and render all five in a
    fragment, following the probe-built composition in 01-RESEARCH.md § Pattern 3 (that exact JSX was
    built and produced every string the phase's assertions look for):
      - `Breadcrumbs` with a two-item trail ending in an unlinked current page, e.g. Home → Warwick
      - `Hero` with `heading` set to a real keyword-bearing line containing a town name (e.g. "Deep
        Cleaning in Warwick"), an `eyebrow`, a `lead`, `rating={{ rating: 4.9, count: 175 }}` and one
        `actions` entry pointing at `/get-a-quote`
      - a standalone `RatingBadge` with `rating={4.9} count={175}` and `emitSchema` set — exactly once
        on the page (Hero's internal badge is `bare` with `emitSchema` defaulting false, so this is
        the page's only AggregateRating emission)
      - `InterlinkBlock` with `variant="services"`, a heading, and at least one link using the locked
        combo URL shape `/location/<region>/<town>/<service>` (e.g.
        `/location/warwickshire/warwick/domestic-cleaning`) — the 95 existing URLs must stay valid, so
        never invent a different combo prefix
      - a `Button` with `href="/get-a-quote"`

    Copy is scaffold, not final — real templates are Phase 2. Review signal is 175 reviews at 4.9★
    as of 2026-08-06.

    Constraints binding both files:
      - No `"use client"` anywhere in `web/app/**`. All six components are verified RSC-safe exactly
        as written; a client directive would move the JSON-LD and the rating badge into a hydration
        payload and silently break CI Locks 3 and 9, which is the entire point of the rebuild (D-07).
      - No analytics, tag manager, GTM, GA4, Trustmary, or any third-party script (D-14).
      - Do not copy any design-system component into `web/`. If something genuinely cannot render, the
        change goes into the package (D-05) — but the research probe proved none is needed.
      - Own markup may use only the layout classes that actually exist (`bhc-container`,
        `bhc-section` and their modifiers, `bhc-visually-hidden`, `bhc-skip-link`) and
        `var(--bhc-*)` tokens. Never invent a `bhc-` name, never hardcode a hex a token already holds.
      - No street address and no UK postcode anywhere, including inside any JSON-LD (D-10).
  </action>

  <verify>
    <automated>npm run build --workspace @bhc/web</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const h=readFileSync('web/.next/server/app/index.html','utf8'); const digits=(s)=>String(s).replace(/\D/g,''); const tels=h.match(/href=\"tel:[^\"]+\"/g)||[]; if(tels.length!==1) throw new Error('expected 1 tel: link, found '+tels.length); const href=tels[0].match(/tel:([^\"]+)/)[1]; const label=h.match(/class=\"bhc-footer__phone\"[^>]*>([^<]+)</)[1]; if(digits(href).replace(/^44/,'')!==digits(label).replace(/^44/,'')) throw new Error('href '+href+' != label '+label); const foot=h.match(/<footer/g)||[]; if(foot.length!==1) throw new Error('expected 1 footer, found '+foot.length); for(const bad of ['07441918832','447441918832','447575709361']) if(h.includes(bad)) throw new Error('retired number present: '+bad); const h1=h.match(/<h1/g)||[]; if(h1.length!==1) throw new Error('expected 1 h1, found '+h1.length); for(const s of ['bhc-hero__heading','bhc-breadcrumbs__list','\"@type\":\"BreadcrumbList\"','bhc-rating__value\">4.9','bhc-interlink__list','bhc-btn--primary']) if(!h.includes(s)) throw new Error('missing: '+s); const ld=h.match(/<script type=\"application\\/ld\\+json\"/g)||[]; if(ld.length!==3) throw new Error('expected 3 JSON-LD blocks, found '+ld.length); if(h.includes('use client')) throw new Error('client boundary in output'); if(/\\b[A-Z]{1,2}\\d{1,2}[A-Z]?\\s?\\d[A-Z]{2}\\b/.test(h)) throw new Error('UK postcode matched in built HTML'); console.log('built HTML OK');"</automated>
    <automated>! grep -rn "use client" web/app && echo "no client directives in web/app"</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build --workspace @bhc/web` exits 0 and reports the `/` route as statically prerendered
    - `web/.next/server/app/index.html` exists on disk after the build
    - That file contains exactly one `href="tel:` occurrence, and the digits of that href — with a
      leading `44` stripped — equal the digits of the `bhc-footer__phone` label text
    - That file contains exactly one `<footer` occurrence and exactly one `<h1` occurrence
    - That file contains none of the strings `07441918832`, `447441918832`, `447575709361`
    - That file contains all of: `bhc-hero__heading`, `bhc-breadcrumbs__list`,
      `"@type":"BreadcrumbList"`, `bhc-rating__value">4.9`, `bhc-interlink__list`, `bhc-btn--primary`
    - That file contains exactly three `<script type="application/ld+json"` occurrences
    - That file contains `name="robots"` with a `noindex` value
    - The regex `/\b[A-Z]{1,2}\d{1,2}[A-Z]?\s?\d[A-Z]{2}\b/` does NOT match that file
    - That file does not contain the string `use client`, and `grep -rn "use client" web/app` returns
      no matches
    - That file contains no `<script src="http` (external-origin) tag
    - `grep -rn "tokens.css" web/app` returns no matches (styles.css already imports it)
  </acceptance_criteria>

  <done>
    One real page renders Hero, Breadcrumbs, RatingBadge, InterlinkBlock, Button and NAPFooter from
    the package as server components, with a single correct `tel:` link, a single footer, three
    server-rendered JSON-LD blocks, no postcode, and no client boundary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ship the D-15 crawl block and confirm the token pipeline</name>

  <files>web/public/robots.txt</files>

  <read_first>
    - web/app/layout.jsx — confirm the `robots: { index: false, follow: false }` metadata added in
      Task 2 is present; this task adds the second, host-level half of the same control
    - design-system/tokens.css — read the first ~30 lines to identify a token name to assert on
      (`--bhc-ink` is the one 01-VALIDATION.md SC-1b names)
    - .planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md — D-15 and the
      note that ROADMAP Phase 5's crawl-allowing criterion applies to the post-cutover apex
    - .planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md — rows
      SC-1b and D-15
  </read_first>

  <action>
    Create `web/public/robots.txt` as a static file (not a `web/app/robots.js` metadata route). A
    static file under `public/` is served verbatim at `/robots.txt`, always exists on disk, and is
    therefore assertable by a zero-dependency check without depending on where Next.js chooses to
    write a prerendered metadata route.

    Its contents: a `User-agent: *` line followed by `Disallow: /`, preceded by comment lines naming
    D-15 and stating the reason and the expiry condition — the live Webflow site holds all 95 pages'
    ranking equity, and letting Google index a second complete copy of the same content at a
    `*.vercel.app` URL risks duplicate-content dilution against the exact keywords this project
    exists to win. State explicitly that this file is flipped at domain cutover, and that ROADMAP
    Phase 5's "robots.txt allows crawling (`User-agent: *` present)" criterion applies to the
    production apex after cutover — the two are not in conflict.

    Note that D-15 is now enforced at two independent levels: the `noindex, nofollow` meta tag in
    every page's server-rendered HTML (Task 2), and this host-level crawl block. Both are asserted
    by Plan 03's check script.

    Then re-run the build and confirm the token pipeline end-to-end: the design tokens must reach the
    built CSS, which is what proves the app is genuinely "built from `design-system/tokens.css`"
    (Success Criterion 1) rather than merely importing a stylesheet. Assert that `--bhc-ink` appears
    in the emitted CSS under `web/.next/static/`, and that it appears exactly once per declaration
    site — a doubled token block is the signature of importing both `tokens.css` and `styles.css`,
    which this plan forbids.
  </action>

  <verify>
    <automated>npm run build --workspace @bhc/web</automated>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const r=readFileSync('web/public/robots.txt','utf8'); const body=r.split('\n').filter(l=>!l.trim().startsWith('#')).join('\n'); if(!/User-agent:\s*\*/.test(body)) throw new Error('missing User-agent: *'); if(!/Disallow:\s*\//.test(body)) throw new Error('missing Disallow: /'); if(/Allow:\s*\//.test(body)) throw new Error('Allow: / contradicts D-15'); console.log('robots.txt OK');"</automated>
    <automated>grep -rl -- "--bhc-ink" web/.next/static/ | head -1 | grep -q . && echo "design tokens present in built CSS"</automated>
    <automated>node --input-type=module -e "import {readdirSync,readFileSync,statSync} from 'node:fs'; import {join} from 'node:path'; const walk=(d)=>readdirSync(d).flatMap(f=>{const p=join(d,f); return statSync(p).isDirectory()?walk(p):[p];}); const css=walk('web/.next/static').filter(f=>f.endsWith('.css')); let n=0; for(const f of css){ n += (readFileSync(f,'utf8').match(/--bhc-ink\s*:/g)||[]).length; } if(n===0) throw new Error('--bhc-ink not declared in any built CSS'); if(n>1) throw new Error('--bhc-ink declared '+n+' times — tokens.css imported twice'); console.log('token block emitted exactly once');"</automated>
  </verify>

  <acceptance_criteria>
    - `web/public/robots.txt` exists; ignoring `#` comment lines it contains `User-agent: *` and
      `Disallow: /`, and contains no `Allow: /` line
    - The file's comments name D-15 and state that Phase 5's crawl-allowing criterion applies to the
      post-cutover apex
    - `web/.next/server/app/index.html` contains `name="robots"` with a `noindex` value
    - At least one `.css` file under `web/.next/static/` contains `--bhc-ink`
    - The declaration `--bhc-ink:` appears exactly once across all built CSS (proves `tokens.css`
      was not imported twice)
    - `npm run build --workspace @bhc/web` exits 0
  </acceptance_criteria>

  <done>
    The deployment is non-indexable at both the meta and host level, and the design tokens are
    provably present in the built CSS exactly once — the app is built from `tokens.css`, not from a
    fork or a copy of its values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | This plan introduces the repo's first `node_modules` and its first lockfile. Four runtime dependencies cross this boundary. |
| build output → public internet | Once Sam connects Vercel (Plan 04), everything in `web/.next/server/app/index.html` is publicly readable, including all JSON-LD payloads. |
| design-system package → app | Consumed via workspace symlink. Component defaults (notably the canonical phone number) cross into rendered output without app-side restatement. |
| repo → live Webflow site | Deliberately NOT crossed. Phase 1 touches no DNS and no live host (D-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering (supply chain) | `npm install` of next/react/react-dom in `web/package.json` | mitigate | All packages cleared in 01-RESEARCH.md § Package Legitimacy Audit: `slopcheck --ecosystem npm` returned `[OK]` for every one, and `npm view <pkg> scripts.postinstall` returned nothing for each. Zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages, so no blocking human checkpoint is required. Task 1 commits `package-lock.json`; CI (Plan 03) installs with `npm ci` against it so the dependency set cannot drift silently. Task 1's acceptance criteria forbid `devDependencies` in `web/`, keeping the surface at four packages. |
| T-01-05 | Information Disclosure (PII) | Rendered HTML and JSON-LD payloads | mitigate | D-10 / Lock 5: no street address, no UK postcode anywhere in rendered output including JSON-LD, because JSON-LD ships in page HTML. Enforced by Task 2's acceptance criterion running the UK-postcode regex against the built HTML; Plan 03 makes it a permanent CI gate. `NAPFooter` emits `areaServed` with no `address` by construction. |
| T-01-06 | Information Disclosure (premature indexing) | The Vercel deployment while Webflow still serves production | mitigate | D-15, defended twice: `robots: { index: false, follow: false }` in the root layout metadata emits a `noindex, nofollow` meta tag into every page (Task 2), and `web/public/robots.txt` disallows all crawling at host level (Task 3). Plan 04 confirms the crawl block is actually served by the deployment, not just present on disk. |
| T-01-07 | Repudiation (unsanctioned tracking) | Any third-party script | mitigate | D-14: no analytics, tag manager, GA4, GTM or Trustmary in Phase 1. Task 2 acceptance asserts no external-origin `<script src="http` in the built HTML; Plan 03 turns that into a CI gate. |
| T-01-08 | Tampering (NAP divergence reintroduced) | `web/app/layout.jsx` | mitigate | The app passes no `phone` prop, so there is no app-side place the number can diverge; display is derived from dial inside `formatPhone.js`. Task 2 asserts exactly one `tel:` and href-digits-equal-label-digits against the built HTML. |
| T-01-09 | Denial of Service | Public static site on Vercel | transfer | Static prerendered pages on Vercel's CDN. Rate limiting and edge DDoS protection are the platform's responsibility; nothing in Phase 1 executes server-side per request. |
| T-01-10 | Spoofing | — | accept | No authentication, no sessions, no cookies, no user accounts in Phase 1. ASVS V2 and V3 do not apply to a static marketing site. |
| T-01-11 | Multiple (missing security headers) | `next.config.mjs` `headers()` | accept | Deferred to Phase 5 with a stated reason: a strict CSP requires a nonce or hash for the inline JSON-LD `<script>` blocks, and adding CSP casually would block exactly the Lock 3 / Lock 9 output this phase exists to produce. |
</threat_model>

<verification>
Run from the repo root:

1. `rm -rf node_modules web/.next && npm ci && npm run build --workspace @bhc/web` → exits 0
   (proves the clean-checkout requirement: no design-system build step exists or is needed)
2. `test -L node_modules/@bhc/design-system` → the design system is a workspace symlink
3. `node --test design-system/test/*.test.js` → `# pass 10`, `# fail 0`
4. `test -f web/.next/server/app/index.html` → the assertable artifact exists
5. Against that HTML: exactly one `href="tel:`, exactly one `<footer`, exactly one `<h1`,
   three `<script type="application/ld+json"`, `noindex` present, no `use client`, no UK-postcode
   match, no `<script src="http`, and none of the three retired phone numbers
6. `grep -rl -- "--bhc-ink" web/.next/static/` → at least one built CSS file, with exactly one
   `--bhc-ink:` declaration across all of them
7. `git status --porcelain` → `node_modules/` and `web/.next/` are absent from the output
   (already gitignored); `package-lock.json` IS listed
</verification>

<success_criteria>
- The repo is an npm workspace `["web","design-system"]` with a committed root `package-lock.json`
  and a symlinked `@bhc/design-system` (D-02).
- `npm ci && npm run build --workspace @bhc/web` succeeds from a clean checkout with no
  design-system build step (D-06, SC-1a).
- The built CSS carries `--bhc-ink` from `tokens.css`, declared exactly once (D-04, SC-1b).
- The built page HTML has exactly one `tel:` link whose digits equal the displayed digits, exactly
  one `<footer>`, and none of the three retired numbers (D-08, D-09, D-11, REQ-nap-consistency).
- No UK postcode or street address appears anywhere in the built HTML, including inside JSON-LD (D-10).
- Hero, Breadcrumbs, RatingBadge, InterlinkBlock, Button and NAPFooter all render from the package
  onto one real page, with exactly one `<h1>` and three server-rendered JSON-LD blocks (SC-4).
- Zero `use client` directives in `web/app/**` and none in the built HTML (D-07).
- A `noindex, nofollow` meta tag ships on every page and `web/public/robots.txt` disallows all
  crawling (D-15).
- No third-party or analytics script is imported (D-14).
- `www.beyondhousecleaning.com` is untouched and still served by Webflow (D-03).
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation-design-system-integration/01-02-SUMMARY.md` when done.
Record: the exact installed versions of next/react/react-dom (and any deviation from the
16.3.0/19.2.8/19.2.8 research pin), the build duration and route table from `next build`, the byte
size of `web/.next/server/app/index.html`, and the measured counts for tel:/footer/h1/JSON-LD.
</output>
