---
phase: 01-platform-foundation-design-system-integration
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - design-system/test/__ci-gate-probe.test.js
autonomous: false
requirements: [REQ-nap-consistency]

must_haves:
  truths:
    - "D-13: a deliberately failing lock makes the GitHub Actions run conclude `failure`, demonstrated end-to-end and then reverted"
    - "D-13: the `locks` and `build` jobs are required status checks on `main`, so a red run blocks a merge rather than merely reporting"
    - "D-12: the locks job still passes with no npm install after the probe is removed"
  artifacts: []
  key_links:
    - from: "design-system/test/__ci-gate-probe.test.js"
      to: "GitHub Actions `locks` job"
      via: "the job's node --test glob picks up any test/*.test.js file"
      pattern: "locks"
    - from: "GitHub branch protection on main"
      to: "the `locks` and `build` job names"
      via: "required status checks"
      pattern: "required_status_checks"
---

<objective>
Prove the CI gate actually blocks, and make it binding.

Purpose: D-13 says a lock suite that runs but does not gate is not a lock. Plan 03 delivered a
workflow that exits non-zero on failure — that is necessary but not sufficient. Two things remain:
demonstrating end-to-end that a failing lock turns the GitHub Actions run red, and marking the jobs
as required status checks on `main` so a red run blocks a merge. The second is a repo-admin action
in GitHub's settings that no agent can perform.

Output: a recorded red-then-green CI run pair, and branch protection configured on `main`.

This plan runs last because it deliberately pushes a broken commit. Everything else in the phase
must already be green so that the red run is unambiguously caused by the probe and by nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md
@.planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md
@.planning/phases/01-platform-foundation-design-system-integration/01-03-SUMMARY.md

<interfaces>
<!-- What exists after Plan 03. No exploration required. -->

.github/workflows/ci.yml — two jobs on `on: push` with `branches: ['**']`:
  locks   checkout -> setup-node 22 -> `node --test design-system/test/*.test.js`   (no npm ci)
  build   checkout -> setup-node 22 (cache npm) -> npm ci -> npm run build --workspace @bhc/web
          -> node --test web/scripts/check-html-locks.mjs -> node web/scripts/check-budget.mjs

Because the locks job globs `design-system/test/*.test.js`, ANY new `*.test.js` file dropped in that
directory is picked up automatically — which is what makes a throwaway probe file the cleanest way
to demonstrate gating without editing `locks.test.js` and risking a bad revert.

Verified locally during research: injecting a failing test made the suite exit 1. This plan proves
the same holds in the CI runner, where it actually matters.

Useful gh invocations:
  gh run list --branch "$(git branch --show-current)" --limit 5 --json databaseId,name,status,conclusion,headSha
  gh run watch <id> --exit-status        # exits non-zero if the run failed
  gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks.contexts'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove a failing lock turns the CI run red, then revert</name>

  <files>design-system/test/__ci-gate-probe.test.js</files>

  <read_first>
    - .github/workflows/ci.yml — the file whose behaviour is being proven; confirm the `locks` job's
      test glob is `design-system/test/*.test.js` so the probe file will actually be picked up
    - design-system/test/locks.test.js — the import and `test(...)` shape the probe must match so it
      runs under the same `node --test` invocation with zero dependencies
    - .planning/phases/01-platform-foundation-design-system-integration/01-03-SUMMARY.md — confirm
      the baseline CI run for this branch is green before deliberately breaking it
    - .planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md — row SC-3b,
      which specifies the throwaway-failing-commit method
  </read_first>

  <action>
    First confirm the baseline: the newest CI run for the current branch must have concluded
    `success` for both the `locks` and `build` jobs. If it has not, stop — a red run caused by
    something other than the probe proves nothing, and this plan must not be used to paper over an
    unrelated failure.

    Create `design-system/test/__ci-gate-probe.test.js` — a throwaway file, deliberately named with a
    leading double underscore so it sorts distinctly and is obviously not a real lock. Use a
    throwaway file rather than editing `locks.test.js`: a botched revert of the real lock suite is
    the one failure mode worth engineering out of this exercise entirely.

    Its contents: `node:test` and `node:assert/strict` imports only, matching the house style, plus a
    single test whose assertion always fails, with a header comment stating in plain terms that the
    file exists solely to prove D-13 gating and must be deleted in the same plan that created it.

    Confirm locally that `node --test design-system/test/*.test.js` now exits non-zero.

    Commit the probe alone (no other changes in the commit) with a message that makes its purpose and
    its temporary nature unmistakable, push it, and observe the resulting GitHub Actions run. Wait for
    the run to complete and record: the run ID, the run URL, the conclusion of the `locks` job, and
    the conclusion of the overall run. The `locks` job must conclude `failure`.

    Then delete the probe file, commit the deletion alone, push, and observe the next run. Both jobs
    must conclude `success`. Record that run ID and URL too.

    Verify the working tree is clean afterwards and that `design-system/test/` contains exactly one
    file, `locks.test.js`. The probe must leave no trace beyond the two commits in history, which are
    themselves the evidence.
  </action>

  <verify>
    <automated>gh run list --branch "$(git branch --show-current)" --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success && echo "baseline is green"</automated>
    <automated>! node --test design-system/test/*.test.js && echo "probe makes the suite exit non-zero locally"</automated>
    <automated>git add design-system/test/__ci-gate-probe.test.js && git commit -m "test(01): TEMPORARY ci gate probe — proves D-13, reverted in the next commit" && git push && sleep 5 && ! gh run watch "$(gh run list --branch "$(git branch --show-current)" --limit 1 --json databaseId --jq '.[0].databaseId')" --exit-status && echo "CI run concluded failure as required by D-13"</automated>
    <automated>git rm design-system/test/__ci-gate-probe.test.js && git commit -m "test(01): remove ci gate probe" && git push && sleep 5 && gh run watch "$(gh run list --branch "$(git branch --show-current)" --limit 1 --json databaseId --jq '.[0].databaseId')" --exit-status && echo "CI green again"</automated>
    <automated>test "$(ls design-system/test | tr '\n' ' ')" = "locks.test.js " && git status --porcelain | grep -q . && echo "unexpected dirty tree" || echo "probe fully removed, tree clean"</automated>
  </verify>

  <acceptance_criteria>
    - The CI run immediately before the probe commit concluded `success` for both jobs
    - With the probe present, `node --test design-system/test/*.test.js` exits non-zero locally
    - The GitHub Actions run triggered by the probe commit has the `locks` job with conclusion
      `failure`, and the overall run conclusion is `failure`
    - The GitHub Actions run triggered by the removal commit has both `locks` and `build` with
      conclusion `success`
    - After the removal commit, `ls design-system/test` outputs exactly `locks.test.js`
    - `git status --porcelain` is empty
    - `node --test design-system/test/*.test.js` exits 0 with `# pass 10`, `# fail 0`
    - Both run IDs and both run URLs are captured for the SUMMARY
  </acceptance_criteria>

  <done>
    There is recorded, reproducible evidence that a failing lock turns this repo's CI red, and the
    repo is back in its clean, green state.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sam marks the CI jobs as required status checks on main</name>

  <files>none — a GitHub repository administration action</files>

  <read_first>
    - .github/workflows/ci.yml — the exact job names that must be selected in the branch-protection
      UI: `locks` and `build`
    - .planning/phases/01-platform-foundation-design-system-integration/01-CONTEXT.md — D-13, which
      states that a suite that runs but does not gate is not a lock
    - .planning/phases/01-platform-foundation-design-system-integration/01-VALIDATION.md —
      § Manual-Only Verifications, which records why this cannot be automated
  </read_first>

  <action>
    This is a blocking human gate. Branch protection lives in GitHub repository settings and requires
    admin rights on `beyondhousecleaning/bhc-website`, which an agent does not have.

    Before pausing, do the automatable preparation. Query the current protection state with
    `gh api repos/beyondhousecleaning/bhc-website/branches/main/protection` and record it — a 404
    means no rule exists yet, which is the expected starting point and must not be treated as an
    error. Confirm from `gh run list` that both job names, exactly `locks` and `build`, have already
    appeared on this repo, since GitHub's status-check picker only offers names it has observed.

    Then present the `<how-to-verify>` steps to Sam verbatim and stop. The two settings that matter
    are: require status checks to pass before merging, with both `locks` and `build` selected; and
    require branches to be up to date before merging, so a stale green run cannot satisfy the gate.
    Do not ask for a required-reviewer setting — this is a solo repo and it would only block Sam from
    merging his own work.

    After Sam replies, re-query the protection API and capture the exact response as the durable
    record that D-13 is satisfied. If `required_status_checks.contexts` does not contain both job
    names, the gate is not in place — report that rather than accepting "done" at face value.
  </action>

  <verify>
    <automated>gh run list --limit 10 --json name --jq '[.[].name] | unique'</automated>
    <automated>gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks'</automated>
    <automated>gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks.contexts | index("locks") != null and (index("build") != null)' | grep -qx true && echo "D-13 satisfied: both jobs are required status checks"</automated>
    <automated>gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks.strict' | grep -qx true && echo "stale runs cannot satisfy the gate"</automated>
  </verify>

  <what-built>
    CI is fully wired and demonstrably red-on-failure. `.github/workflows/ci.yml` runs on every push
    to every branch with two jobs: `locks` (the design-system lock suite on bare `node --test`, no
    install) and `build` (npm ci, the Next.js build, the built-HTML lock assertions, and the
    gzip transfer-weight performance budget). Task 1 of this plan proved end-to-end that a failing
    lock makes the run conclude `failure` — both run URLs are in this plan's summary.

    What is still missing is the binding half. Right now a red run is a signal, not a gate: a pull
    request with failing locks can still be merged. Branch protection lives in GitHub repository
    settings and requires admin rights on `beyondhousecleaning/bhc-website`, which an agent does not
    have.
  </what-built>

  <how-to-verify>
    1. Open `github.com/beyondhousecleaning/bhc-website/settings/branches`.
    2. Add (or edit) a branch protection rule — or a ruleset — targeting `main`.
    3. Enable **Require status checks to pass before merging**.
    4. In the status-check search box, select both `locks` and `build`. These names must appear
       because Task 1 has already produced runs on this repo; if the search box shows nothing, the
       runs have not landed on a branch GitHub associates with `main` yet — say so rather than
       guessing at names.
    5. Enable **Require branches to be up to date before merging** so a stale green run cannot
       satisfy the gate.
    6. Save, then reply "done".

    Do not enable anything that would prevent Sam from merging his own work — a required review is
    not wanted here; this is a solo repo. The only requirement is that the checks pass.
  </how-to-verify>

  <acceptance_criteria>
    - `gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks.contexts'`
      returns a list containing both `locks` and `build`
    - `gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks.strict'`
      returns `true`
    - No required-reviewer setting was added that would block Sam from merging his own pull requests
    - The exact API response is captured in the SUMMARY as the durable record that D-13 is satisfied
  </acceptance_criteria>

  <resume-signal>
    Type "done" once both `locks` and `build` are saved as required status checks on `main`, or
    describe what the settings page showed if the check names did not appear.
  </resume-signal>

  <done>
    A failing lock now blocks a merge into `main`, not merely reports one — D-13 is fully satisfied
    and Success Criterion 3 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → `main` | Branch protection is the only thing preventing unverified code from reaching the default branch. Until it is configured, this boundary is unenforced. |
| deliberately broken commit → repository history | This plan intentionally pushes a failing commit. It must not be able to reach `main` unreverted, and must not be mistaken later for a real regression. |
| GitHub Actions runner → repository | The workflow runs on every push, including on the probe commit, with a read-only token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24 | Repudiation (a gate that does not gate) | GitHub branch protection on `main` | mitigate | Task 2 makes `locks` and `build` required status checks with `strict: true`, so a red or stale run blocks a merge. This is the item D-13 explicitly forbids omitting, and it closes threat T-01-14 opened in Plan 03. |
| T-01-25 | Tampering (the control cannot fail) | `.github/workflows/ci.yml` | mitigate | Task 1 is a live negative control against the real runner, not a local simulation. A CI configuration that reports green regardless of test outcome is a worse failure than no CI, because it manufactures false confidence. |
| T-01-26 | Tampering (the probe outlives its purpose) | `design-system/test/__ci-gate-probe.test.js` | mitigate | The probe is a distinctly named throwaway file, created and deleted within the same task, with commit messages marking it temporary in both directions. Acceptance criteria assert `design-system/test/` contains exactly `locks.test.js` afterwards and that the working tree is clean. Using a separate file rather than editing `locks.test.js` removes any chance of a botched revert corrupting the real lock suite. |
| T-01-27 | Elevation of Privilege | The probe commit reaching `main` | accept | The probe is pushed to the phase branch, not to `main`, and is reverted within the same task before any merge. Once Task 2 lands, an equivalent commit could not merge into `main` anyway — which is the exact property being established. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. The `locks` job that carries the probe runs with no `node_modules` at all, which is precisely why it is a trustworthy gate. |
</threat_model>

<verification>
1. `gh run list --branch "$(git branch --show-current)" --limit 4 --json name,conclusion,headSha`
   → shows the probe commit's `locks` job as `failure` and the removal commit's jobs as `success`
2. `ls design-system/test` → exactly `locks.test.js`
3. `git status --porcelain` → empty
4. `node --test design-system/test/*.test.js` → `# pass 10`, `# fail 0`
5. `npm run verify` → exits 0
6. `gh api repos/beyondhousecleaning/bhc-website/branches/main/protection --jq '.required_status_checks'`
   → `contexts` contains `locks` and `build`, and `strict` is `true`
</verification>

<success_criteria>
- A recorded GitHub Actions run exists in which a deliberately failing lock made the `locks` job and
  the overall run conclude `failure` (D-13, SC-3b).
- A subsequent recorded run, after the probe was removed, concluded `success` for both jobs.
- The probe file is gone, `design-system/test/` contains only `locks.test.js`, and the working tree
  is clean.
- Both `locks` and `build` are required status checks on `main` with `strict: true`, so a failing
  lock blocks a merge rather than merely reporting one (D-13, SC-3b).
- The design-system lock suite still passes on bare `node --test` with no `node_modules` (D-12).
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation-design-system-integration/01-05-SUMMARY.md` when done.
Record: both CI run IDs and URLs (the red one and the green one), the `locks` job conclusion on each,
the exact `gh api .../branches/main/protection` response showing the required contexts, and
confirmation that the working tree is clean and `design-system/test/` contains only `locks.test.js`.
</output>
